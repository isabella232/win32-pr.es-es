---
title: Acerca de las listas de imágenes
description: Una lista de imágenes es una colección de imágenes del mismo tamaño, a las que se puede hacer referencia a través de su índice.
ms.assetid: vs|controls|~\controls\imagelist\imagelist.htm
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: f059e89b04d16088fff1d937bd29cb23a427d4c2
ms.sourcegitcommit: 5f33645661bf8c825a7a2e73950b1f4ea0f1cd82
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 08/21/2020
ms.locfileid: "104359439"
---
# <a name="about-image-lists"></a><span data-ttu-id="f5d5d-103">Acerca de las listas de imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-103">About Image Lists</span></span>

<span data-ttu-id="f5d5d-104">Una lista de imágenes es una colección de imágenes del mismo tamaño, a las que se puede hacer referencia a través de su índice.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-104">An image list is a collection of images of the same size, each of which can be referred to by its index.</span></span> <span data-ttu-id="f5d5d-105">Las listas de imágenes se utilizan para administrar de forma eficaz grandes conjuntos de iconos o mapas de bits.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-105">Image lists are used to efficiently manage large sets of icons or bitmaps.</span></span> <span data-ttu-id="f5d5d-106">Todas las imágenes de una lista de imágenes están contenidas en un único mapa de bits ancho en formato de dispositivo de pantalla.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-106">All images in an image list are contained in a single, wide bitmap in screen device format.</span></span> <span data-ttu-id="f5d5d-107">Una lista de imágenes también puede incluir un mapa de bits monocromo que contenga máscaras que se utilizan para dibujar imágenes de forma transparente (estilo de icono).</span><span class="sxs-lookup"><span data-stu-id="f5d5d-107">An image list can also include a monochrome bitmap that contains masks that are used to draw images transparently (icon style).</span></span>

<span data-ttu-id="f5d5d-108">La API de Microsoft Win32 proporciona funciones y macros de lista de imágenes que permiten crear y destruir listas de imágenes, agregar y quitar imágenes, reemplazar y combinar imágenes, dibujar imágenes y arrastrar imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-108">The Microsoft Win32 API provides image list functions and macros that enable you to create and destroy image lists, add and remove images, replace and merge images, draw images, and drag images.</span></span> <span data-ttu-id="f5d5d-109">Para usar las funciones de lista de imágenes, incluya el archivo de encabezado de control común en los archivos de código fuente y vincule con la biblioteca de exportación de control común.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-109">To use the image list functions, include the common control header file in your source code files and link with the common control export library.</span></span> <span data-ttu-id="f5d5d-110">Además, antes de llamar a cualquier función de lista de imágenes, use la función [**InitCommonControls**](/windows/desktop/api/Commctrl/nf-commctrl-initcommoncontrols) o [**InitCommonControlsEx**](/windows/desktop/api/Commctrl/nf-commctrl-initcommoncontrolsex) para asegurarse de que se carga el archivo dll de control común.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-110">In addition, before calling any image list function, use the [**InitCommonControls**](/windows/desktop/api/Commctrl/nf-commctrl-initcommoncontrols) or [**InitCommonControlsEx**](/windows/desktop/api/Commctrl/nf-commctrl-initcommoncontrolsex) function to ensure that the common control DLL is loaded.</span></span>

<span data-ttu-id="f5d5d-111">En esta sección se explican los temas siguientes:</span><span class="sxs-lookup"><span data-stu-id="f5d5d-111">The following topics are discussed in this section:</span></span>

-   [<span data-ttu-id="f5d5d-112">Tipos</span><span class="sxs-lookup"><span data-stu-id="f5d5d-112">Types</span></span>](#types)
-   [<span data-ttu-id="f5d5d-113">Crear y destruir listas de imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-113">Creating and Destroying Image Lists</span></span>](#creating-and-destroying-image-lists)
-   [<span data-ttu-id="f5d5d-114">Adición y eliminación de imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-114">Adding and Removing Images</span></span>](#adding-and-removing-images)
-   [<span data-ttu-id="f5d5d-115">Reemplazar y combinar imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-115">Replacing and Merging Images</span></span>](#replacing-and-merging-images)
-   [<span data-ttu-id="f5d5d-116">Dibujar imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-116">Drawing Images</span></span>](#drawing-images)
-   [<span data-ttu-id="f5d5d-117">Arrastrar imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-117">Dragging Images</span></span>](#dragging-images)
-   [<span data-ttu-id="f5d5d-118">Información de imagen</span><span class="sxs-lookup"><span data-stu-id="f5d5d-118">Image Information</span></span>](#image-information)
-   [<span data-ttu-id="f5d5d-119">Superposiciones de imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-119">Image Overlays</span></span>](#image-overlays)
-   [<span data-ttu-id="f5d5d-120">Iconos sin alias de 32 bits</span><span class="sxs-lookup"><span data-stu-id="f5d5d-120">32-Bit Antialiased Icons</span></span>](#32-bit-antialiased-icons)

## <a name="types"></a><span data-ttu-id="f5d5d-121">Tipos</span><span class="sxs-lookup"><span data-stu-id="f5d5d-121">Types</span></span>

<span data-ttu-id="f5d5d-122">Hay dos tipos de listas de imágenes: no enmascaradas ni enmascaradas.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-122">There are two types of image lists: nonmasked and masked.</span></span> <span data-ttu-id="f5d5d-123">Una lista de imágenes no enmascaradas consta de un mapa de bits de color que contiene una o varias imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-123">A nonmasked image list consists of a color bitmap that contains one or more images.</span></span> <span data-ttu-id="f5d5d-124">Una lista de imágenes enmascaradas consta de dos mapas de bits de igual tamaño.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-124">A masked image list consists of two bitmaps of equal size.</span></span> <span data-ttu-id="f5d5d-125">El primero es un mapa de bits de color que contiene las imágenes y el segundo es un mapa de bits monocromo que contiene una serie de máscaras, una para cada imagen del primer mapa de bits.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-125">The first is a color bitmap that contains the images, and the second is a monochrome bitmap that contains a series of masks—one for each image in the first bitmap.</span></span>

<span data-ttu-id="f5d5d-126">Cuando se dibuja una imagen no enmascarada, simplemente se copia en el contexto del dispositivo de destino. es decir, se dibuja sobre el color de fondo existente del contexto del dispositivo.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-126">When a nonmasked image is drawn, it is simply copied into the target device context; that is, it is drawn over the existing background color of the device context.</span></span> <span data-ttu-id="f5d5d-127">Cuando se dibuja una imagen enmascarada, los bits de la imagen se combinan con los bits de la máscara, normalmente produciendo áreas transparentes en el mapa de bits en el que se muestra el color de fondo del contexto del dispositivo de destino.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-127">When a masked image is drawn, the bits of the image are combined with the bits of the mask, typically producing transparent areas in the bitmap where the background color of the target device context shows through.</span></span> <span data-ttu-id="f5d5d-128">Hay varios estilos de dibujo que puede especificar al dibujar una imagen enmascarada.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-128">There are several drawing styles that you can specify when drawing a masked image.</span></span> <span data-ttu-id="f5d5d-129">Por ejemplo, puede especificar que la imagen se haya desactivado para indicar un objeto seleccionado.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-129">For example, you can specify that the image be dithered to indicate a selected object.</span></span>

## <a name="creating-and-destroying-image-lists"></a><span data-ttu-id="f5d5d-130">Crear y destruir listas de imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-130">Creating and Destroying Image Lists</span></span>

<span data-ttu-id="f5d5d-131">Para crear una lista de imágenes, llame a la función [**ImageList \_ Create**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_create) .</span><span class="sxs-lookup"><span data-stu-id="f5d5d-131">You create an image list by calling the [**ImageList\_Create**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_create) function.</span></span> <span data-ttu-id="f5d5d-132">Los parámetros incluyen el tipo de lista de imágenes que se va a crear, las dimensiones de cada imagen y el número de imágenes que se van a agregar a la lista.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-132">The parameters include the type of image list to create, the dimensions of each image, and the number of images you intend to add to the list.</span></span> <span data-ttu-id="f5d5d-133">En el caso de una lista de imágenes sin máscara, la función crea un mapa de bits único lo suficientemente grande como para contener el número especificado de imágenes de las dimensiones especificadas.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-133">For a nonmasked image list, the function creates a single bitmap large enough to hold the specified number of images of the given dimensions.</span></span> <span data-ttu-id="f5d5d-134">Después, crea un contexto de dispositivo compatible con la pantalla y selecciona el mapa de bits en él.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-134">Then it creates a screen-compatible device context and selects the bitmap into it.</span></span> <span data-ttu-id="f5d5d-135">En el caso de una lista de imágenes enmascaradas, la función crea dos mapas de bits y dos contextos de dispositivo compatibles con la pantalla.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-135">For a masked image list, the function creates two bitmaps and two screen-compatible device contexts.</span></span> <span data-ttu-id="f5d5d-136">Selecciona el mapa de bits de la imagen en un contexto de dispositivo y el mapa de bits de la máscara en el otro.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-136">It selects the image bitmap into one device context and the mask bitmap into the other.</span></span> <span data-ttu-id="f5d5d-137">El archivo DLL de control común contiene el código ejecutable para las funciones de lista de imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-137">The common control DLL contains the executable code for the image list functions.</span></span>

<span data-ttu-id="f5d5d-138">En [**ImageList \_ crear**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_create), se especifica el número inicial de imágenes que se incluirán en una lista de imágenes, así como el número de imágenes que puede alcanzar la lista.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-138">In [**ImageList\_Create**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_create), you specify the initial number of images that will be in an image list as well as the number of images by which the list can grow.</span></span> <span data-ttu-id="f5d5d-139">Por lo tanto, si intenta agregar más imágenes de las especificadas inicialmente, la lista de imágenes crece automáticamente para alojar las nuevas imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-139">So if you attempt to add more images than you initially specified, the image list automatically grows to accommodate the new images.</span></span>

<span data-ttu-id="f5d5d-140">Si [**ImageList \_ crea**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_create) correctamente, devuelve un identificador al tipo HIMAGELIST.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-140">If [**ImageList\_Create**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_create) succeeds, it returns a handle to the HIMAGELIST type.</span></span> <span data-ttu-id="f5d5d-141">Este identificador se usa en otras funciones de lista de imágenes para tener acceso a la lista de imágenes y administrar las imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-141">You use this handle in other image list functions to access the image list and manage the images.</span></span> <span data-ttu-id="f5d5d-142">Puede Agregar y quitar imágenes, copiar imágenes de una lista de imágenes a otra y combinar imágenes de dos listas de imágenes diferentes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-142">You can add and remove images, copy images from one image list to another, and merge images from two different image lists.</span></span> <span data-ttu-id="f5d5d-143">Cuando ya no necesite una lista de imágenes, puede destruirla especificando su identificador en una llamada a la función [**ImageList \_ Destroy**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_destroy) .</span><span class="sxs-lookup"><span data-stu-id="f5d5d-143">When you no longer need an image list, you can destroy it by specifying its handle in a call to the [**ImageList\_Destroy**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_destroy) function.</span></span>

## <a name="adding-and-removing-images"></a><span data-ttu-id="f5d5d-144">Adición y eliminación de imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-144">Adding and Removing Images</span></span>

<span data-ttu-id="f5d5d-145">Puede Agregar imágenes de mapa de imagen, iconos o cursores a una lista de imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-145">You can add bitmapped images, icons, or cursors to an image list.</span></span> <span data-ttu-id="f5d5d-146">Agregue imágenes de mapa de bits especificando los identificadores de dos mapas de bits en una llamada a la función de [**\_ adición ImageList**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_add) .</span><span class="sxs-lookup"><span data-stu-id="f5d5d-146">You add bitmapped images by specifying the handles to two bitmaps in a call to the [**ImageList\_Add**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_add) function.</span></span> <span data-ttu-id="f5d5d-147">El primer mapa de bits contiene una o más imágenes para agregar al mapa de bits de la imagen, y el segundo mapa de bits contiene las máscaras que se van a agregar al mapa de bits de la máscara.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-147">The first bitmap contains one or more images to add to the image bitmap, and the second bitmap contains the masks to add to the mask bitmap.</span></span> <span data-ttu-id="f5d5d-148">En el caso de las listas de imágenes no enmascaradas, se omite el segundo identificador de mapa de bits; se puede establecer en **null**.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-148">For nonmasked image lists, the second bitmap handle is ignored; it can be set to **NULL**.</span></span>

<span data-ttu-id="f5d5d-149">La función [**ImageList \_ AddMasked**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_addmasked) también agrega imágenes de mapa Dete a una lista de imágenes enmascaradas.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-149">The [**ImageList\_AddMasked**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_addmasked) function also adds bitmapped images to a masked image list.</span></span> <span data-ttu-id="f5d5d-150">Esta función es similar a [**ImageList \_ Add**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_add), salvo que no se especifica un mapa de bits de máscara.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-150">This function is similar to [**ImageList\_Add**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_add), except that you do not specify a mask bitmap.</span></span> <span data-ttu-id="f5d5d-151">En su lugar, se especifica un color que el sistema combina con el mapa de bits de la imagen para generar automáticamente las máscaras.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-151">Instead, you specify a color that the system combines with the image bitmap to automatically generate the masks.</span></span> <span data-ttu-id="f5d5d-152">Cada píxel del color especificado en el mapa de bits de la imagen se cambia a negro y el bit correspondiente de la máscara se establece en 1.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-152">Each pixel of the specified color in the image bitmap is changed to black, and the corresponding bit in the mask is set to 1.</span></span> <span data-ttu-id="f5d5d-153">Como resultado, cualquier píxel de la imagen que coincida con el color especificado es transparente cuando se dibuja la imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-153">As a result, any pixel in the image that matches the specified color is transparent when the image is drawn.</span></span>

<span data-ttu-id="f5d5d-154">La macro [**ImageList \_ AddIcon**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_addicon) agrega un icono o un cursor a una lista de imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-154">The [**ImageList\_AddIcon**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_addicon) macro adds an icon or cursor to an image list.</span></span> <span data-ttu-id="f5d5d-155">Si la lista de imágenes está enmascarada, **ImageList \_ AddIcon** agrega la máscara que se proporciona con el icono o el cursor al mapa de bits de la máscara.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-155">If the image list is masked, **ImageList\_AddIcon** adds the mask that is provided with the icon or cursor to the mask bitmap.</span></span> <span data-ttu-id="f5d5d-156">Si la lista de imágenes no está enmascarada, la máscara del icono o del cursor no se utiliza al dibujar la imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-156">If the image list is nonmasked, the mask for the icon or cursor is not used when drawing the image.</span></span>

<span data-ttu-id="f5d5d-157">Puede crear un icono basado en una imagen y una máscara en una lista de imágenes mediante la función [**ImageList \_ GetIcon**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_geticon) .</span><span class="sxs-lookup"><span data-stu-id="f5d5d-157">You can create an icon based on an image and mask in an image list by using the [**ImageList\_GetIcon**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_geticon) function.</span></span> <span data-ttu-id="f5d5d-158">La función devuelve el identificador del nuevo icono.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-158">The function returns the handle to the new icon.</span></span>

<span data-ttu-id="f5d5d-159">[**ImageList \_ Add**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_add), [**ImageList \_ AddMasked**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_addmasked)y [**ImageList \_ AddIcon**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_addicon) asignan un índice a cada imagen a medida que se agrega a una lista de imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-159">[**ImageList\_Add**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_add), [**ImageList\_AddMasked**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_addmasked), and [**ImageList\_AddIcon**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_addicon) assign an index to each image as it is added to an image list.</span></span> <span data-ttu-id="f5d5d-160">Los índices son de base cero; es decir, la primera imagen de la lista tiene un índice de cero, el siguiente tiene un índice de uno, y así sucesivamente.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-160">The indexes are zero-based; that is, the first image in the list has an index of zero, the next has an index of one, and so on.</span></span> <span data-ttu-id="f5d5d-161">Después de agregar una sola imagen, las funciones devuelven el índice de la imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-161">After adding a single image, the functions return the index of the image.</span></span> <span data-ttu-id="f5d5d-162">Cuando se agrega más de una imagen a la vez, las funciones devuelven el índice de la primera imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-162">When more than one image is added at a time, the functions return the index of the first image.</span></span>

<span data-ttu-id="f5d5d-163">La función [**ImageList \_ Remove**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_remove) quita una imagen de una lista de imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-163">The [**ImageList\_Remove**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_remove) function removes an image from an image list.</span></span>

## <a name="replacing-and-merging-images"></a><span data-ttu-id="f5d5d-164">Reemplazar y combinar imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-164">Replacing and Merging Images</span></span>

<span data-ttu-id="f5d5d-165">Las funciones [**ImageList \_ Replace**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_replace) y [**ImageList \_ ReplaceIcon**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_replaceicon) reemplazan una imagen de una lista de imágenes con una nueva imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-165">The [**ImageList\_Replace**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_replace) and [**ImageList\_ReplaceIcon**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_replaceicon) functions replace an image in an image list with a new image.</span></span> <span data-ttu-id="f5d5d-166">**ImageList \_ Replace** reemplaza una imagen por una imagen de mapa de imágenes y una máscara, y **ImageList \_ ReplaceIcon** reemplaza una imagen por un icono o un cursor.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-166">**ImageList\_Replace** replaces an image with a bitmapped image and mask, and **ImageList\_ReplaceIcon** replaces an image with an icon or cursor.</span></span>

<span data-ttu-id="f5d5d-167">La función de [**\_ combinación ImageList**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_merge) combina dos imágenes, almacenando la nueva imagen en una nueva lista de imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-167">The [**ImageList\_Merge**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_merge) function merges two images, storing the new image in a new image list.</span></span> <span data-ttu-id="f5d5d-168">La nueva imagen se crea dibujando la segunda imagen de forma transparente sobre la primera.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-168">The new image is created by drawing the second image transparently over the first.</span></span> <span data-ttu-id="f5d5d-169">La máscara de la nueva imagen es el resultado de realizar una operación **or** lógica en los bits de las máscaras de las dos imágenes existentes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-169">The mask for the new image is the result of performing a logical **OR** operation on the bits of the masks for the two existing images.</span></span>

## <a name="drawing-images"></a><span data-ttu-id="f5d5d-170">Dibujar imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-170">Drawing Images</span></span>

<span data-ttu-id="f5d5d-171">Para dibujar una imagen, use la función [**ImageList \_ Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) o [**ImageList \_ DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) .</span><span class="sxs-lookup"><span data-stu-id="f5d5d-171">To draw an image, you use the [**ImageList\_Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) or [**ImageList\_DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) function.</span></span> <span data-ttu-id="f5d5d-172">Especifique el identificador de una lista de imágenes, el índice de la imagen que se va a dibujar, el identificador del contexto de dispositivo de destino, una ubicación en el contexto del dispositivo y uno o varios estilos de dibujo.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-172">You specify the handle to an image list, the index of the image to draw, the handle to the destination device context, a location within the device context, and one or more drawing styles.</span></span>

<span data-ttu-id="f5d5d-173">Al especificar el estilo **\_ transparente rar** , [**ImageList \_ Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) o ImageList [**\_ DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) usa un proceso de dos pasos para dibujar una imagen enmascarada.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-173">When you specify the **ILD\_TRANSPARENT** style, [**ImageList\_Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) or [**ImageList\_DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) uses a two-step process to draw a masked image.</span></span> <span data-ttu-id="f5d5d-174">En primer lugar, realiza una operación **and** lógica en los bits de la imagen y los bits de la máscara.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-174">First, it performs a logical **AND** operation on the bits of the image and the bits of the mask.</span></span> <span data-ttu-id="f5d5d-175">A continuación, realiza una operación **XOR** lógica en los resultados de la primera operación y los bits de fondo del contexto del dispositivo de destino.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-175">Then it performs a logical **XOR** operation on the results of the first operation and the background bits of the destination device context.</span></span> <span data-ttu-id="f5d5d-176">Este proceso crea áreas transparentes en la imagen resultante; es decir, cada bit en blanco de la máscara hace que el bit correspondiente de la imagen resultante sea transparente.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-176">This process creates transparent areas in the resulting image; that is, each white bit in the mask causes the corresponding bit in the resulting image to be transparent.</span></span>

<span data-ttu-id="f5d5d-177">Antes de dibujar una imagen enmascarada en un fondo de color sólido, debe usar la función [**ImageList \_ SetBkColor**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_setbkcolor) para establecer el color de fondo de la lista de imágenes en el mismo color que el destino.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-177">Before drawing a masked image on a solid color background, you should use the [**ImageList\_SetBkColor**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_setbkcolor) function to set the background color of the image list to the same color as the destination.</span></span> <span data-ttu-id="f5d5d-178">Al establecer el color, se elimina la necesidad de crear áreas transparentes en la imagen y se habilita [**ImageList \_ Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) o [**ImageList \_ DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) para copiar simplemente la imagen en el contexto del dispositivo de destino, lo que da lugar a un aumento significativo del rendimiento.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-178">Setting the color eliminates the need to create transparent areas in the image and enables [**ImageList\_Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) or [**ImageList\_DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) to simply copy the image to the destination device context, resulting in a significant increase in performance.</span></span> <span data-ttu-id="f5d5d-179">Para dibujar la imagen, especifique el **estilo \_ normal rar** en una llamada a **ImageList \_ Draw** o **ImageList \_ DrawEx**.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-179">To draw the image, specify the **ILD\_NORMAL** style in a call to **ImageList\_Draw** or **ImageList\_DrawEx**.</span></span>

<span data-ttu-id="f5d5d-180">Puede establecer el color de fondo de una lista de imágenes enmascaradas en cualquier momento para que se dibuje correctamente en cualquier fondo sólido.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-180">You can set the background color for a masked image list at any time so that it draws correctly on any solid background.</span></span> <span data-ttu-id="f5d5d-181">Al establecer el color de fondo en CLR \_ None, las imágenes se dibujan de manera transparente de forma predeterminada.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-181">Setting the background color to CLR\_NONE causes images to be drawn transparently by default.</span></span> <span data-ttu-id="f5d5d-182">Para recuperar el color de fondo de una lista de imágenes, use la función [**ImageList \_ GetBkColor**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_getbkcolor) .</span><span class="sxs-lookup"><span data-stu-id="f5d5d-182">To retrieve the background color of an image list, use the [**ImageList\_GetBkColor**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_getbkcolor) function.</span></span>

<span data-ttu-id="f5d5d-183">Los estilos **rar \_ BLEND25** y **rar \_ BLEND50** interpolan la imagen con el color de resaltado del sistema.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-183">The **ILD\_BLEND25** and **ILD\_BLEND50** styles dither the image with the system highlight color.</span></span> <span data-ttu-id="f5d5d-184">Estos estilos son útiles si usa una imagen enmascarada para representar un objeto que el usuario puede seleccionar.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-184">These styles are useful if you use a masked image to represent an object that the user can select.</span></span> <span data-ttu-id="f5d5d-185">Por ejemplo, puede usar el estilo **rar \_ BLEND50** para dibujar la imagen cuando el usuario la selecciona.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-185">For example, you can use the **ILD\_BLEND50** style to draw the image when the user selects it.</span></span>

<span data-ttu-id="f5d5d-186">Una imagen no enmascarada se copia en el contexto de dispositivo de destino mediante la operación de trama SRCCOPY.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-186">A nonmasked image is copied to the destination device context by using the SRCCOPY raster operation.</span></span> <span data-ttu-id="f5d5d-187">Los colores de la imagen aparecen igual independientemente del color de fondo del contexto del dispositivo.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-187">The colors in the image appear the same regardless of the background color of the device context.</span></span> <span data-ttu-id="f5d5d-188">Los estilos de dibujo especificados en [**ImageList \_ Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) o [**ImageList \_ DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) también no tienen ningún efecto en la apariencia de una imagen no enmascarada.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-188">The drawing styles specified in [**ImageList\_Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) or [**ImageList\_DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) also have no effect on the appearance of a nonmasked image.</span></span>

## <a name="dragging-images"></a><span data-ttu-id="f5d5d-189">Arrastrar imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-189">Dragging Images</span></span>

<span data-ttu-id="f5d5d-190">La API de Win32 incluye funciones para arrastrar una imagen en la pantalla.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-190">The Win32 API includes functions for dragging an image on the screen.</span></span> <span data-ttu-id="f5d5d-191">Las funciones de arrastre mueven una imagen sin problemas, en color y sin intermitencia del cursor.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-191">The dragging functions move an image smoothly, in color, and without any flashing of the cursor.</span></span> <span data-ttu-id="f5d5d-192">Se pueden arrastrar imágenes enmascaradas y sin máscara.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-192">Both masked and unmasked images can be dragged.</span></span>

<span data-ttu-id="f5d5d-193">La función [**ImageList \_ BeginDrag**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_begindrag) inicia una operación de arrastre.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-193">The [**ImageList\_BeginDrag**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_begindrag) function begins a drag operation.</span></span> <span data-ttu-id="f5d5d-194">Los parámetros incluyen el identificador de la lista de imágenes, el índice de la imagen que se va a arrastrar y la ubicación de la zona activa dentro de la imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-194">The parameters include the handle to the image list, the index of the image to drag, and the location of the hot spot within the image.</span></span> <span data-ttu-id="f5d5d-195">La zona activa es un solo píxel que las funciones de arrastre reconocen como la ubicación de pantalla exacta de la imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-195">The hot spot is a single pixel that the dragging functions recognize as the exact screen location of the image.</span></span> <span data-ttu-id="f5d5d-196">Normalmente, una aplicación establece la zona activa para que coincida con la zona activa del cursor del mouse.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-196">Typically, an application sets the hot spot so that it coincides with the hot spot of the mouse cursor.</span></span> <span data-ttu-id="f5d5d-197">La función [**ImageList \_ DragMove**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_dragmove) mueve la imagen a una nueva ubicación.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-197">The [**ImageList\_DragMove**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_dragmove) function moves the image to a new location.</span></span>

<span data-ttu-id="f5d5d-198">La función [**\_ DragEnter de ImageList**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_dragenter) establece la posición inicial de la imagen de arrastre dentro de una ventana y dibuja la imagen en la posición.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-198">The [**ImageList\_DragEnter**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_dragenter) function sets the initial position of the drag image within a window and draws the image at the position.</span></span> <span data-ttu-id="f5d5d-199">Los parámetros incluyen el identificador de la ventana en la que se va a dibujar la imagen y las coordenadas de la posición inicial dentro de la ventana.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-199">The parameters include the handle to the window in which to draw the image and the coordinates of the initial position within the window.</span></span> <span data-ttu-id="f5d5d-200">Las coordenadas son relativas a la esquina superior izquierda de la ventana, no al área cliente.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-200">The coordinates are relative to the window's upper-left corner, not the client area.</span></span> <span data-ttu-id="f5d5d-201">Lo mismo se aplica a todas las funciones de arrastre de imagen que toman las coordenadas como parámetros.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-201">The same is true for all of the image dragging functions that take coordinates as parameters.</span></span> <span data-ttu-id="f5d5d-202">Esto significa que debe compensar el ancho de los elementos de ventana como el borde, la barra de título y la barra de menús al especificar las coordenadas.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-202">This means you must compensate for the widths of window elements such as the border, title bar, and menu bar when specifying the coordinates.</span></span> <span data-ttu-id="f5d5d-203">Si especifica un identificador de ventana **nulo** al llamar a la función **ImageList \_**, las funciones de arrastre dibujan la imagen en el contexto del dispositivo que está asociado a la ventana del escritorio y las coordenadas son relativas a la esquina superior izquierda de la pantalla.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-203">If you specify a **NULL** window handle when calling **ImageList\_DragEnter**, the dragging functions draw the image in the device context that is associated with the desktop window, and the coordinates are relative to the upper-left corner of the screen.</span></span>

<span data-ttu-id="f5d5d-204">La función [**ImageList \_ SetDragCursorImage**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_setdragcursorimage) crea una nueva imagen de arrastre combinando la imagen especificada (normalmente una imagen de cursor del mouse) con la imagen de arrastre actual.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-204">The [**ImageList\_SetDragCursorImage**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_setdragcursorimage) function creates a new drag image by combining the given image (typically a mouse cursor image) with the current drag image.</span></span> <span data-ttu-id="f5d5d-205">Dado que las funciones de arrastre usan la nueva imagen durante una operación de arrastre, debe usar la función [**ShowCursor**](/windows/desktop/api/winuser/nf-winuser-showcursor) para ocultar el cursor del mouse real después de llamar a **ImageList \_ SetDragCursorImage**.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-205">Because the dragging functions use the new image during a drag operation, you should use the [**ShowCursor**](/windows/desktop/api/winuser/nf-winuser-showcursor) function to hide the actual mouse cursor after calling **ImageList\_SetDragCursorImage**.</span></span> <span data-ttu-id="f5d5d-206">De lo contrario, puede parecer que el sistema tiene dos cursores del mouse mientras dure la operación de arrastre.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-206">Otherwise, the system may appear to have two mouse cursors for the duration of the drag operation.</span></span>

<span data-ttu-id="f5d5d-207">Cuando una aplicación llama a [**ImageList \_ BeginDrag**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_begindrag), el sistema crea una lista de imágenes temporal e internas y, a continuación, copia la imagen de arrastre especificada en la lista interna.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-207">When an application calls [**ImageList\_BeginDrag**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_begindrag), the system creates a temporary, internal image list and then copies the specified drag image to the internal list.</span></span> <span data-ttu-id="f5d5d-208">Puede recuperar el identificador de la lista de imágenes de arrastre temporal mediante la función [**ImageList \_ GetDragImage**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_getdragimage) .</span><span class="sxs-lookup"><span data-stu-id="f5d5d-208">You can retrieve the handle to the temporary drag image list by using the [**ImageList\_GetDragImage**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_getdragimage) function.</span></span> <span data-ttu-id="f5d5d-209">La función también recupera la posición de arrastre actual y el desplazamiento de la imagen de arrastre con respecto a la posición de arrastre.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-209">The function also retrieves the current drag position and the offset of the drag image relative to the drag position.</span></span>

## <a name="image-information"></a><span data-ttu-id="f5d5d-210">Información de imagen</span><span class="sxs-lookup"><span data-stu-id="f5d5d-210">Image Information</span></span>

<span data-ttu-id="f5d5d-211">Hay una serie de funciones que recuperan información de una lista de imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-211">There are a number of functions that retrieve information from an image list.</span></span> <span data-ttu-id="f5d5d-212">La función [**ImageList \_ GetImageInfo**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_getimageinfo) rellena una estructura [**IMAGEINFO**](/windows/desktop/api) con información sobre una sola imagen, incluidos los identificadores de los mapas de bits de la imagen y de la máscara, el número de planos de color y los bits por píxel, y el rectángulo delimitador de la imagen dentro del mapa de bits de la imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-212">The [**ImageList\_GetImageInfo**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_getimageinfo) function fills an [**IMAGEINFO**](/windows/desktop/api) structure with information about a single image, including the handles of the image and mask bitmaps, the number of color planes and bits per pixel, and the bounding rectangle of the image within the image bitmap.</span></span> <span data-ttu-id="f5d5d-213">Puede usar esta información para manipular directamente los mapas de bits de la imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-213">You can use this information to directly manipulate the bitmaps for the image.</span></span> <span data-ttu-id="f5d5d-214">La función [**ImageList \_ GetImageCount**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_getimagecount) recupera el número de imágenes de una lista de imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-214">The [**ImageList\_GetImageCount**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_getimagecount) function retrieves the number of images in an image list.</span></span>

## <a name="image-overlays"></a><span data-ttu-id="f5d5d-215">Superposiciones de imágenes</span><span class="sxs-lookup"><span data-stu-id="f5d5d-215">Image Overlays</span></span>

<span data-ttu-id="f5d5d-216">Cada lista de imágenes incluye una lista de los índices que se van a usar como superposiciones.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-216">Every image list includes a list of indexes to use as overlays.</span></span> <span data-ttu-id="f5d5d-217">Una superposición es una imagen que se dibuja de forma transparente sobre otra imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-217">An overlay is an image that is drawn transparently over another image.</span></span> <span data-ttu-id="f5d5d-218">Cualquier imagen que se encuentre actualmente en la lista de imágenes se puede usar como una superposición.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-218">Any image currently in the image list can be used as an overlay.</span></span> <span data-ttu-id="f5d5d-219">Puede especificar hasta cuatro superposiciones por lista de imágenes.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-219">You can specify up to four overlays per image list.</span></span> <span data-ttu-id="f5d5d-220">Este límite se ha ampliado a 15 en la versión 4,71.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-220">This limit has been expanded to 15 in version 4.71.</span></span>

<span data-ttu-id="f5d5d-221">Agregue el índice de una imagen a la lista de superposiciones mediante la función [**ImageList \_ SetOverlayImage**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_setoverlayimage) , especificando el identificador de la lista de imágenes, el índice de la imagen existente y el índice de superposición deseado.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-221">You add the index of an image to the list of overlays by using the [**ImageList\_SetOverlayImage**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_setoverlayimage) function, specifying the handle to the image list, the index of the existing image, and the desired overlay index.</span></span> <span data-ttu-id="f5d5d-222">Esto, en efecto, indica a la lista de imágenes que "la imagen en el índice *x* se puede usar como superposición y quiero hacer referencia a la superposición como índice de superposición *y*".</span><span class="sxs-lookup"><span data-stu-id="f5d5d-222">This, in effect, tells the image list that "the image at index *x* can be used as an overlay, and I want to refer to the overlay as overlay index *y*."</span></span> <span data-ttu-id="f5d5d-223">Los índices de superposición están basados en uno en lugar de basarse en cero porque un índice de superposición de cero significa que no se usará ninguna superposición.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-223">The overlay indexes are one-based rather than zero-based because an overlay index of zero means that no overlay will be used.</span></span>

<span data-ttu-id="f5d5d-224">Se especifica una superposición al dibujar una imagen con la función [**\_ DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) de [**ImageList \_ Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) o ImageList.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-224">You specify an overlay when drawing an image with the [**ImageList\_Draw**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_draw) or [**ImageList\_DrawEx**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_drawex) function.</span></span> <span data-ttu-id="f5d5d-225">La superposición se especifica mediante la realización de una operación **or** lógica entre las marcas de dibujo deseadas y el resultado de la macro [**INDEXTOOVERLAYMASK**](/windows/desktop/api/Commctrl/nf-commctrl-indextooverlaymask) .</span><span class="sxs-lookup"><span data-stu-id="f5d5d-225">The overlay is specified by performing a logical **OR** operation between the desired drawing flags and the result of the [**INDEXTOOVERLAYMASK**](/windows/desktop/api/Commctrl/nf-commctrl-indextooverlaymask) macro.</span></span> <span data-ttu-id="f5d5d-226">La macro **INDEXTOOVERLAYMASK** toma el índice de superposición y lo formatea para su inclusión con las marcas de estas funciones.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-226">The **INDEXTOOVERLAYMASK** macro takes the overlay index and formats it for inclusion with the flags for these functions.</span></span> <span data-ttu-id="f5d5d-227">Esto dibujará la imagen con la superposición especificada.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-227">This will draw the image with the specified overlay.</span></span> <span data-ttu-id="f5d5d-228">En el ejemplo siguiente se muestra cómo se agrega una superposición y se especifica al dibujar la imagen.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-228">The following example demonstrates how an overlay is added and specified when drawing the image.</span></span>


```
ImageList_SetOverlayImage(himl, 0, 3);
ImageList_Draw(himl, 1, hdc, 0, 0, ILD_MASK | INDEXTOOVERLAYMASK(3));
```



<span data-ttu-id="f5d5d-229">Esto dibujará la imagen 1 y, a continuación, superpondrá la imagen con la imagen 0.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-229">This will draw image 1 and then overlay that image with image 0.</span></span> <span data-ttu-id="f5d5d-230">Dado que 3 es el índice de superposición que especificó en la llamada [**ImageList \_ SetOverlayImage**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_setoverlayimage) , 3 se coloca en la macro [**INDEXTOOVERLAYMASK**](/windows/desktop/api/Commctrl/nf-commctrl-indextooverlaymask) .</span><span class="sxs-lookup"><span data-stu-id="f5d5d-230">Because 3 is the overlay index that you specified in the [**ImageList\_SetOverlayImage**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_setoverlayimage) call, 3 is placed in the [**INDEXTOOVERLAYMASK**](/windows/desktop/api/Commctrl/nf-commctrl-indextooverlaymask) macro.</span></span>

## <a name="32-bit-antialiased-icons"></a><span data-ttu-id="f5d5d-231">Iconos sin alias de 32 bits</span><span class="sxs-lookup"><span data-stu-id="f5d5d-231">32-Bit Antialiased Icons</span></span>

<span data-ttu-id="f5d5d-232">El suavizado de contorno es una técnica para suavizar o desenfocar bordes nítidos.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-232">Antialiasing is a technique for softening or blurring sharp edges.</span></span> <span data-ttu-id="f5d5d-233">Esto proporciona a las imágenes una apariencia más natural.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-233">This gives images a more natural appearance.</span></span> <span data-ttu-id="f5d5d-234">Las listas de imágenes en Windows Vista y Windows 7 admiten el uso de los iconos y mapas de bits con suavizado de 32 bits.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-234">Image lists in Windows Vista and Windows 7 support the use of 32-bit antialiased icons and bitmaps.</span></span> <span data-ttu-id="f5d5d-235">Los valores de color usan 24 bits y se usan 8 bits como canal alfa en los iconos.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-235">Color values use 24 bits, and 8 bits are used as an alpha channel on the icons.</span></span> <span data-ttu-id="f5d5d-236">Para crear una lista de imágenes que pueda controlar una imagen de 32 bits por píxel (BPP), llame a la función de [**\_ creación ImageList**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_create) , pasando una \_ marca COLOR32 de ILC.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-236">To create an image list that can handle a 32 bits-per-pixel (bpp) image, call the [**ImageList\_Create**](/windows/desktop/api/Commctrl/nf-commctrl-imagelist_create) function, passing in an ILC\_COLOR32 flag.</span></span>

<span data-ttu-id="f5d5d-237">Para crear iconos de 32 bits correctamente, debe crear varias imágenes para cada icono, tal como se muestra en la siguiente ilustración.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-237">To author 32-bit icons correctly, you must create multiple images for each icon, as shown in the following illustration.</span></span>

![Ilustración que muestra tres tamaños de iconos para cada una de las tres profundidades de color](images/theme2.png)

-   <span data-ttu-id="f5d5d-239">Las tres primeras imágenes están en modo de 16 colores para su uso en modo seguro.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-239">The first three images are in 16-color mode for use in safe mode.</span></span>
-   <span data-ttu-id="f5d5d-240">Los tres iconos siguientes se usan en el modo de 256 colores.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-240">The next three icons are used in 256-color mode.</span></span>
-   <span data-ttu-id="f5d5d-241">Los tres últimos iconos tienen el canal alfa y solo se pueden usar en sistemas operativos que ejecutan un color de 24 bits o superior.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-241">The last three icons have the alpha channel and can be used only in operating systems running 24-bit color or higher.</span></span>
-   <span data-ttu-id="f5d5d-242">El orden de las imágenes en el formato de icono es importante.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-242">The order of images in the icon format does matter.</span></span> <span data-ttu-id="f5d5d-243">Si el orden es incorrecto, las versiones anteriores de Windows funcionarán mal al extraer los iconos.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-243">If the order is wrong, older versions of Windows function poorly when extracting the icons.</span></span> <span data-ttu-id="f5d5d-244">La extracción incorrecta de los iconos puede producir daños en la memoria y una representación incorrecta.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-244">Extracting the icons incorrectly can cause memory corruption and improper rendering.</span></span>
-   <span data-ttu-id="f5d5d-245">Las versiones anteriores de Windows tenían un límite de recursos de 10 iconos.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-245">Previous versions of Windows had a 10-icon resource limit.</span></span>

> [!Note]  
> <span data-ttu-id="f5d5d-246">Puede usar herramientas de terceros para generar archivos de icono y mapas de bits que contengan un canal alfa.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-246">You can use third party tools to generate icon files and bitmaps that contain an alpha channel.</span></span> <span data-ttu-id="f5d5d-247">Si usa [**LoadImage**](/windows/desktop/api/winuser/nf-winuser-loadimagea) para cargar un mapa de bits 32 bpp que contiene Alpha, debe especificar la marca LR \_ CREATEDIBSECTION.</span><span class="sxs-lookup"><span data-stu-id="f5d5d-247">If you use [**LoadImage**](/windows/desktop/api/winuser/nf-winuser-loadimagea) to load a 32 bpp bitmap that contains alpha, you need to specify the LR\_CREATEDIBSECTION flag.</span></span>

 

 

 