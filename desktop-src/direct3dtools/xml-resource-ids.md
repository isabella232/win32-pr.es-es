---
description: Identificadores de cadena de recursos establecidos por el llamador que se devolverán en los datos XML para visualizar objetos.
MS-HAID: vspixengine.Xml\_Resource\_Ids
MSHAttr:
- PreferredSiteName:MSDN
- PreferredLib:/library/windows/desktop
title: Enumeración Xml_Resource_Ids
ms.topic: reference
ms.date: 05/31/2018
ms.assetid: 1F2ECC78-73EC-4A59-8CF7-1BB7FB80244E
api_name:
- Xml_Resource_Ids
api_type:
- HeaderDef
api_location:
- vspixengine.h
topic_type:
- APIRef
- kbSyntax
ms.openlocfilehash: 1dca06e32962d35eed7ff52034648299ba0ddc89
ms.sourcegitcommit: a47bd86f517de76374e4fff33cfeb613eb259a7e
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 01/06/2021
ms.locfileid: "104274799"
---
# <a name="span-idvspixenginexml_resource_idsspanxml_resource_ids-enumeration"></a><span id="vspixengine.xml_resource_ids"></span>\_ \_ Enumeración de identificadores de recursos XML

Identificadores de cadena de recursos establecidos por el llamador para que se devuelvan en datos XML para visualizar objetos

## <a name="syntax"></a>Sintaxis


```C++
};
```

## <a name="constants"></a>Constantes

<span id="XML_DEVICE_INFORMATION"></span><span id="xml_device_information"></span>**\_información del dispositivo XML \_**  
IDENTIFICADOR de recurso para la cadena "información del dispositivo" que se usa en los datos XML.

<span id="XML_IA_STATE"></span><span id="xml_ia_state"></span>**Estado de XML \_ IA \_**  
IDENTIFICADOR de recurso para la cadena "ensamblador de entrada" usado en los datos XML.

<span id="XML_VS_STATE"></span><span id="xml_vs_state"></span>**\_Estado de vs XML \_**  
IDENTIFICADOR de recurso para la cadena "Stage del sombreador de vértices" que se usa en los datos XML.

<span id="XML_GS_STATE"></span><span id="xml_gs_state"></span>**Estado de XML \_ GS \_**  
IDENTIFICADOR de recurso para la cadena "fase de sombreador de geometría" que se usa en los datos XML.

<span id="XML_RASTERIZER_STATE"></span><span id="xml_rasterizer_state"></span>**\_Estado del rasterizador XML \_**  
IDENTIFICADOR de recurso para la cadena "fase de rasterización" que se usa en los datos XML.

<span id="XML_PS_STATE"></span><span id="xml_ps_state"></span>**Estado de XML \_ PS \_**  
IDENTIFICADOR de recurso para la cadena "Stage del sombreador de píxeles" usado en los datos XML.

<span id="XML_OM_STATE"></span><span id="xml_om_state"></span>**\_Estado de OM XML \_**  
IDENTIFICADOR de recurso para la cadena "fase de fusión de salida" utilizada en los datos XML.

<span id="XML_HS_STATE"></span><span id="xml_hs_state"></span>**Estado de XML \_ HS \_**  
IDENTIFICADOR de recurso para la cadena "fase de sombreador de casco" que se usa en los datos XML.

<span id="XML_DS_STATE"></span><span id="xml_ds_state"></span>**Estado de XML \_ DS \_**  
IDENTIFICADOR de recurso para la cadena "fase del sombreador de dominios" que se usa en los datos XML.

<span id="XML_CS_STATE"></span><span id="xml_cs_state"></span>**\_Estado XML CS \_**  
IDENTIFICADOR de recurso para la cadena "fase de sombreador de cálculo" que se usa en los datos XML.

<span id="XML_CLASS_INSTANCE_11"></span><span id="xml_class_instance_11"></span>**\_Instancia de clase XML \_ \_ 11**  
IDENTIFICADOR de recurso para la cadena "instancias de clase" utilizada en los datos XML.

<span id="XML_DEVICE_10"></span><span id="xml_device_10"></span>**\_Dispositivo XML \_ 10**  
IDENTIFICADOR de recurso para la cadena "Device 10" que se usa en los datos XML.

<span id="XML_INPUT_LAYOUT_11"></span><span id="xml_input_layout_11"></span>**\_Diseño de entrada XML \_ \_ 11**  
IDENTIFICADOR de recurso para la cadena "diseño de entrada 11" usado en los datos XML.

<span id="XML_SAMPLER_STATE_11"></span><span id="xml_sampler_state_11"></span>**Estado de la \_ muestra XML \_ \_ 11**  
IDENTIFICADOR de recurso para la cadena "estado de muestra 11" que se usa en los datos XML.

<span id="XML_RASTER_STATE_11"></span><span id="xml_raster_state_11"></span>**Estado de XML \_ Raster \_ \_ 11**  
IDENTIFICADOR de recurso de la cadena "Raster State 11" que se usa en los datos XML.

<span id="XML_BLEND_STATE_11"></span><span id="xml_blend_state_11"></span>**\_Estado de mezcla XML \_ \_ 11**  
IDENTIFICADOR de recurso para la cadena "estado de Blend 11" que se usa en los datos XML.

<span id="XML_DEPTH_STENCIL_STATE_11"></span><span id="xml_depth_stencil_state_11"></span>**Estado de la \_ Galería de símbolos de profundidad XML \_ \_ \_**  
IDENTIFICADOR de recurso para la cadena "estado 11 de estarcido de profundidad" usado en los datos XML.

<span id="XML_UNORDERED_ACCESS_VIEW_11"></span><span id="xml_unordered_access_view_11"></span>**\_Vista de acceso sin ordenar XML \_ \_ \_ 11**  
IDENTIFICADOR de recurso para la cadena "vista de acceso desordenado 11" que se usa en los datos XML.

<span id="XML_SHADER_RESOURCE_VIEW_11"></span><span id="xml_shader_resource_view_11"></span>**\_Vista de recursos del sombreador XML \_ \_ \_ 11**  
IDENTIFICADOR de recurso para la cadena "vista de recursos del sombreador 11" que se usa en los datos XML.

<span id="XML_DEPTH_STENCIL_VIEW_11"></span><span id="xml_depth_stencil_view_11"></span>**\_Vista de galería de símbolos de profundidad XML \_ \_ \_ 11**  
IDENTIFICADOR de recurso para la cadena "vista de estarcido de profundidad 11" que se usa en los datos XML.

<span id="XML_RENDER_TARGET_VIEW_11"></span><span id="xml_render_target_view_11"></span>**\_Vista de destino de representación XML \_ \_ \_ 11**  
IDENTIFICADOR de recurso para la cadena "vista de destino de presentación 11" que se usa en los datos XML.

<span id="XML_DEVICE_11"></span><span id="xml_device_11"></span>**\_Dispositivo XML \_ 11**  
IDENTIFICADOR de recurso para la cadena "Device 11" que se usa en los datos XML.

<span id="XML_SWAP_CHAIN"></span><span id="xml_swap_chain"></span>**\_cadena de intercambio XML \_**  
IDENTIFICADOR de recurso para la cadena "cadena de intercambio" utilizada en los datos XML.

<span id="XML_INPUT_LAYOUT_10"></span><span id="xml_input_layout_10"></span>**\_Diseño de entrada XML \_ \_ 10**  
IDENTIFICADOR de recurso para la cadena "diseño de entrada 10" usado en los datos XML.

<span id="XML_SAMPLER_STATE_10"></span><span id="xml_sampler_state_10"></span>**\_ \_ Estado \_ 10 de la muestra XML**  
IDENTIFICADOR de recurso para la cadena "estado de muestra 10" usado en los datos XML.

<span id="XML_RASTER_STATE_10"></span><span id="xml_raster_state_10"></span>**\_ \_ Estado \_ 10 de la trama de XML**  
IDENTIFICADOR de recurso para la cadena "estado de trama 10" que se usa en los datos XML.

<span id="XML_BLEND_STATE_10"></span><span id="xml_blend_state_10"></span>**\_ \_ Estado 10 de Blend XML \_**  
IDENTIFICADOR de recurso para la cadena "estado de Blend 10" que se usa en los datos XML.

<span id="XML_DEPTH_STENCIL_STATE_10"></span><span id="xml_depth_stencil_state_10"></span>**Estado de la \_ Galería de símbolos de profundidad XML \_ \_ \_ 10**  
IDENTIFICADOR de recurso para la cadena "estado 10 de estarcido de profundidad" usado en los datos XML.

<span id="XML_SHADER_RESOURCE_VIEW_10"></span><span id="xml_shader_resource_view_10"></span>**\_Vista de recursos del sombreador XML \_ \_ \_ 10**  
IDENTIFICADOR de recurso para la cadena "vista de recursos del sombreador 10" que se usa en los datos XML.

<span id="XML_DEPTH_STENCIL_VIEW_10"></span><span id="xml_depth_stencil_view_10"></span>**\_Vista de galería de símbolos de profundidad XML \_ \_ \_ 10**  
IDENTIFICADOR de recurso para la cadena "vista de estarcido de profundidad 10" que se usa en los datos XML.

<span id="XML_RENDER_TARGET_VIEW_10"></span><span id="xml_render_target_view_10"></span>**\_Vista de destino de representación XML \_ \_ \_ 10**  
IDENTIFICADOR de recurso para la cadena "vista de destino de representación 10" que se usa en los datos XML.

<span id="XML_VERTEX_DECLARATION"></span><span id="xml_vertex_declaration"></span>**\_declaración de vértice XML \_**  
IDENTIFICADOR de recurso para la cadena "Declaración de vértice" utilizada en los datos XML.

<span id="XML_STATE_BLOCK_STATE_10"></span><span id="xml_state_block_state_10"></span>**Estado de bloque de estado de XML \_ \_ \_ \_ 10**  
IDENTIFICADOR de recurso para la cadena "estado de bloque de estado 10" usado en los datos XML.

<span id="XML_DEVICE_CONTEXT_STATE_11"></span><span id="xml_device_context_state_11"></span>**\_Estado de contexto de dispositivo XML \_ \_ \_ 11**  
IDENTIFICADOR de recurso para la cadena "estado de contexto de dispositivo 11" usado en los datos XML.

<span id="XML_DEVICE_STATE_10"></span><span id="xml_device_state_10"></span>**\_ \_ Estado 10 del dispositivo XML \_**  
IDENTIFICADOR de recurso para la cadena "estado de dispositivo 10" que se usa en los datos XML.

<span id="XML_BACK_FACE"></span><span id="xml_back_face"></span>**frontal de XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "retroceso" que se usa en los datos XML.

<span id="XML_FRONT_FACE"></span><span id="xml_front_face"></span>**\_cara frontal \_ XML**  
IDENTIFICADOR de recurso para la cadena "cara frontal" utilizada en los datos XML.

<span id="XML_DEVICE_CREATION_INFORMATION"></span><span id="xml_device_creation_information"></span>**\_información de \_ creación de dispositivos XML \_**  
IDENTIFICADOR de recurso para la cadena "información de creación de dispositivos" utilizada en los datos XML.

<span id="XML_OTHER_INFORMATION"></span><span id="xml_other_information"></span>**\_otra \_ información XML**  
IDENTIFICADOR de recurso para la cadena "otra información" utilizada en los datos XML.

<span id="XML_MULTISAMPLING"></span><span id="xml_multisampling"></span>**\_muestreo múltiple XML**  
IDENTIFICADOR de recurso para la cadena "multimuestreing" que se usa en los datos XML.

<span id="XML_BACK_BUFFER"></span><span id="xml_back_buffer"></span>**\_búfer de reserva XML \_**  
IDENTIFICADOR de recurso para la cadena "búfer de reserva" usado en los datos XML.

<span id="XML_GENERAL_INFORMATION"></span><span id="xml_general_information"></span>**\_información general \_ XML**  
IDENTIFICADOR de recurso para la cadena "información general" utilizada en los datos XML.

<span id="XML_RENDER_TARGETS"></span><span id="xml_render_targets"></span>**\_destinos de representación XML \_**  
IDENTIFICADOR de recurso para la cadena "destinos de representación" que se usa en los datos XML.

<span id="XML_BLEND_DESCRIPTION"></span><span id="xml_blend_description"></span>**Descripción de XML \_ Blend \_**  
IDENTIFICADOR de recurso para la cadena "Descripción de Blend" utilizada en los datos XML.

<span id="XML_DEPTH_STENCIL"></span><span id="xml_depth_stencil"></span>**Galería de símbolos de \_ profundidad XML \_**  
IDENTIFICADOR de recurso para la cadena "estarcido de profundidad" que se usa en los datos XML.

<span id="XML_FRONT_FACE_STENCIL"></span><span id="xml_front_face_stencil"></span>**\_Galería de \_ símbolos de cara frontal XML \_**  
IDENTIFICADOR de recurso para la cadena "Galería de símbolos de cara frontal" que se usa en los datos XML.

<span id="XML_BACK_FACE_STENCIL"></span><span id="xml_back_face_stencil"></span>**\_Galería de \_ símbolos de caras de XML \_**  
IDENTIFICADOR de recurso para la cadena "Galería de símbolos de la superficie trasera" utilizada en los datos XML.

<span id="XML_DEVICE1_CREATION_INFORMATION"></span><span id="xml_device1_creation_information"></span>**Información de creación de XML \_ dispositivo1 \_ \_**  
IDENTIFICADOR de recurso para la cadena "información de creación de dispositivo1" utilizada en los datos XML.

<span id="XML_VIEWPORTS"></span><span id="xml_viewports"></span>**\_ventanillas XML**  
IDENTIFICADOR de recurso para la cadena "ventanillas" que se usa en los datos XML.

<span id="XML_SCISSOR_RECTS"></span><span id="xml_scissor_rects"></span>**\_rectángulos de tijeras XML \_**  
IDENTIFICADOR de recurso para la cadena "rectángulos rectangulares" que se usa en los datos XML.

<span id="XML_RASTERIZER"></span><span id="xml_rasterizer"></span>**\_rasterizador XML**  
IDENTIFICADOR de recurso para la cadena "rasterizador" que se usa en los datos XML.

<span id="XML_SHADER_GENERAL_INFORMATION"></span><span id="xml_shader_general_information"></span>**\_información general del sombreador XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "información general del sombreador" que se usa en los datos XML.

<span id="XML_SHADER_RESOURCES"></span><span id="xml_shader_resources"></span>**\_recursos del sombreador XML \_**  
IDENTIFICADOR de recurso para la cadena "recursos del sombreador" usado en los datos XML.

<span id="XML_SAMPLER_STATES"></span><span id="xml_sampler_states"></span>**\_Estados de muestra XML \_**  
IDENTIFICADOR de recurso para la cadena "Estados de muestra" que se usa en los datos XML.

<span id="XML_CONSTANT_BUFFERS"></span><span id="xml_constant_buffers"></span>**\_búferes de constantes XML \_**  
IDENTIFICADOR de recurso para la cadena "búferes de constantes" que se usa en los datos XML.

<span id="XML_STREAM_OUTPUT_BUFFERS"></span><span id="xml_stream_output_buffers"></span>**\_ \_ búferes de salida de flujo XML \_**  
IDENTIFICADOR de recurso para la cadena "búferes de salida de flujo" que se usa en los datos XML.

<span id="XML_INPUT_LAYOUT_GENERAL_INFORMATION"></span><span id="xml_input_layout_general_information"></span>**\_ \_ \_ información general sobre el diseño de entrada XML \_**  
IDENTIFICADOR de recurso para la cadena "información general sobre el diseño de entrada" que se usa en los datos XML.

<span id="XML_INPUT_LAYOUTS"></span><span id="xml_input_layouts"></span>**\_diseños de entrada XML \_**  
IDENTIFICADOR de recurso para la cadena "diseños de entrada" que se usa en los datos XML.

<span id="XML_VERTEX_BUFFERS"></span><span id="xml_vertex_buffers"></span>**\_búferes de vértices XML \_**  
IDENTIFICADOR de recurso para la cadena "búferes de vértice" que se usa en los datos XML.

<span id="XML_INDEX_BUFFERS"></span><span id="xml_index_buffers"></span>**\_búferes de índice XML \_**  
IDENTIFICADOR de recurso para la cadena "búferes de índice" que se usa en los datos XML.

<span id="XML_PRIMITIVE_TOP_GENERAL"></span><span id="xml_primitive_top_general"></span>**\_General de primitivos XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "primitiva Top general" que se usa en los datos XML.

<span id="XML_BLEND_STATE"></span><span id="xml_blend_state"></span>**\_Estado de Blend XML \_**  
IDENTIFICADOR de recurso para la cadena "estado de Blend" usado en los datos XML.

<span id="XML_UNORDERED_ACCESS_VIEWS"></span><span id="xml_unordered_access_views"></span>**vistas de acceso sin \_ ordenar XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "vistas de acceso desordenado" que se usa en los datos XML.

<span id="XML_CLASS_INSTANCES"></span><span id="xml_class_instances"></span>**\_instancias de clase XML \_**  
IDENTIFICADOR de recurso para la cadena "instancias de clase" utilizada en los datos XML.

<span id="XML_INSTANCE_INDEX"></span><span id="xml_instance_index"></span>**\_Índice de instancia XML \_**  
IDENTIFICADOR de recurso para la cadena "índice de instancia" que se usa en los datos XML.

<span id="XML_INSTANCE_NAME"></span><span id="xml_instance_name"></span>**\_nombre de instancia XML \_**  
IDENTIFICADOR de recurso para la cadena "nombre de instancia" que se usa en los datos XML.

<span id="XML_BASE_SAMPLER"></span><span id="xml_base_sampler"></span>**\_muestra de base XML \_**  
IDENTIFICADOR de recurso para la cadena "muestra de base" utilizada en los datos XML.

<span id="XML_BASE_TEXTURE"></span><span id="xml_base_texture"></span>**\_textura base \_ XML**  
IDENTIFICADOR de recurso para la cadena "base Texture" usada en los datos XML.

<span id="XML_BASE_CONSTANT_BUFFER_OFFSET"></span><span id="xml_base_constant_buffer_offset"></span>**\_desplazamiento de \_ búfer de constante de base XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "desplazamiento de búfer de constante base" usado en los datos XML.

<span id="XML_CONSTANT_BUFFER"></span><span id="xml_constant_buffer"></span>**\_búfer de constantes XML \_**  
IDENTIFICADOR de recurso para la cadena "búfer de constantes" usado en los datos XML.

<span id="XML_TYPE_ID"></span><span id="xml_type_id"></span>**\_identificador de tipo XML \_**  
IDENTIFICADOR de recurso para la cadena "ID. de tipo" usado en los datos XML.

<span id="XML_INSTANCE_ID"></span><span id="xml_instance_id"></span>**\_identificador de instancia XML \_**  
IDENTIFICADOR de recurso para la cadena "ID. de instancia" usado en los datos XML.

<span id="XML_TYPE_NAME"></span><span id="xml_type_name"></span>**\_nombre del tipo XML \_**  
IDENTIFICADOR de recurso para la cadena "nombre de tipo" que se usa en los datos XML.

<span id="XML_CREATED_BY"></span><span id="xml_created_by"></span>**XML \_ creado \_ por**  
IDENTIFICADOR de recurso para la cadena "creado por" que se usa en los datos XML.

<span id="XML_CLASS_LINKAGE"></span><span id="xml_class_linkage"></span>**\_vinculación de clases XML \_**  
IDENTIFICADOR de recurso para la cadena "vinculación de clases" que se usa en los datos XML.

<span id="XML_DESCRIPTION"></span><span id="xml_description"></span>**Descripción de XML \_**  
IDENTIFICADOR de recurso para la cadena "Description" que se usa en los datos XML.

<span id="XML_SEMANTIC_NAME"></span><span id="xml_semantic_name"></span>**\_nombre semántico \_ XML**  
IDENTIFICADOR de recurso para la cadena "nombre semántico" que se usa en los datos XML.

<span id="XML_SEMANTIC_INDEX"></span><span id="xml_semantic_index"></span>**\_Índice semántico \_ XML**  
IDENTIFICADOR de recurso para la cadena "índice semántico" usado en los datos XML.

<span id="XML_FORMAT"></span><span id="xml_format"></span>**\_formato XML**  
IDENTIFICADOR de recurso para la cadena "format" que se usa en los datos XML.

<span id="XML_INPUT_SLOT"></span><span id="xml_input_slot"></span>**\_ranura de entrada XML \_**  
IDENTIFICADOR de recurso para la cadena "ranura de entrada" que se usa en los datos XML.

<span id="XML_ALIGNED_BYTE_OFFSET"></span><span id="xml_aligned_byte_offset"></span>**\_desplazamiento de \_ bytes XML alineado \_**  
IDENTIFICADOR de recurso para la cadena "desplazamiento de bytes alineado" que se usa en los datos XML.

<span id="XML_INPUT_SLOT_CLASS"></span><span id="xml_input_slot_class"></span>**\_clase de \_ ranura de entrada XML \_**  
IDENTIFICADOR de recurso para la cadena "clase de ranura de entrada" utilizada en los datos XML.

<span id="XML_INSTANCE_DATA_STEP_RATE"></span><span id="xml_instance_data_step_rate"></span>**\_velocidad de \_ paso de datos de instancia XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "tasa de paso de datos de instancia" que se usa en los datos XML.

<span id="XML_ADDRESS_U"></span><span id="xml_address_u"></span>**\_Dirección XML \_ U**  
IDENTIFICADOR de recurso para la cadena "dirección u" utilizada en los datos XML.

<span id="XML_ADDRESS_V"></span><span id="xml_address_v"></span>**\_Dirección XML \_ V**  
IDENTIFICADOR de recurso para la cadena "Address v" usada en los datos XML.

<span id="XML_ADDRESS_W"></span><span id="xml_address_w"></span>**\_Dirección XML \_ W**  
IDENTIFICADOR de recurso para la cadena "Address w" usada en los datos XML.

<span id="XML_MIN_LOD"></span><span id="xml_min_lod"></span>**código \_ LOD mínimo XML \_**  
IDENTIFICADOR de recurso para la cadena "min LOD" que se usa en los datos XML.

<span id="XML_MAX_LOD"></span><span id="xml_max_lod"></span>**\_LOD Max \_ XML**  
IDENTIFICADOR de recurso para la cadena "Max LOD" que se usa en los datos XML.

<span id="XML_FILTER"></span><span id="xml_filter"></span>**\_filtro XML**  
IDENTIFICADOR de recurso para la cadena "Filter" que se usa en los datos XML.

<span id="XML_MIP_SLICE"></span><span id="xml_mip_slice"></span>**\_segmento de MIP XML \_**  
IDENTIFICADOR de recurso para la cadena "segmento MIP" usado en los datos XML.

<span id="XML_MOST_DETAILED_MIP"></span><span id="xml_most_detailed_mip"></span>**\_MIP más \_ detallado de XML \_**  
IDENTIFICADOR de recurso para la cadena "MIP más detallado" que se usa en los datos XML.

<span id="XML_MIP_LEVELS"></span><span id="xml_mip_levels"></span>**\_niveles de MIP XML \_**  
IDENTIFICADOR de recurso para la cadena "niveles de MIP" que se usa en los datos XML.

<span id="XML_FIRST_ARRAY_SLICE"></span><span id="xml_first_array_slice"></span>**\_segmento de primera \_ matriz \_ de XML**  
IDENTIFICADOR de recurso para la cadena "primer segmento de matriz" usado en los datos XML.

<span id="XML_ARRAY_SIZE"></span><span id="xml_array_size"></span>**tamaño de la \_ matriz XML \_**  
IDENTIFICADOR de recurso para la cadena "tamaño de matriz" usado en los datos XML.

<span id="XML_RESOURCE"></span><span id="xml_resource"></span>**\_recurso XML**  
IDENTIFICADOR de recurso para la cadena "Resource" que se usa en los datos XML.

<span id="XML_RESOURCE_TYPE"></span><span id="xml_resource_type"></span>**\_tipo de recurso XML \_**  
IDENTIFICADOR de recurso para la cadena "tipo de recurso" usado en los datos XML.

<span id="XML_ELEMENT_OFFSET"></span><span id="xml_element_offset"></span>**\_desplazamiento del elemento XML \_**  
IDENTIFICADOR de recurso para la cadena "desplazamiento del elemento" usado en los datos XML.

<span id="XML_ELEMENT_WIDTH"></span><span id="xml_element_width"></span>**\_ancho de elemento XML \_**  
IDENTIFICADOR de recurso para la cadena "ancho de elemento" usado en los datos XML.

<span id="XML_FIRST_WITH_SLICE"></span><span id="xml_first_with_slice"></span>**XML \_ primero \_ con \_ segmento**  
IDENTIFICADOR de recurso de la cadena "First with Slice" que se usa en los datos XML.

<span id="XML_BORDER_COLOR"></span><span id="xml_border_color"></span>**\_color de borde XML \_**  
IDENTIFICADOR de recurso para la cadena "color de borde" que se usa en los datos XML.

<span id="XML_SLOT"></span><span id="xml_slot"></span>**\_ranura XML**  
IDENTIFICADOR de recurso para la cadena "slot" que se usa en los datos XML.

<span id="XML_COMPARISON_FUNCTION"></span><span id="xml_comparison_function"></span>**\_función de comparación XML \_**  
IDENTIFICADOR de recurso para la cadena "función de comparación" utilizada en los datos XML.

<span id="XML_MAX_ANISOTROPY"></span><span id="xml_max_anisotropy"></span>**\_anisotropía Max \_ XML**  
IDENTIFICADOR de recurso para la cadena "Max anisotropía" que se usa en los datos XML.

<span id="XML_MIP_LOD_BIAS"></span><span id="xml_mip_lod_bias"></span>**\_ \_ sesgo LOD de XML MIP \_**  
IDENTIFICADOR de recurso para la cadena "" MIP LOD bias "que se usa en los datos XML.

<span id="XML_FILL_MODE"></span><span id="xml_fill_mode"></span>**\_modo de relleno XML \_**  
IDENTIFICADOR de recurso para la cadena "modo de relleno" usado en los datos XML.

<span id="XML_CULL_MODE"></span><span id="xml_cull_mode"></span>**\_modo de selección de XML \_**  
IDENTIFICADOR de recurso para la cadena "modo de selección" que se usa en los datos XML.

<span id="XML_FRONT_COUNTER_CLOCKWISE"></span><span id="xml_front_counter_clockwise"></span>**\_front-end XML a la \_ \_ derecha**  
IDENTIFICADOR de recurso para la cadena "front-end en el sentido de las agujas del reloj" usado en los datos XML.

<span id="XML_DEPTH_BIAS"></span><span id="xml_depth_bias"></span>**\_diferencia de profundidad XML \_**  
IDENTIFICADOR de recurso para la cadena "bias depth" usada en los datos XML.

<span id="XML_DEPTH_BIAS_CLAMP"></span><span id="xml_depth_bias_clamp"></span>**\_abrazadera de \_ sesgo de profundidad XML \_**  
IDENTIFICADOR de recurso para la cadena "abrazadera de polarización de profundidad" que se usa en los datos XML.

<span id="XML_SLOPE_SCALE_DEPTH_BIAS"></span><span id="xml_slope_scale_depth_bias"></span>**\_diferencia de \_ profundidad de escala de pendiente XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "sesgo de profundidad de escala de pendiente" usado en los datos XML.

<span id="XML_DEPTH_CLIP_ENABLE"></span><span id="xml_depth_clip_enable"></span>**\_ \_ Habilitar clip de profundidad XML \_**  
IDENTIFICADOR de recurso para la cadena "habilitar clip de profundidad" usado en los datos XML.

<span id="XML_SCISSOR_ENABLE"></span><span id="xml_scissor_enable"></span>**habilitación de redondeos XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "retenerse habilitado" que se usa en los datos XML.

<span id="XML_MULTISAMPLE_ENABLE"></span><span id="xml_multisample_enable"></span>**\_habilitación de MULTIMUESTRA XML \_**  
IDENTIFICADOR de recurso para la cadena "habilitación multiejemplo" que se usa en los datos XML.

<span id="XML_ANTIALIASED_LINE_ENABLE"></span><span id="xml_antialiased_line_enable"></span>**\_Habilitar la línea ANTIALIAS XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "alisado de línea habilitada" que se usa en los datos XML.

<span id="XML_FUNCTION"></span><span id="xml_function"></span>**\_función XML**  
IDENTIFICADOR de recurso para la cadena "Function" que se usa en los datos XML.

<span id="XML_PASS_OP"></span><span id="xml_pass_op"></span>**\_operación Pass \_ XML**  
IDENTIFICADOR de recurso para la cadena "Pass OP" que se usa en los datos XML.

<span id="XML_FAIL_OP"></span><span id="xml_fail_op"></span>**error de XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "Fail OP" que se usa en los datos XML.

<span id="XML_DEPTH_FAIL_OP"></span><span id="xml_depth_fail_op"></span>**\_error de profundidad XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "error de profundidad", que se usa en los datos XML.

<span id="XML_DEPTH_ENABLE"></span><span id="xml_depth_enable"></span>**\_Habilitar profundidad \_ XML**  
IDENTIFICADOR de recurso para la cadena "habilitar profundidad" usada en los datos XML.

<span id="XML_DEPTH_FUNCTION"></span><span id="xml_depth_function"></span>**\_función de profundidad XML \_**  
IDENTIFICADOR de recurso para la cadena "función depth" usada en los datos XML.

<span id="XML_DEPTH_WRITE_MASK"></span><span id="xml_depth_write_mask"></span>**\_máscara de \_ escritura de profundidad XML \_**  
IDENTIFICADOR de recurso para la cadena "máscara de escritura de profundidad" que se usa en los datos XML.

<span id="XML_STENCIL_ENABLE"></span><span id="xml_stencil_enable"></span>**\_Habilitar Galería de símbolos XML \_**  
IDENTIFICADOR de recurso para la cadena "habilitada para estarcido" que se usa en los datos XML.

<span id="XML_STENCIL_READ_MASK"></span><span id="xml_stencil_read_mask"></span>**máscara de lectura de galería de \_ símbolos XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "máscara de lectura de estarcido" que se usa en los datos XML.

<span id="XML_STENCIL_WRITE_MASK"></span><span id="xml_stencil_write_mask"></span>**\_máscara de \_ escritura de estarcido XML \_**  
IDENTIFICADOR de recurso para la cadena "máscara de escritura de estarcido" que se usa en los datos XML.

<span id="XML_FIRST_ELEMENT"></span><span id="xml_first_element"></span>**\_primer \_ elemento XML**  
IDENTIFICADOR de recurso para la cadena "primer elemento" que se usa en los datos XML.

<span id="XML_NUM_ELEMENTS"></span><span id="xml_num_elements"></span>**\_elementos numéricos XML \_**  
IDENTIFICADOR de recurso para la cadena "número de elementos" que se usa en los datos XML.

<span id="XML_FLAGS"></span><span id="xml_flags"></span>**\_marcas XML**  
IDENTIFICADOR de recurso para la cadena "flags" que se usa en los datos XML.

<span id="XML_W_SIZE"></span><span id="xml_w_size"></span>**tamaño de XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "w size" usada en los datos XML.

<span id="XML_FIRST_2D_ARRAY_FACE"></span><span id="xml_first_2d_array_face"></span>**Apariencia de la \_ primera \_ \_ matriz 2D \_ de XML**  
IDENTIFICADOR de recurso para la cadena "primera matriz de 2D en la que se usa en los datos XML.

<span id="XML_NUM_CUBES"></span><span id="xml_num_cubes"></span>**\_cubos numéricos XML \_**  
IDENTIFICADOR de recurso para la cadena "número de cubos" usado en los datos XML.

<span id="XML_FACTORY"></span><span id="xml_factory"></span>**\_generador XML**  
IDENTIFICADOR de recurso para la cadena "Factory" que se usa en los datos XML.

<span id="XML_ADAPTER"></span><span id="xml_adapter"></span>**\_adaptador XML**  
IDENTIFICADOR de recurso para la cadena "adaptador" que se usa en los datos XML.

<span id="XML_HEIGHT"></span><span id="xml_height"></span>**alto de XML \_**  
IDENTIFICADOR de recurso para la cadena "height" utilizada en los datos XML.

<span id="XML_WIDTH"></span><span id="xml_width"></span>**\_ancho XML**  
IDENTIFICADOR de recurso para la cadena "width" utilizada en los datos XML.

<span id="XML_CREATION_FLAGS"></span><span id="xml_creation_flags"></span>**\_marcas de creación de XML \_**  
IDENTIFICADOR de recurso para la cadena "marcas de creación" que se usa en los datos XML.

<span id="XML_SDK_VERSION"></span><span id="xml_sdk_version"></span>**\_versión del SDK de XML \_**  
IDENTIFICADOR de recurso para la cadena "versión del SDK" utilizada en los datos XML.

<span id="XML_DRIVER_TYPE"></span><span id="xml_driver_type"></span>**\_tipo de controlador XML \_**  
IDENTIFICADOR de recurso para la cadena "tipo de controlador" usado en los datos XML.

<span id="XML_FEATURE_LEVEL"></span><span id="xml_feature_level"></span>**\_nivel de característica XML \_**  
IDENTIFICADOR de recurso para la cadena "nivel de característica" usado en los datos XML.

<span id="XML_IMMEDIATE_CONTEXT"></span><span id="xml_immediate_context"></span>**\_contexto inmediato \_ XML**  
IDENTIFICADOR de recurso para la cadena "contexto inmediato" usado en los datos XML.

<span id="XML_EXCEPTION_MODE"></span><span id="xml_exception_mode"></span>**\_modo de excepción XML \_**  
IDENTIFICADOR de recurso para la cadena "modo de excepción" usado en los datos XML.

<span id="XML_REFRESH_RATE"></span><span id="xml_refresh_rate"></span>**\_frecuencia de actualización de XML \_**  
IDENTIFICADOR de recurso para la cadena "frecuencia de actualización" usada en los datos XML.

<span id="XML_SCANLINE_ORDERING"></span><span id="xml_scanline_ordering"></span>**\_ordenación de SCANLINE XML \_**  
IDENTIFICADOR de recurso para la cadena "Scanline Ordering" que se usa en los datos XML.

<span id="XML_SCALING"></span><span id="xml_scaling"></span>**\_escalado XML**  
IDENTIFICADOR de recurso para la cadena "escalado" que se usa en los datos XML.

<span id="XML_USAGE"></span><span id="xml_usage"></span>**uso de XML \_**  
IDENTIFICADOR de recurso para la cadena "Usage" utilizada en los datos XML.

<span id="XML_BUFFER_COUNT"></span><span id="xml_buffer_count"></span>**recuento de \_ búferes XML \_**  
IDENTIFICADOR de recurso para la cadena "recuento de búferes" usado en los datos XML.

<span id="XML_WINDOWED"></span><span id="xml_windowed"></span>**XML con \_ ventanas**  
IDENTIFICADOR de recurso para la cadena "en ventana" que se usa en los datos XML.

<span id="XML_SWAP_EFFECT"></span><span id="xml_swap_effect"></span>**\_efecto de intercambio de XML \_**  
IDENTIFICADOR de recurso para la cadena "efecto de intercambio" usado en los datos XML.

<span id="XML_STREAM"></span><span id="xml_stream"></span>**\_secuencia XML**  
IDENTIFICADOR de recurso para la cadena "Stream" que se usa en los datos XML.

<span id="XML_OFFSET"></span><span id="xml_offset"></span>**\_desplazamiento XML**  
IDENTIFICADOR de recurso para la cadena "offset" utilizada en los datos XML.

<span id="XML_TYPE"></span><span id="xml_type"></span>**\_tipo XML**  
IDENTIFICADOR de recurso para la cadena "type" que se usa en los datos XML.

<span id="XML_METHOD"></span><span id="xml_method"></span>**\_método XML**  
IDENTIFICADOR de recurso para la cadena "Method" que se usa en los datos XML.

<span id="XML_USAGE_INDEX"></span><span id="xml_usage_index"></span>**\_Índice de uso de XML \_**  
IDENTIFICADOR de recurso para la cadena "índice de uso" utilizado en los datos XML.

<span id="XML_BLEND_SOURCE"></span><span id="xml_blend_source"></span>**\_origen de Blend XML \_**  
IDENTIFICADOR de recurso para la cadena "origen de Blend" usado en los datos XML.

<span id="XML_BLEND_DESTINATION"></span><span id="xml_blend_destination"></span>**destino de XML \_ Blend \_**  
IDENTIFICADOR de recurso para la cadena "destino de Blend" que se usa en los datos XML.

<span id="XML_BLEND_OP"></span><span id="xml_blend_op"></span>**OP de XML \_ Blend \_**  
IDENTIFICADOR de recurso de la cadena "Blend OP" que se usa en los datos XML.

<span id="XML_ALPHA_BLEND_SOURCE"></span><span id="xml_alpha_blend_source"></span>**\_origen de \_ Blend XML alpha \_**  
IDENTIFICADOR de recurso para la cadena "origen de Blend alfa" usado en los datos XML.

<span id="XML_ALPHA_BLEND_DEST"></span><span id="xml_alpha_blend_dest"></span>**DEST de XML \_ alpha \_ Blend \_**  
IDENTIFICADOR de recurso para la cadena "Alpha Blend dest" que se usa en los datos XML.

<span id="XML_ALPHA_BLEND_OP"></span><span id="xml_alpha_blend_op"></span>**OP de XML \_ alpha \_ Blend \_**  
IDENTIFICADOR de recurso para la cadena "Alpha Blend OP" que se usa en los datos XML.

<span id="XML_RENDER_TARGET_WRITE_MASK"></span><span id="xml_render_target_write_mask"></span>**\_máscara de \_ escritura de destino de representación XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "representated Target Write Mask" que se usa en los datos XML.

<span id="XML_BLEND_ENABLE"></span><span id="xml_blend_enable"></span>**\_Habilitar XML Blend \_**  
IDENTIFICADOR de recurso para la cadena "habilitar Blend" que se usa en los datos XML.

<span id="XML_ALPHA_TO_COVERAGE_ENABLE"></span><span id="xml_alpha_to_coverage_enable"></span>**\_ \_ habilitación de XML Alpha a \_ COVERAGE \_**  
IDENTIFICADOR de recurso de la cadena "Alpha to Coverage Enable" que se usa en los datos XML.

<span id="XML_BLEND_STATE_1"></span><span id="xml_blend_state_1"></span>**\_ \_ Estado 1 de Blend XML \_**  
IDENTIFICADOR de recurso para la cadena "estado de Blend 1" que se usa en los datos XML.

<span id="XML_INDEPENDENT_BLEND_ENABLE"></span><span id="xml_independent_blend_enable"></span>**\_ \_ habilitación de Blend independiente XML \_**  
IDENTIFICADOR de recurso para la cadena "habilitada para Blend independiente" que se usa en los datos XML.

<span id="XML_RENDER_TARGET_NUMBER"></span><span id="xml_render_target_number"></span>**\_número de \_ destino de representación XML \_**  
IDENTIFICADOR de recurso para la cadena "número de destino de representación" usado en los datos XML.

<span id="XML_BUFFER"></span><span id="xml_buffer"></span>**\_búfer XML**  
IDENTIFICADOR de recurso para la cadena "buffer" usada en los datos XML.

<span id="XML_PRIMITIVE_TOPOLOGY"></span><span id="xml_primitive_topology"></span>**\_topología primitiva \_ XML**  
IDENTIFICADOR de recurso para la cadena "topología primitiva" utilizada en los datos XML.

<span id="XML_STRIDE"></span><span id="xml_stride"></span>**\_STRIDE XML**  
IDENTIFICADOR de recurso para la cadena "STRIDE" que se usa en los datos XML.

<span id="XML_TOP"></span><span id="xml_top"></span>**XML \_ superior**  
IDENTIFICADOR de recurso para la cadena "Top" que se usa en los datos XML.

<span id="XML_RIGHT"></span><span id="xml_right"></span>**\_derecho XML**  
IDENTIFICADOR de recurso para la cadena "Right" utilizada en los datos XML.

<span id="XML_BOTTOM"></span><span id="xml_bottom"></span>**\_parte inferior de XML**  
IDENTIFICADOR de recurso para la cadena "Bottom" que se usa en los datos XML.

<span id="XML_LEFT"></span><span id="xml_left"></span>**XML a la \_ izquierda**  
IDENTIFICADOR de recurso para la cadena "left" utilizada en los datos XML.

<span id="XML_TOP_LEFT_X"></span><span id="xml_top_left_x"></span>**XML \_ superior \_ izquierdo \_ X**  
IDENTIFICADOR de recurso para la cadena "superior izquierda x" que se usa en los datos XML.

<span id="XML_TOP_LEFT_Y"></span><span id="xml_top_left_y"></span>**XML \_ superior \_ izquierdo \_ Y**  
IDENTIFICADOR de recurso para la cadena "superior izquierda y" que se usa en los datos XML.

<span id="XML_MIN_DEPTH"></span><span id="xml_min_depth"></span>**\_profundidad mínima de XML \_**  
IDENTIFICADOR de recurso para la cadena "min depth" que se usa en los datos XML.

<span id="XML_MAX_DEPTH"></span><span id="xml_max_depth"></span>**profundidad de XML \_ máx. \_**  
IDENTIFICADOR de recurso para la cadena "Max Depth" usada en los datos XML.

<span id="XML_STENCIL_REFERENCE_VALUE"></span><span id="xml_stencil_reference_value"></span>**valor de referencia de galería de \_ símbolos XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "valor de referencia de estarcido" usado en los datos XML.

<span id="XML_DEPTH_BUFFER"></span><span id="xml_depth_buffer"></span>**\_búfer de profundidad XML \_**  
IDENTIFICADOR de recurso para la cadena "búfer de profundidad" usado en los datos XML.

<span id="XML_BLEND_FACTOR"></span><span id="xml_blend_factor"></span>**\_factor de mezcla XML \_**  
IDENTIFICADOR de recurso para la cadena "factor de mezcla" usado en los datos XML.

<span id="XML_SAMPLE_MASK"></span><span id="xml_sample_mask"></span>**\_máscara de ejemplo XML \_**  
IDENTIFICADOR de recurso para la cadena "máscara de ejemplo" que se usa en los datos XML.

<span id="XML_DEPTH_STENCIL_STATE"></span><span id="xml_depth_stencil_state"></span>**Estado de la \_ Galería de símbolos de profundidad XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "estado de estarcido de profundidad" usado en los datos XML.

<span id="XML_SCISSOR_RECT"></span><span id="xml_scissor_rect"></span>**\_rectángulo de tijera XML \_**  
IDENTIFICADOR de recurso para la cadena "rectángulo de tijeras" que se usa en los datos XML.

<span id="XML_SAMPLER"></span><span id="xml_sampler"></span>**\_muestra XML**  
IDENTIFICADOR de recurso para la cadena "muestra" utilizada en los datos XML.

<span id="XML_SHADER"></span><span id="xml_shader"></span>**\_sombreador XML**  
IDENTIFICADOR de recurso para la cadena "Shader" que se usa en los datos XML.

<span id="XML_DEPTH_STENCIL_VIEW"></span><span id="xml_depth_stencil_view"></span>**\_vista de \_ Galería de símbolos de profundidad XML \_**  
IDENTIFICADOR de recurso para la cadena "vista de galería de símbolos de profundidad" que se usa en los datos XML.

<span id="XML_SHADER_RESOURCE_VIEW"></span><span id="xml_shader_resource_view"></span>**\_vista de recursos del sombreador XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "vista de recursos del sombreador" que se usa en los datos XML.

<span id="XML_INPUT_LAYOUT"></span><span id="xml_input_layout"></span>**\_diseño de entrada XML \_**  
IDENTIFICADOR de recurso para la cadena "diseño de entrada" usado en los datos XML.

<span id="XML_CLASS_INSTANCE"></span><span id="xml_class_instance"></span>**\_instancia de clase XML \_**  
IDENTIFICADOR de recurso para la cadena "instancia de clase" utilizada en los datos XML.

<span id="XML_STREAM_OUTPUT_BUFFER"></span><span id="xml_stream_output_buffer"></span>**\_búfer de \_ salida de flujo XML \_**  
IDENTIFICADOR de recurso para la cadena "búfer de salida de flujo" usado en los datos XML.

<span id="XML_SHADER_RESOURCE"></span><span id="xml_shader_resource"></span>**\_recurso de sombreador XML \_**  
IDENTIFICADOR de recurso para la cadena "recurso de sombreador" usado en los datos XML.

<span id="XML_RENDER_TARGET_INSTANCE"></span><span id="xml_render_target_instance"></span>**\_instancia de \_ destino de representación XML \_**  
IDENTIFICADOR de recurso para la cadena "representar instancia de destino" que se usa en los datos XML.

<span id="XML_VERTEX"></span><span id="xml_vertex"></span>**\_vértice XML**  
IDENTIFICADOR de recurso para la cadena "Vertex" que se usa en los datos XML.

<span id="XML_VERTEX_BUFFER"></span><span id="xml_vertex_buffer"></span>**\_búfer de vértices XML \_**  
IDENTIFICADOR de recurso para la cadena "búfer de vértice" usado en los datos XML.

<span id="XML_INSTANCE"></span><span id="xml_instance"></span>**\_instancia XML**  
IDENTIFICADOR de recurso para la cadena "instancia" que se usa en los datos XML.

<span id="XML_VIEWPORT"></span><span id="xml_viewport"></span>**\_ventanilla XML**  
IDENTIFICADOR de recurso para la cadena "viewport" que se usa en los datos XML.

<span id="XML_FIRSTCONSTANT"></span><span id="xml_firstconstant"></span>**\_FIRSTCONSTANT XML**  
IDENTIFICADOR de recurso de la cadena "firstconstant" que se usa en los datos XML.

<span id="XML_NUMCONSTANTS"></span><span id="xml_numconstants"></span>**\_NUMCONSTANTS XML**  
IDENTIFICADOR de recurso de la cadena "numconstants" que se usa en los datos XML.

<span id="XML_LOGIC_OP"></span><span id="xml_logic_op"></span>**\_operación lógica \_ XML**  
IDENTIFICADOR de recurso de la cadena "Logic OP" utilizada en los datos XML.

<span id="XML_LOGICOPS_ENABLE"></span><span id="xml_logicops_enable"></span>**\_habilitación de LOGICOPS XML \_**  
IDENTIFICADOR de recurso de la cadena "logicops Enable" que se usa en los datos XML.

<span id="XML_SHADER_FILE"></span><span id="xml_shader_file"></span>**\_archivo de sombreador XML \_**  
IDENTIFICADOR de recurso para la cadena "archivo de sombreador" usado en los datos XML.

<span id="XML_FILE_NAME"></span><span id="xml_file_name"></span>**\_nombre de archivo XML \_**  
IDENTIFICADOR de recurso para la cadena "nombre de archivo" utilizado en los datos XML.

<span id="XML_10TEXTURE1D_DESC"></span><span id="xml_10texture1d_desc"></span>**\_10TEXTURE1D XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "10texture1d DESC" que se usa en los datos XML.

<span id="XML_10TEXTURE2D_DESC"></span><span id="xml_10texture2d_desc"></span>**\_10TEXTURE2D XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "10texture2d DESC" que se usa en los datos XML.

<span id="XML_10TEXTURE3D_DESC"></span><span id="xml_10texture3d_desc"></span>**\_10TEXTURE3D XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "10texture3d DESC" que se usa en los datos XML.

<span id="XML_TEXTURE_ARRAYSIZE"></span><span id="xml_texture_arraysize"></span>**\_tamaño de textura XML \_**  
IDENTIFICADOR de recurso para la cadena "Texture tamaño" que se usa en los datos XML.

<span id="XML_TEXTURE_MIPLEVEL"></span><span id="xml_texture_miplevel"></span>**\_MIPLEVEL de textura XML \_**  
IDENTIFICADOR de recurso para la cadena "Texture miplevel" que se usa en los datos XML.

<span id="XML_CPUFLAGS"></span><span id="xml_cpuflags"></span>**\_CPUFLAGS XML**  
IDENTIFICADOR de recurso de la cadena "cpuflags" que se usa en los datos XML.

<span id="XML_MISCFLAGS"></span><span id="xml_miscflags"></span>**\_MISCFLAGS XML**  
IDENTIFICADOR de recurso de la cadena "miscflags" que se usa en los datos XML.

<span id="XML_BINDFLAGS"></span><span id="xml_bindflags"></span>**\_BINDFLAGS XML**  
IDENTIFICADOR de recurso de la cadena "bindflags" que se usa en los datos XML.

<span id="XML_SAMPLEDESC_QUALITY"></span><span id="xml_sampledesc_quality"></span>**\_calidad de SAMPLEDESC XML \_**  
IDENTIFICADOR de recurso de la cadena "sampledesc Quality" que se usa en los datos XML.

<span id="XML_SAMPLEDESC_COUNT"></span><span id="xml_sampledesc_count"></span>**recuento de \_ SAMPLEDESCs XML \_**  
IDENTIFICADOR de recurso de la cadena "sampledesc Count" que se usa en los datos XML.

<span id="XML_DEPTH"></span><span id="xml_depth"></span>**\_profundidad XML**  
IDENTIFICADOR de recurso para la cadena "depth" utilizada en los datos XML.

<span id="XML_11TEXTURE1D_DESC"></span><span id="xml_11texture1d_desc"></span>**\_11TEXTURE1D XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "11texture1d DESC" que se usa en los datos XML.

<span id="XML_11TEXTURE2D_DESC"></span><span id="xml_11texture2d_desc"></span>**\_11TEXTURE2D XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "11texture2d DESC" que se usa en los datos XML.

<span id="XML_11TEXTURE3D_DESC"></span><span id="xml_11texture3d_desc"></span>**\_11TEXTURE3D XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "11texture3d DESC" que se usa en los datos XML.

<span id="XML_BYTEWIDTH"></span><span id="xml_bytewidth"></span>**\_BYTEWIDTH XML**  
IDENTIFICADOR de recurso de la cadena "bytewidth" que se usa en los datos XML.

<span id="XML_10BUFFER_DESC"></span><span id="xml_10buffer_desc"></span>**\_10BUFFER XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "10buffer DESC" que se usa en los datos XML.

<span id="XML_11BUFFER_DESC"></span><span id="xml_11buffer_desc"></span>**\_11BUFFER XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "11Buffer DESC" que se usa en los datos XML.

<span id="XML_BYTESTRIDE"></span><span id="xml_bytestride"></span>**\_BYTESTRIDE XML**  
IDENTIFICADOR de recurso de la cadena "bytestride" que se usa en los datos XML.

<span id="XML_STEREO"></span><span id="xml_stereo"></span>**\_estéreo XML**  
IDENTIFICADOR de recurso para la cadena "estéreo" que se usa en los datos XML.

<span id="XML_BACKGROUNDCOLOR"></span><span id="xml_backgroundcolor"></span>**BACKGROUNDCOLOR de XML \_**  
IDENTIFICADOR de recurso de la cadena "backgroundColor" que se usa en los datos XML.

<span id="XML_ALPHA_MODE"></span><span id="xml_alpha_mode"></span>**\_modo alfa \_ XML**  
IDENTIFICADOR de recurso para la cadena "modo alfa" usado en los datos XML.

<span id="XML_RESTRICTTOOUTPUT"></span><span id="xml_restricttooutput"></span>**\_RESTRICTTOOUTPUT XML**  
IDENTIFICADOR de recurso de la cadena "restricttooutput" que se usa en los datos XML.

<span id="XML_MODEROTATION"></span><span id="xml_moderotation"></span>**\_MODEROTATION XML**  
IDENTIFICADOR de recurso de la cadena "moderotation" que se usa en los datos XML.

<span id="XML_ISTEMPORARYMONOSUPPORTED"></span><span id="xml_istemporarymonosupported"></span>**\_ISTEMPORARYMONOSUPPORTED XML**  
IDENTIFICADOR de recurso de la cadena "istemporarymonosupported" que se usa en los datos XML.

<span id="XML_FORCED_SAMPLE_COUNT"></span><span id="xml_forced_sample_count"></span>**recuento de \_ muestras forzada de XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "recuento de muestras forzada" usado en los datos XML.

<span id="XML_QUERY"></span><span id="xml_query"></span>**\_consulta XML**  
IDENTIFICADOR de recurso para la cadena "Query" utilizada en los datos XML.

<span id="XML_10QUERY_DESC"></span><span id="xml_10query_desc"></span>**\_10QUERY XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "10query DESC" que se usa en los datos XML.

<span id="XML_11QUERY_DESC"></span><span id="xml_11query_desc"></span>**\_11QUERY XML \_ DESC**  
IDENTIFICADOR de recurso de la cadena "11query DESC" que se usa en los datos XML.

<span id="XML_FLAGS_NONE"></span><span id="xml_flags_none"></span>**\_marcas XML \_ ninguna**  
IDENTIFICADOR de recurso para la cadena "flags none" que se usa en los datos XML.

<span id="XML_BUFFERUSAGE"></span><span id="xml_bufferusage"></span>**\_BUFFERUSAGE XML**  
IDENTIFICADOR de recurso de la cadena "bufferusage" que se usa en los datos XML.

<span id="XML_UNKNOWN"></span><span id="xml_unknown"></span>**XML \_ desconocido**  
IDENTIFICADOR de recurso para la cadena "Unknown" que se usa en los datos XML.

<span id="XML_MAXFRAMELATENCY"></span><span id="xml_maxframelatency"></span>**\_MAXFRAMELATENCY XML**  
IDENTIFICADOR de recurso de la cadena "maxframelatency" que se usa en los datos XML.

<span id="XML_SOURCESIZEWIDTH"></span><span id="xml_sourcesizewidth"></span>**\_SOURCESIZEWIDTH XML**  
IDENTIFICADOR de recurso de la cadena "sourcesizewidth" que se usa en los datos XML.

<span id="XML_SOURCESIZEHEIGHT"></span><span id="xml_sourcesizeheight"></span>**\_SOURCESIZEHEIGHT XML**  
IDENTIFICADOR de recurso de la cadena "sourcesizeheight" que se usa en los datos XML.

<span id="XML_MATRIXTRANSFORM"></span><span id="xml_matrixtransform"></span>**MATRIXTRANSFORM de XML \_**  
IDENTIFICADOR de recurso para la cadena "MatrixTransform" que se usa en los datos XML.

<span id="XML_TILED_RESOURCE_INFO"></span><span id="xml_tiled_resource_info"></span>**información de recursos en \_ mosaico XML \_ \_**  
IDENTIFICADOR de recurso de la cadena "información de recursos en mosaico" utilizada en los datos XML.

<span id="XML_TILES_RESOURCE_SUBRESOURCE_TILINGS"></span><span id="xml_tiles_resource_subresource_tilings"></span>**\_iconos XML mosaicos de \_ \_ Subrecursos de recursos \_**  
IDENTIFICADOR de recurso para la cadena "mosaicos subresource tileings" usado en datos XML.

<span id="XML_DEVICE_CHILD_INFORMATION"></span><span id="xml_device_child_information"></span>**\_ \_ información secundaria del dispositivo XML \_**  
IDENTIFICADOR de recurso para la cadena "información de elemento secundario del dispositivo" que se usa en los datos XML.

<span id="XML_DEVICE"></span><span id="xml_device"></span>**\_dispositivo XML**  
IDENTIFICADOR de recurso para la cadena "Device" que se usa en los datos XML.

<span id="XML_OTHER"></span><span id="xml_other"></span>**\_otro XML**  
IDENTIFICADOR de recurso de la cadena "Other" que se usa en los datos XML.

<span id="XML_PREDICATE"></span><span id="xml_predicate"></span>**\_predicado XML**  
IDENTIFICADOR de recurso de la cadena "Predicate" utilizada en los datos XML.

<span id="XML_PREDICATE_VALUE"></span><span id="xml_predicate_value"></span>**\_valor de predicado XML \_**  
IDENTIFICADOR de recurso para la cadena "valor de predicado" usado en los datos XML.

<span id="XML_DEVICE_CONTEXT_TYPE"></span><span id="xml_device_context_type"></span>**\_tipo de \_ contexto de dispositivo XML \_**  
IDENTIFICADOR de recurso para la cadena "tipo de contexto de dispositivo" usado en los datos XML.

<span id="XML_BUFFER_HANDLE"></span><span id="xml_buffer_handle"></span>**\_identificador de búfer XML \_**  
IDENTIFICADOR de recurso para la cadena "controlador de búfer" usado en los datos XML.

<span id="XML_DEVICE_12"></span><span id="xml_device_12"></span>**\_Dispositivo XML \_ 12**  
IDENTIFICADOR de recurso para la cadena "Device 12" que se usa en los datos XML.

<span id="XML_MAX_RECORDING_COMMAND_LISTS"></span><span id="xml_max_recording_command_lists"></span>**\_listas de \_ comandos de grabación \_ \_ de XML máx.**  
IDENTIFICADOR de recurso para la cadena "Max Recording Command lists" usada en los datos XML.

<span id="XML_DEFAULT_COMMAND_QUEUE"></span><span id="xml_default_command_queue"></span>**\_cola de \_ comandos predeterminados XML \_**  
IDENTIFICADOR de recurso para la cadena "cola de comandos predeterminada" que se usa en los datos XML.

<span id="XML_D3D11_DEVICE_INFORMATION"></span><span id="xml_d3d11_device_information"></span>**\_Información del \_ dispositivo \_ D3D11 XML**  
IDENTIFICADOR de recurso de la cadena "d3d11 Device Information" que se usa en los datos XML.

<span id="XML_D3D12_DEVICE_INFORMATION"></span><span id="xml_d3d12_device_information"></span>**\_Información del \_ dispositivo \_ D3D12 XML**  
IDENTIFICADOR de recurso de la cadena "d3d12 Device Information" que se usa en los datos XML.

<span id="XML_CONSERVATIVE_RASTER"></span><span id="xml_conservative_raster"></span>**\_Raster XML conservador \_**  
IDENTIFICADOR de recurso para la cadena "reconservador Raster" que se usa en los datos XML.

<span id="XML_INPUT_LAYOUT_12"></span><span id="xml_input_layout_12"></span>**\_Diseño de entrada XML \_ \_ 12**  
IDENTIFICADOR de recurso para la cadena "diseño de entrada 12" usado en los datos XML.

<span id="XML_GRAPHICS_PIPELINE_STATE_12"></span><span id="xml_graphics_pipeline_state_12"></span>**\_Estado de \_ canalización de gráficos XML \_ \_ 12**  
IDENTIFICADOR de recurso para la cadena "estado de canalización de gráficos 12" que se usa en los datos XML.

<span id="XML_BLEND_STATE_12"></span><span id="xml_blend_state_12"></span>**\_ \_ Estado 12 de Blend XML \_**  
IDENTIFICADOR de recurso para la cadena "estado de Blend 12" que se usa en los datos XML.

<span id="XML_ROOT_SIGNATURE_12"></span><span id="xml_root_signature_12"></span>**\_Firma raíz de XML \_ \_ 12**  
IDENTIFICADOR de recurso para la cadena "firma raíz 12" que se usa en los datos XML.

<span id="XML_STREAM_OUTPUT_12"></span><span id="xml_stream_output_12"></span>**\_Salida de flujo XML \_ \_ 12**  
IDENTIFICADOR de recurso para la cadena "salida de flujo 12" que se usa en los datos XML.

<span id="XML_RASTERIZED_STREAM"></span><span id="xml_rasterized_stream"></span>**\_secuencia rasterizada \_ XML**  
IDENTIFICADOR de recurso para la cadena "Streamed Stream" que se usa en los datos XML.

<span id="XML_SO_DECLARATION_ENTRY_12"></span><span id="xml_so_declaration_entry_12"></span>**Entrada de declaración de XML \_ \_ \_ \_ 12**  
IDENTIFICADOR de recurso para la cadena "entrada de declaración 12" que se usa en los datos XML.

<span id="XML_START_COMPONENT"></span><span id="xml_start_component"></span>**\_componente de inicio XML \_**  
IDENTIFICADOR de recurso para la cadena "Start Component" usado en los datos XML.

<span id="XML_COMPONENT_COUNT"></span><span id="xml_component_count"></span>**recuento de \_ componentes XML \_**  
IDENTIFICADOR de recurso para la cadena "recuento de componentes" utilizado en los datos XML.

<span id="XML_OUTPUT_SLOT"></span><span id="xml_output_slot"></span>**\_ranura de salida XML \_**  
IDENTIFICADOR de recurso para la cadena "ranura de salida" utilizada en los datos XML.

<span id="XML_COMPUTE_PIPELINE_STATE_12"></span><span id="xml_compute_pipeline_state_12"></span>**\_Estado de canalización de proceso de XML \_ \_ \_ 12**  
IDENTIFICADOR de recurso para la cadena "Compute Pipeline State 12" que se usa en los datos XML.

<span id="XML_D3D12_SHADER_BYTECODE"></span><span id="xml_d3d12_shader_bytecode"></span>**\_Código de \_ bytes del sombreador D3D12 XML \_**  
IDENTIFICADOR de recurso para la cadena "d3d12 (código de bytes del sombreador)" usado en los datos XML.

<span id="XML_D3D12_SHADER_BYTECODE_PTR"></span><span id="xml_d3d12_shader_bytecode_ptr"></span>**\_D3D12 de \_ código de byte del sombreador de XML \_ \_**  
IDENTIFICADOR de recurso de la cadena "d3d12 de código de byte del sombreador de intersección" usado en los datos XML.

<span id="XML_D3D12_SHADER_BYTECODE_LENGTH"></span><span id="xml_d3d12_shader_bytecode_length"></span>**\_Longitud del \_ código de \_ bytes del SOMBREAdor D3D12 XML \_**  
IDENTIFICADOR de recurso para la cadena "longitud del código de bytes del sombreador d3d12" usada en los datos XML.

<span id="XML_D3D12_VIEW_DIMENSION"></span><span id="xml_d3d12_view_dimension"></span>**\_Dimensión de \_ vista de D3D12 XML \_**  
IDENTIFICADOR de recurso de la cadena "d3d12 View Dimension" que se usa en los datos XML.

<span id="XML_D3D12_NOTHING_TO_DEFINE"></span><span id="xml_d3d12_nothing_to_define"></span>**No \_ \_ se ha \_ \_ definido ningún D3D12 XML**  
IDENTIFICADOR de recurso de la cadena "d3d12 Nothing to define" que se usa en los datos XML.

<span id="XML_OFFSET_IN_BYTES"></span><span id="xml_offset_in_bytes"></span>**\_desplazamiento XML \_ en \_ bytes**  
IDENTIFICADOR de recurso para la cadena "desplazamiento en bytes" que se usa en los datos XML.

<span id="XML_SIZE_IN_BYTES"></span><span id="xml_size_in_bytes"></span>**\_tamaño XML \_ en \_ bytes**  
IDENTIFICADOR de recurso para la cadena "tamaño en bytes" que se usa en los datos XML.

<span id="XML_SO_CUR_OFFSET"></span><span id="xml_so_cur_offset"></span>**desplazamiento de for CUR de XML \_ \_ \_**  
IDENTIFICADOR de recurso para la cadena "desplazamiento del CUR" usado en los datos XML.

<span id="XML_ROOT_SIGNATURE"></span><span id="xml_root_signature"></span>**\_firma raíz \_ XML**  
IDENTIFICADOR de recurso para la cadena "firma raíz" utilizada en los datos XML.

<span id="XML_NUM_PARAMETERS"></span><span id="xml_num_parameters"></span>**\_parámetros numéricos XML \_**  
IDENTIFICADOR de recurso para la cadena "NUM Parameters" que se usa en los datos XML.

<span id="XML_ROOT_PARAMETERS"></span><span id="xml_root_parameters"></span>**\_parámetros raíz \_ XML**  
IDENTIFICADOR de recurso para la cadena "parámetros raíz" que se usa en los datos XML.

<span id="XML_ROOT_PARAMETER"></span><span id="xml_root_parameter"></span>**\_parámetro raíz \_ XML**  
IDENTIFICADOR de recurso para la cadena "parámetro raíz" usado en los datos XML.

<span id="XML_ROOT_PARAMETER_TYPE"></span><span id="xml_root_parameter_type"></span>**\_tipo de \_ parámetro \_ raíz XML**  
IDENTIFICADOR de recurso para la cadena "tipo de parámetro raíz" usado en los datos XML.

<span id="XML_ROOT_PARAMETER_SHADER_VISIBILITY"></span><span id="xml_root_parameter_shader_visibility"></span>**\_visibilidad del \_ sombreador de parámetros raíz \_ \_ XML**  
IDENTIFICADOR de recurso para la cadena "visibilidad del sombreador de parámetros raíz" que se usa en los datos XML.

<span id="XML_DESCRIPTOR_HANDLE"></span><span id="xml_descriptor_handle"></span>**\_identificador de descriptor XML \_**  
IDENTIFICADOR de recurso para la cadena "identificador de descriptor" usado en los datos XML.

<span id="XML_DESCRIPTOR_HANDLE_PTR"></span><span id="xml_descriptor_handle_ptr"></span>**\_identificador de descriptor XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "identificador de descriptor PTR" usado en los datos XML.

<span id="XML_ROOT_CONSTANTS"></span><span id="xml_root_constants"></span>**\_constantes raíz de XML \_**  
IDENTIFICADOR de recurso para la cadena "constantes raíz" utilizada en los datos XML.

<span id="XML_ROOT_DESCRIPTOR"></span><span id="xml_root_descriptor"></span>**\_descriptor raíz XML \_**  
IDENTIFICADOR de recurso para la cadena "descriptor raíz" usado en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_SHADER_REGISTER"></span><span id="xml_root_descriptor_shader_register"></span>**\_ \_ \_ registro del sombreador de descriptor raíz XML \_**  
IDENTIFICADOR de recurso para la cadena "registro del sombreador de descriptores raíz" usado en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_REGISTER_SPACE"></span><span id="xml_root_descriptor_register_space"></span>**\_espacio de \_ \_ registro del descriptor raíz XML \_**  
IDENTIFICADOR de recurso para la cadena "espacio de registro del descriptor raíz" usado en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_TABLE"></span><span id="xml_root_descriptor_table"></span>**\_tabla del \_ descriptor raíz XML \_**  
IDENTIFICADOR de recurso para la cadena "tabla de descriptores raíz" utilizada en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_NUM_RANGES"></span><span id="xml_root_descriptor_num_ranges"></span>**\_ \_ \_ rangos numéricos del descriptor raíz XML \_**  
IDENTIFICADOR de recurso para la cadena "rangos de número de descriptores raíz" usado en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_RANGES"></span><span id="xml_root_descriptor_ranges"></span>**\_intervalos de \_ descriptor raíz XML \_**  
IDENTIFICADOR de recurso para la cadena "intervalos de descriptores raíz" utilizados en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE"></span><span id="xml_root_descriptor_range"></span>**\_intervalo del \_ descriptor raíz XML \_**  
IDENTIFICADOR de recurso de la cadena "intervalo de descriptor raíz" usado en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_TYPE"></span><span id="xml_root_descriptor_range_type"></span>**\_tipo de \_ intervalo de descriptor raíz XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "tipo de intervalo de descriptor raíz" usado en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_COUNT"></span><span id="xml_root_descriptor_range_count"></span>**recuento de \_ \_ intervalos de descriptor raíz \_ XML \_**  
IDENTIFICADOR de recurso para la cadena "recuento de intervalos de descriptores raíz" usado en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_BASE_SHADER_REGISTER"></span><span id="xml_root_descriptor_range_base_shader_register"></span>**\_ \_ \_ \_ \_ registro del sombreador base del intervalo \_ del descriptor raíz XML**  
IDENTIFICADOR de recurso para la cadena "registro del sombreador base del intervalo de descriptores raíz" usado en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_REGISTER_SPACE"></span><span id="xml_root_descriptor_range_register_space"></span>**\_espacio de \_ \_ \_ registro del intervalo del DEscriptor raíz XML \_**  
IDENTIFICADOR de recurso de la cadena "espacio de registro del intervalo de descriptor raíz" usado en los datos XML.

<span id="XML_ROOT_DESCRIPTOR_RANGE_OFFSET"></span><span id="xml_root_descriptor_range_offset"></span>**\_desplazamiento de \_ intervalo de descriptor raíz XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "desplazamiento de intervalo de descriptor raíz" usado en los datos XML.

<span id="XML_ROOT_CONSTANTS_SHADER_REGISTER"></span><span id="xml_root_constants_shader_register"></span>**\_ \_ \_ registro del sombreador de constantes de raíz XML \_**  
IDENTIFICADOR de recurso para la cadena "registro del sombreador de constantes raíz" usado en los datos XML.

<span id="XML_ROOT_CONSTANTS_REGISTER_SPACE"></span><span id="xml_root_constants_register_space"></span>**\_espacio de \_ registro de constantes de raíz XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "root Constants Register Space" usado en los datos XML.

<span id="XML_ROOT_CONSTANTS_INT32_COUNT"></span><span id="xml_root_constants_int32_count"></span>**Recuento de INT32 de constantes de \_ raíz XML \_ \_ \_**  
IDENTIFICADOR de recurso para la cadena "recuento de constantes de raíz Int32" que se usa en los datos XML.

<span id="XML_NOT_SET"></span><span id="xml_not_set"></span>**XML \_ no \_ establecido**  
IDENTIFICADOR de recurso para la cadena "no establecida" que se usa en los datos XML.

<span id="XML_ERROR_RETRIEVING_VALUE"></span><span id="xml_error_retrieving_value"></span>**ERROR de XML \_ al \_ recuperar el \_ valor**  
IDENTIFICADOR de recurso para la cadena "error al recuperar el valor" usado en los datos XML.

<span id="XML_VERTEX_SHADER"></span><span id="xml_vertex_shader"></span>**\_sombreador de vértices XML \_**  
IDENTIFICADOR de recurso para la cadena "sombreador de vértices" usado en los datos XML.

<span id="XML_PIXEL_SHADER"></span><span id="xml_pixel_shader"></span>**\_sombreador de píxeles XML \_**  
IDENTIFICADOR de recurso para la cadena "sombreador de píxeles" usado en los datos XML.

<span id="XML_DOMAIN_SHADER"></span><span id="xml_domain_shader"></span>**\_sombreador de dominios XML \_**  
IDENTIFICADOR de recurso para la cadena "sombreador de dominios" usado en los datos XML.

<span id="XML_HULL_SHADER"></span><span id="xml_hull_shader"></span>**\_sombreador de casco XML \_**  
IDENTIFICADOR de recurso para la cadena "sombreador de casco" usado en los datos XML.

<span id="XML_GEOMETRY_SHADER"></span><span id="xml_geometry_shader"></span>**\_sombreador de geometría XML \_**  
IDENTIFICADOR de recurso para la cadena "sombreador de geometría" usado en los datos XML.

<span id="XML_ROOT_SIGNATURE_OBJECT"></span><span id="xml_root_signature_object"></span>**\_objeto de \_ firma \_ raíz XML**  
IDENTIFICADOR de recurso para la cadena "objeto de firma raíz" usado en los datos XML.

<span id="XML_ROOT_SIGNATURE_DESERIALIZED_DESC"></span><span id="xml_root_signature_deserialized_desc"></span>**DESC. de \_ signatura de raíz XML \_ \_ deserializada \_**  
IDENTIFICADOR de recurso para la cadena "desc de la signatura raíz deserializada" que se usa en los datos XML.

<span id="XML_COMMAND_QUEUE"></span><span id="xml_command_queue"></span>**\_cola de comandos XML \_**  
IDENTIFICADOR de recurso para la cadena "cola de comandos" que se usa en los datos XML.

<span id="XML_D3D12_COMMAND_LIST_TYPE_TYPE"></span><span id="xml_d3d12_command_list_type_type"></span>**\_Tipo de \_ \_ tipo de lista de comandos D3D12 \_ XML \_**  
IDENTIFICADOR de recurso de la cadena "d3d12 tipo de tipo de lista de comandos" que se usa en los datos XML.

<span id="XML_COMMAND_QUEUE_PRIORITY"></span><span id="xml_command_queue_priority"></span>**prioridad de la \_ cola de comandos XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "prioridad de la cola de comandos" que se usa en los datos XML.

<span id="XML_COMMAND_QUEUE_IS_DEFAULT"></span><span id="xml_command_queue_is_default"></span>**la \_ cola de comandos XML \_ \_ es \_ predeterminada**  
IDENTIFICADOR de recurso para la cadena "la cola de comandos es default" que se usa en los datos XML.

<span id="XML_COMMAND_ALLOCATOR"></span><span id="xml_command_allocator"></span>**\_asignador de comandos XML \_**  
IDENTIFICADOR de recurso de la cadena "asignador de comandos" que se usa en los datos XML.

<span id="XML_D3D12_RESOURCE"></span><span id="xml_d3d12_resource"></span>**\_Recurso D3D12 \_ XML**  
IDENTIFICADOR de recurso de la cadena "d3d12 Resource" que se usa en los datos XML.

<span id="XML_D3D12_DESCRIPTOR_HEAP"></span><span id="xml_d3d12_descriptor_heap"></span>**\_Montón de \_ descriptor D3D12 XML \_**  
IDENTIFICADOR de recurso de la cadena "d3d12 descriptor heap" que se usa en los datos XML.

<span id="XML_D3D12_DESCRIPTOR"></span><span id="xml_d3d12_descriptor"></span>**\_Descriptor D3D12 XML \_**  
IDENTIFICADOR de recurso de la cadena "d3d12 descriptor" que se usa en los datos XML.

<span id="XML_D3D12_RTV"></span><span id="xml_d3d12_rtv"></span>**XML \_ D3D12 \_ RTV**  
IDENTIFICADOR de recurso de la cadena "d3d12 RTV" que se usa en los datos XML.

<span id="XML_D3D12_VBV"></span><span id="xml_d3d12_vbv"></span>**\_VBV D3D12 \_ XML**  
IDENTIFICADOR de recurso de la cadena "d3d12 VBV" que se usa en los datos XML.

<span id="XML_D3D12_IBV"></span><span id="xml_d3d12_ibv"></span>**\_IBV D3D12 \_ XML**  
IDENTIFICADOR de recurso de la cadena "d3d12 IBV" que se usa en los datos XML.

<span id="XML_D3D12_CBV"></span><span id="xml_d3d12_cbv"></span>**\_CBV D3D12 \_ XML**  
IDENTIFICADOR de recurso de la cadena "d3d12 CBV" que se usa en los datos XML.

<span id="XML_D3D12_SRV"></span><span id="xml_d3d12_srv"></span>**XML \_ D3D12 \_ SRV**  
IDENTIFICADOR de recurso de la cadena "d3d12 SRV" que se usa en los datos XML.

<span id="XML_D3D12_UAV"></span><span id="xml_d3d12_uav"></span>**\_UAV D3D12 \_ XML**  
IDENTIFICADOR de recurso de la cadena "d3d12 UAV" que se usa en los datos XML.

<span id="XML_D3D12_SOV"></span><span id="xml_d3d12_sov"></span>**\_SOV D3D12 \_ XML**  
IDENTIFICADOR de recurso de la cadena "d3d12 SOV" que se usa en los datos XML.

<span id="XML_D3D12_SAMPLER"></span><span id="xml_d3d12_sampler"></span>**\_Muestra de D3D12 XML \_**  
IDENTIFICADOR de recurso de la cadena "d3d12 muestra" que se usa en los datos XML.

<span id="XML_D3D12_DSV"></span><span id="xml_d3d12_dsv"></span>**XML \_ D3D12 \_ DSV**  
IDENTIFICADOR de recurso de la cadena "d3d12 DSV" que se usa en los datos XML.

<span id="XML_TEXTURE_1D"></span><span id="xml_texture_1d"></span>**\_Textura XML \_ 1D**  
IDENTIFICADOR de recurso para la cadena "Texture 1D" que se usa en los datos XML.

<span id="XML_TEXTURE_1DARRAY"></span><span id="xml_texture_1darray"></span>**\_1DARRAY de textura XML \_**  
IDENTIFICADOR de recurso para la cadena "Texture 1darray" que se usa en los datos XML.

<span id="XML_TEXTURE_2D"></span><span id="xml_texture_2d"></span>**\_Textura XML \_ 2D**  
IDENTIFICADOR de recurso para la cadena "Texture 2D" que se usa en los datos XML.

<span id="XML_TEXTURE_2DARRAY"></span><span id="xml_texture_2darray"></span>**\_2DARRAY de textura XML \_**  
IDENTIFICADOR de recurso para la cadena "Texture 2darray" que se usa en los datos XML.

<span id="XML_TEXTURE_2DMS"></span><span id="xml_texture_2dms"></span>**\_2DMS de textura XML \_**  
IDENTIFICADOR de recurso para la cadena "Texture 2dms" que se usa en los datos XML.

<span id="XML_TEXTURE_2DMSARRAY"></span><span id="xml_texture_2dmsarray"></span>**\_2DMSARRAY de textura XML \_**  
IDENTIFICADOR de recurso para la cadena "Texture 2dmsarray" que se usa en los datos XML.

<span id="XML_TEXTURE_3D"></span><span id="xml_texture_3d"></span>**\_Textura XML \_ 3D**  
IDENTIFICADOR de recurso para la cadena "Texture 3D" que se usa en los datos XML.

<span id="XML_MISCELLANEOUS"></span><span id="xml_miscellaneous"></span>**XML \_ misceláneo**  
IDENTIFICADOR de recurso para la cadena "Miscellaneous" que se usa en los datos XML.

<span id="XML_RENDER_TARGET_VIEW_12"></span><span id="xml_render_target_view_12"></span>**\_Vista de destino de representación XML \_ \_ \_ 12**  
IDENTIFICADOR de recurso para la cadena "vista de destino de representación 12" que se usa en los datos XML.

<span id="XML_STRIDE_COUNT"></span><span id="xml_stride_count"></span>**recuento de \_ STRIDE XML \_**  
IDENTIFICADOR de recurso para la cadena "recuento de STRIDE" usado en los datos XML.

<span id="XML_SHADER_ROOT_SIGNATURES"></span><span id="xml_shader_root_signatures"></span>**\_firmas raíz del sombreador XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "firmas de raíz del sombreador" utilizada en los datos XML.

<span id="XML_CMDLIST_ROOT_SIGNATURE_OBJECT"></span><span id="xml_cmdlist_root_signature_object"></span>**\_objeto de \_ \_ firma raíz CMDLIST XML \_**  
IDENTIFICADOR de recurso de la cadena "cmdlist root Signature Object" que se usa en los datos XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_COUNT"></span><span id="xml_input_layout_element_count"></span>**recuento de elementos de diseño de \_ entrada XML \_ \_ \_**  
IDENTIFICADOR de recurso para la cadena "recuento de elementos de diseño de entrada" usado en los datos XML.

<span id="XML_INPUT_LAYOUT_ELEMENT_DESCRIPTIONS"></span><span id="xml_input_layout_element_descriptions"></span>**\_descripciones de elementos de diseño de entrada XML \_ \_ \_**  
IDENTIFICADOR de recurso para la cadena "descripciones de elementos de diseño de entrada" que se usa en datos XML.

<span id="XML_SHADER_DESCRIPTION"></span><span id="xml_shader_description"></span>**\_Descripción del sombreador XML \_**  
IDENTIFICADOR de recurso para la cadena "Descripción del sombreador" que se usa en los datos XML.

<span id="XML_PSO_ROOT_SIGNATURE_OBJECT"></span><span id="xml_pso_root_signature_object"></span>**\_objeto de \_ \_ firma raíz \_ de XML PSO**  
IDENTIFICADOR de recurso para la cadena "PSO raíz Signature Object" usado en los datos XML.

<span id="XML_BUFFER_LOCATION"></span><span id="xml_buffer_location"></span>**\_Ubicación del búfer XML \_**  
IDENTIFICADOR de recurso para la cadena "ubicación de búfer" usada en los datos XML.

<span id="XML_BUFFER_FILLED_LOCATION"></span><span id="xml_buffer_filled_location"></span>**\_ \_ Ubicación rellena de búfer XML \_**  
IDENTIFICADOR de recurso para la cadena "Ubicación rellena del búfer" que se usa en los datos XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS"></span><span id="xml_d3d12_virtual_address"></span>**\_ \_ Dirección virtual D3D12 \_ XML**  
IDENTIFICADOR de recurso de la cadena "d3d12 virtual address" que se usa en los datos XML.

<span id="XML_D3D12_VIRTUAL_ADDRESS_OFFSET"></span><span id="xml_d3d12_virtual_address_offset"></span>**\_Desplazamiento de \_ \_ dirección virtual D3D12 XML \_**  
IDENTIFICADOR de recurso de la cadena "d3d12 virtual Address offset" que se usa en los datos XML.

<span id="XML_D3D12_OFFSET_IN_BYTES"></span><span id="xml_d3d12_offset_in_bytes"></span>**\_Desplazamiento D3D12 \_ XML \_ en \_ bytes**  
IDENTIFICADOR de recurso de la cadena "d3d12 PosiciónInicial en bytes" que se usa en los datos XML.

<span id="XML_MAPPED_TILES"></span><span id="xml_mapped_tiles"></span>**\_mosaicos asignados XML \_**  
IDENTIFICADOR de recurso para la cadena "mosaicos asignados" que se usa en los datos XML.

<span id="XML_POOL_TILES"></span><span id="xml_pool_tiles"></span>**\_mosaicos de grupo XML \_**  
IDENTIFICADOR de recurso para la cadena "mosaicos de grupo" que se usa en los datos XML.

<span id="XML_TILE"></span><span id="xml_tile"></span>**\_mosaico XML**  
IDENTIFICADOR de recurso para la cadena "Tile" que se usa en los datos XML.

<span id="XML_OPTIONS"></span><span id="xml_options"></span>**\_Opciones XML**  
IDENTIFICADOR de recurso para la cadena "Options" que se usa en los datos XML.

<span id="XML_TILE_LIST_MODE"></span><span id="xml_tile_list_mode"></span>**\_modo de \_ lista de mosaicos XML \_**  
IDENTIFICADOR de recurso para la cadena "modo de lista de mosaicos" usado en los datos XML.

<span id="XML_TILE_POOL_SLOT"></span><span id="xml_tile_pool_slot"></span>**\_ranura del \_ grupo de mosaicos XML \_**  
IDENTIFICADOR de recurso para la cadena "ranura del grupo de mosaicos" utilizada en los datos XML.

<span id="XML_TILE_COORDINATE"></span><span id="xml_tile_coordinate"></span>**\_coordenadas del mosaico XML \_**  
IDENTIFICADOR de recurso para la cadena "coordenadas del mosaico" que se usa en los datos XML.

<span id="XML_HEAP_TYPE"></span><span id="xml_heap_type"></span>**\_tipo de montón XML \_**  
IDENTIFICADOR de recurso para la cadena "tipo de montón" usado en los datos XML.

<span id="XML_RESOURCE_USAGE"></span><span id="xml_resource_usage"></span>**\_uso de recursos XML \_**  
IDENTIFICADOR de recurso para la cadena "uso de recursos" usado en los datos XML.

<span id="XML_RESOURCE_USAGES"></span><span id="xml_resource_usages"></span>**\_usos de recursos XML \_**  
IDENTIFICADOR de recurso para la cadena "usos de recursos" que se usa en los datos XML.

<span id="XML_SUBRESOURCE"></span><span id="xml_subresource"></span>**subrecurso XML \_**  
IDENTIFICADOR de recurso para la cadena "subresource" que se usa en los datos XML.

<span id="XML_SUBRESOURCE_USAGE"></span><span id="xml_subresource_usage"></span>**\_uso de SUBrecursos XML \_**  
IDENTIFICADOR de recurso para la cadena "uso de subrecurso" usado en los datos XML.

<span id="XML_COMPUTE_SHADER"></span><span id="xml_compute_shader"></span>**\_sombreador de cálculo de XML \_**  
IDENTIFICADOR de recurso para la cadena "Shader Calculator" que se usa en los datos XML.

<span id="XML_COMMAND_LIST_12"></span><span id="xml_command_list_12"></span>**\_Lista de comandos XML \_ \_ 12**  
IDENTIFICADOR de recurso para la cadena "Command List 12" que se usa en los datos XML.

<span id="XML_PSO_RENDER_TARGET_FORMATS"></span><span id="xml_pso_render_target_formats"></span>**\_formatos de \_ destino de representación \_ \_ de XML PSO**  
IDENTIFICADOR de recurso para la cadena "PSO Render Target Formats" que se usa en los datos XML.

<span id="XML_RENDER_TARGET_VIEWS"></span><span id="xml_render_target_views"></span>**\_vistas de \_ destino de representación XML \_**  
IDENTIFICADOR de recurso para la cadena "representar vistas de destino" que se usa en los datos XML.

<span id="XML_SHADER_RESOURCE_VIEWS"></span><span id="xml_shader_resource_views"></span>**\_vistas de recursos del sombreador XML \_ \_**  
IDENTIFICADOR de recurso para la cadena "vistas de recursos del sombreador" que se usa en los datos XML.

## <a name="requirements"></a>Requisitos

<table><colgroup><col style="width: 50%" /><col style="width: 50%" /></colgroup><tbody><tr class="odd"><td><p>Encabezado</p></td><td>Vspixengine. h</td></tr></tbody></table>

 

 



