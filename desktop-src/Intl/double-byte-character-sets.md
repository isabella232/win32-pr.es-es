---
description: Juego de caracteres de doble byte (DBCS), también conocido como &\# 0034; juego de caracteres de 8 bits expandido&\# 0034;, es un juego de caracteres de byte único extendido (SBCS), implementado como una página de códigos.
ms.assetid: df049d22-02e2-48b2-8b74-52f71c00c549
title: Juegos de caracteres de doble byte
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 431b762b19f5531644e4bbaace548f34245c9d5c
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 01/08/2021
ms.locfileid: "105688347"
---
# <a name="double-byte-character-sets"></a><span data-ttu-id="4ed89-103">Juegos de caracteres de doble byte</span><span class="sxs-lookup"><span data-stu-id="4ed89-103">Double-byte Character Sets</span></span>

<span data-ttu-id="4ed89-104">Un juego de caracteres de doble byte (DBCS), también conocido como "juego de caracteres de 8 bits expandido", es un [juego de caracteres de un solo byte](single-byte-character-sets.md) extendido (SBCS), implementado como una página de códigos.</span><span class="sxs-lookup"><span data-stu-id="4ed89-104">A double-byte character set (DBCS), also known as an "expanded 8-bit character set", is an extended [single-byte character set](single-byte-character-sets.md) (SBCS), implemented as a code page.</span></span> <span data-ttu-id="4ed89-105">DBCS se desarrolló originalmente para ampliar el diseño de SBCS para administrar idiomas como el japonés y el chino.</span><span class="sxs-lookup"><span data-stu-id="4ed89-105">DBCSs were originally developed to extend the SBCS design to handle languages such as Japanese and Chinese.</span></span> <span data-ttu-id="4ed89-106">Algunos caracteres de un DBCS, incluidos los dígitos y las letras que se usan para escribir en inglés, tienen valores de código de un solo byte.</span><span class="sxs-lookup"><span data-stu-id="4ed89-106">Some characters in a DBCS, including the digits and letters used for writing English, have single-byte code values.</span></span> <span data-ttu-id="4ed89-107">Otros caracteres, como los ideogramas chinos o el kanji japonés, tienen valores de código de doble byte.</span><span class="sxs-lookup"><span data-stu-id="4ed89-107">Other characters, such as Chinese ideographs or Japanese kanji, have double-byte code values.</span></span> <span data-ttu-id="4ed89-108">Un DBCS puede corresponder a una página de códigos de Windows o a una página de códigos OEM.</span><span class="sxs-lookup"><span data-stu-id="4ed89-108">A DBCS can correspond either to a Windows code page or an OEM code page.</span></span> <span data-ttu-id="4ed89-109">Una página de códigos DBCS también puede incluir una página de códigos no nativa, por ejemplo, una página de códigos EBCDIC.</span><span class="sxs-lookup"><span data-stu-id="4ed89-109">A DBCS code page can also include a non-native code page, for example, an EBCDIC code page.</span></span> <span data-ttu-id="4ed89-110">Para ver las definiciones de estas páginas de códigos, vea [páginas de códigos](code-pages.md).</span><span class="sxs-lookup"><span data-stu-id="4ed89-110">For definitions of these code pages, see [Code Pages](code-pages.md).</span></span>

> [!Note]  
> <span data-ttu-id="4ed89-111">Las nuevas aplicaciones de Windows deben usar [Unicode](unicode.md) para evitar las incoherencias de las distintas páginas de códigos y para facilitar la localización.</span><span class="sxs-lookup"><span data-stu-id="4ed89-111">New Windows applications should use [Unicode](unicode.md) to avoid the inconsistencies of varied code pages and for ease of localization.</span></span> <span data-ttu-id="4ed89-112">Sin embargo, algunos protocolos heredados podrían requerir el uso de páginas de códigos DBCS.</span><span class="sxs-lookup"><span data-stu-id="4ed89-112">However, some legacy protocols might require the use of DBCS code pages.</span></span> <span data-ttu-id="4ed89-113">Cada página de códigos DBCS admite distintos caracteres, pero ninguna página admite toda la amplitud de caracteres proporcionados por Unicode.</span><span class="sxs-lookup"><span data-stu-id="4ed89-113">Each DBCS code page supports different characters, but no page supports the full breadth of characters provided by Unicode.</span></span> <span data-ttu-id="4ed89-114">Cada página de códigos DBCS admite un subconjunto diferente, codificado de forma diferente.</span><span class="sxs-lookup"><span data-stu-id="4ed89-114">Each DBCS code page supports a different subset, differently encoded.</span></span> <span data-ttu-id="4ed89-115">Los datos convertidos de una página de códigos DBCS a otra están sujetos a daños porque el mismo valor de datos en páginas de códigos diferentes puede codificar un carácter diferente.</span><span class="sxs-lookup"><span data-stu-id="4ed89-115">Data converted from one DBCS code page to another is subject to corruption because the same data value on different code pages can encode a different character.</span></span> <span data-ttu-id="4ed89-116">Los datos convertidos de Unicode a DBCS están sujetos a pérdida de datos, ya que es posible que una página de códigos determinada no pueda representar todos los caracteres utilizados en esos datos Unicode concretos.</span><span class="sxs-lookup"><span data-stu-id="4ed89-116">Data converted from Unicode to DBCS is subject to data loss, because a given code page might not be able to represent every character used in that particular Unicode data.</span></span>

 

<span data-ttu-id="4ed89-117">Para interpretar una cadena DBCS, una aplicación debe empezar al principio de la cadena y examinar hacia delante.</span><span class="sxs-lookup"><span data-stu-id="4ed89-117">To interpret a DBCS string, an application must start at the beginning of the string and scan forward.</span></span> <span data-ttu-id="4ed89-118">Realiza un seguimiento cuando encuentra un byte inicial en la cadena y trata el siguiente byte como la parte final del mismo carácter.</span><span class="sxs-lookup"><span data-stu-id="4ed89-118">It keeps track when it encounters a lead byte in the string, and treats the next byte as the trailing part of the same character.</span></span> <span data-ttu-id="4ed89-119">Si la aplicación simplemente examina la cadena de un byte a la vez y encuentra un byte que parece ser el valor de código que representa una barra diagonal inversa (" \\ "), ese byte podría ser simplemente el byte final de un carácter de dos bytes.</span><span class="sxs-lookup"><span data-stu-id="4ed89-119">If the application simply scans the string one byte at a time and encounters a byte that appears to be the code value representing a backslash ("\\"), that byte might simply be the trail byte of a two-byte character.</span></span> <span data-ttu-id="4ed89-120">La aplicación no puede simplemente hacer una copia de seguridad de un byte para ver si el byte anterior es un byte inicial, ya que ese valor de byte puede ser válido para su uso como byte inicial y como byte final.</span><span class="sxs-lookup"><span data-stu-id="4ed89-120">The application cannot just back up one byte to see if the preceding byte is a lead byte, as that byte value might be eligible to be used as both a lead byte and a trail byte.</span></span> <span data-ttu-id="4ed89-121">Por lo tanto, la aplicación tiene esencialmente el mismo problema que con la barra diagonal inversa posible.</span><span class="sxs-lookup"><span data-stu-id="4ed89-121">Thus the application has essentially the same problem with it as with the possible backslash.</span></span> <span data-ttu-id="4ed89-122">En otras palabras, las búsquedas de subcadenas son mucho más complicadas con un DBCS que con SBCSs o Unicode.</span><span class="sxs-lookup"><span data-stu-id="4ed89-122">In other words, substring searches are much more complicated with a DBCS than with either SBCSs or Unicode.</span></span> <span data-ttu-id="4ed89-123">En consecuencia, las aplicaciones que admiten DBCS deben usar funciones especiales, como [ \_ mbsstr](/cpp/c-runtime-library/reference/strstr-wcsstr-mbsstr-mbsstr-l), en lugar de la función [**StrStr**](https://msdn.microsoft.com/library/z9da80kz(v=VS.71).aspx) .</span><span class="sxs-lookup"><span data-stu-id="4ed89-123">Accordingly, applications that support a DBCS must use special functions, such as [\_mbsstr](/cpp/c-runtime-library/reference/strstr-wcsstr-mbsstr-mbsstr-l), instead of the [**StrStr**](https://msdn.microsoft.com/library/z9da80kz(v=VS.71).aspx) function.</span></span>

<span data-ttu-id="4ed89-124">Las aplicaciones usan páginas de códigos DBCS de Windows con las versiones "A" de las funciones de Windows.</span><span class="sxs-lookup"><span data-stu-id="4ed89-124">Your applications use DBCS Windows code pages with the "A" versions of Windows functions.</span></span> <span data-ttu-id="4ed89-125">Vea [convenciones para prototipos de función](conventions-for-function-prototypes.md) y [páginas de códigos](code-pages.md).</span><span class="sxs-lookup"><span data-stu-id="4ed89-125">See [Conventions for Function Prototypes](conventions-for-function-prototypes.md) and [Code Pages](code-pages.md).</span></span> <span data-ttu-id="4ed89-126">Para ayudar a identificar una página de códigos DBCS, una aplicación puede usar la función [**GetCPInfo**](/windows/desktop/api/Winnls/nf-winnls-getcpinfo) o [**GetCPInfoEx**](/windows/desktop/api/Winnls/nf-winnls-getcpinfoexa) .</span><span class="sxs-lookup"><span data-stu-id="4ed89-126">To help identify a DBCS code page, an application can use the [**GetCPInfo**](/windows/desktop/api/Winnls/nf-winnls-getcpinfo) or [**GetCPInfoEx**](/windows/desktop/api/Winnls/nf-winnls-getcpinfoexa) function.</span></span> <span data-ttu-id="4ed89-127">Una aplicación puede usar la función [**IsDBCSLeadByte**](/windows/desktop/api/Winnls/nf-winnls-isdbcsleadbyte) para determinar si un valor determinado se puede usar como byte inicial de un carácter de 2 bytes.</span><span class="sxs-lookup"><span data-stu-id="4ed89-127">An application can use the [**IsDBCSLeadByte**](/windows/desktop/api/Winnls/nf-winnls-isdbcsleadbyte) function to determine if a given value can be used as the lead byte of a 2-byte character.</span></span> <span data-ttu-id="4ed89-128">Además, una aplicación puede usar las funciones [**MultiByteToWideChar**](/windows/desktop/api/Stringapiset/nf-stringapiset-multibytetowidechar) y [**WideCharToMultiByte**](/windows/desktop/api/Stringapiset/nf-stringapiset-widechartomultibyte) para asignar entre cadenas Unicode y DBCS.</span><span class="sxs-lookup"><span data-stu-id="4ed89-128">In addition, an application can use the [**MultiByteToWideChar**](/windows/desktop/api/Stringapiset/nf-stringapiset-multibytetowidechar) and [**WideCharToMultiByte**](/windows/desktop/api/Stringapiset/nf-stringapiset-widechartomultibyte) functions to map between Unicode and DBCS strings.</span></span>

## <a name="related-topics"></a><span data-ttu-id="4ed89-129">Temas relacionados</span><span class="sxs-lookup"><span data-stu-id="4ed89-129">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="4ed89-130">Juegos de caracteres</span><span class="sxs-lookup"><span data-stu-id="4ed89-130">Character Sets</span></span>](character-sets.md)
</dt> <dt>

[<span data-ttu-id="4ed89-131">Juegos de caracteres de un solo byte</span><span class="sxs-lookup"><span data-stu-id="4ed89-131">Single-byte Character Sets</span></span>](single-byte-character-sets.md)
</dt> </dl>

 

 
