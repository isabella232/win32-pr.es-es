---
description: Las extensiones en proceso se cargan en los procesos que las desencadenan.
title: Instrucciones para implementar extensiones de In-Process
ms.topic: article
ms.date: 05/31/2018
ms.assetid: FE830DBF-3F18-453c-9A51-91E10559D0E8
api_name: ''
api_type: ''
api_location: ''
topic_type:
- kbArticle
ms.openlocfilehash: e4dc9fd0573f3f98f0ec1110079f95f56a8c42e1
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 01/08/2021
ms.locfileid: "104003075"
---
# <a name="guidance-for-implementing-in-process-extensions"></a><span data-ttu-id="c8f94-103">Instrucciones para implementar extensiones de In-Process</span><span class="sxs-lookup"><span data-stu-id="c8f94-103">Guidance for Implementing In-Process Extensions</span></span>

<span data-ttu-id="c8f94-104">Las extensiones en proceso se cargan en los procesos que las desencadenan.</span><span class="sxs-lookup"><span data-stu-id="c8f94-104">In-process extensions are loaded into any processes that trigger them.</span></span> <span data-ttu-id="c8f94-105">Por ejemplo, una extensión de espacio de nombres de Shell se puede cargar en cualquier proceso que tenga acceso al espacio de nombres del shell, ya sea directa o indirectamente.</span><span class="sxs-lookup"><span data-stu-id="c8f94-105">For example, a Shell namespace extension can be loaded into any process which accesses the Shell namespace either directly or indirectly.</span></span> <span data-ttu-id="c8f94-106">El espacio de nombres Shell se usa en muchas operaciones de Shell, como la presentación de un cuadro de diálogo de archivo común, el inicio de un documento a través de su aplicación asociada o la obtención del icono que se usa para representar un archivo.</span><span class="sxs-lookup"><span data-stu-id="c8f94-106">The Shell namespace is used by many Shell operations, such as the display of a common file dialog, the launch of a document through its associated application, or the obtaining of the icon used to represent a file.</span></span> <span data-ttu-id="c8f94-107">Dado que las extensiones en proceso se pueden cargar en procesos arbitrarios, se debe tener cuidado de que no afecten negativamente a la aplicación host u otras extensiones en proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-107">Because in-process extensions can be loaded into arbitrary processes, care must be taken that they do not negatively impact the host application or other in-process extensions.</span></span>

<span data-ttu-id="c8f94-108">Un tiempo de ejecución de una nota concreta es el *Common Language Runtime (CLR)*, también conocido como *código administrado* o el *.NET Framework*.</span><span class="sxs-lookup"><span data-stu-id="c8f94-108">One runtime of particular note is the *common language runtime (CLR)*, also known as *managed code* or the *.NET Framework*.</span></span> <span data-ttu-id="c8f94-109">**Microsoft recomienda no escribir extensiones en proceso administradas en el explorador de Windows o en Windows Internet Explorer y no las considera un escenario admitido.**</span><span class="sxs-lookup"><span data-stu-id="c8f94-109">**Microsoft recommends against writing managed in-process extensions to Windows Explorer or Windows Internet Explorer and does not consider them a supported scenario.**</span></span>

<span data-ttu-id="c8f94-110">En este tema se describen los factores que se deben tener en cuenta al determinar si un tiempo de ejecución distinto del CLR es adecuado para su uso por parte de extensiones en proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-110">This topic discusses factors to consider when you determine whether any runtime other than the CLR is suitable for use by in-process extensions.</span></span> <span data-ttu-id="c8f94-111">Algunos ejemplos de otros tiempos de ejecución son Java, Visual Basic, JavaScript/ECMAScript, Delphi y la biblioteca en tiempo de ejecución de C/C++.</span><span class="sxs-lookup"><span data-stu-id="c8f94-111">Examples of other runtimes include Java, Visual Basic, JavaScript/ECMAScript, Delphi, and the C/C++ runtime library.</span></span> <span data-ttu-id="c8f94-112">En este tema también se proporcionan algunos motivos por los que el código administrado no se admite en las extensiones en proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-112">This topic also provides some reasons that managed code is unsupported in in-process extensions.</span></span>

## <a name="version-conflicts"></a><span data-ttu-id="c8f94-113">Conflictos de versiones</span><span class="sxs-lookup"><span data-stu-id="c8f94-113">Version Conflicts</span></span>

<span data-ttu-id="c8f94-114">Un conflicto de versión puede producirse a través de un tiempo de ejecución que no admite la carga de varias versiones en tiempo de ejecución en un único proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-114">A version conflict can arise through a runtime that does not support the loading of multiple runtime versions within a single process.</span></span> <span data-ttu-id="c8f94-115">Las versiones de CLR anteriores a la versión 4,0 se encuentran en esta categoría.</span><span class="sxs-lookup"><span data-stu-id="c8f94-115">Versions of the CLR prior to version 4.0 fall into this category.</span></span> <span data-ttu-id="c8f94-116">Si la carga de una versión de un Runtime impide la carga de otras versiones de ese mismo tiempo de ejecución, se puede producir un conflicto si la aplicación host u otra extensión en proceso utiliza una versión en conflicto.</span><span class="sxs-lookup"><span data-stu-id="c8f94-116">If the loading of one version of a runtime precludes the loading of other versions of that same runtime, this can create a conflict if the host application or another in-process extension uses a conflicting version.</span></span> <span data-ttu-id="c8f94-117">En el caso de un conflicto de versión con otra extensión en proceso, el conflicto puede ser difícil de reproducir porque el error requiere las extensiones en conflicto correctas y el modo de error depende del orden en el que se cargan las extensiones en conflicto.</span><span class="sxs-lookup"><span data-stu-id="c8f94-117">In the case of a version conflict with another in-process extension, the conflict can be difficult to reproduce because the failure requires the right conflicting extensions and the failure mode depends on the order in which the conflicting extensions are loaded.</span></span>

<span data-ttu-id="c8f94-118">Considere la posibilidad de usar una extensión en proceso escrita con una versión de CLR anterior a la versión 4,0.</span><span class="sxs-lookup"><span data-stu-id="c8f94-118">Consider an in-process extension written using a version of the CLR prior to version 4.0.</span></span> <span data-ttu-id="c8f94-119">Todas las aplicaciones del equipo que utilizan un cuadro de diálogo **abrir** archivo podrían tener el código administrado del cuadro de diálogo y su dependencia CLR del operador cargadas en el proceso de la aplicación.</span><span class="sxs-lookup"><span data-stu-id="c8f94-119">Every application on the computer that uses a file **Open** dialog box could potentially have the dialog's managed code and its attendant CLR dependency loaded into the application's process.</span></span> <span data-ttu-id="c8f94-120">La aplicación o extensión que es la primera vez que se carga una versión anterior a 4,0 de CLR en el proceso de la aplicación restringe qué versiones de CLR se pueden usar posteriormente en ese proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-120">The application or extension that is first to load a pre-4.0 version of the CLR into the application's process restricts which versions of the CLR can be used subsequently by that process.</span></span> <span data-ttu-id="c8f94-121">Si una aplicación administrada con un cuadro de diálogo **abrir** se basa en una versión en conflicto de CLR, la extensión podría no ejecutarse correctamente y podría producir errores en la aplicación.</span><span class="sxs-lookup"><span data-stu-id="c8f94-121">If a managed application with an **Open** dialog box is built on a conflicting version of the CLR, then the extension could fail to run correctly and could cause failures in the application.</span></span> <span data-ttu-id="c8f94-122">Por el contrario, si la extensión es la primera que se carga en un proceso y una versión en conflicto de código administrado intenta iniciarse después de eso (quizás una aplicación administrada o una aplicación en ejecución carga el CLR a petición), se produce un error en la operación.</span><span class="sxs-lookup"><span data-stu-id="c8f94-122">Conversely, if the extension is the first to load in a process and a conflicting version of managed code tries to launch after that (perhaps a managed application or a running application loads the CLR on demand), the operation fails.</span></span> <span data-ttu-id="c8f94-123">Para el usuario, parece que algunas características de la aplicación detienen un funcionamiento aleatorio o la aplicación se bloquea.</span><span class="sxs-lookup"><span data-stu-id="c8f94-123">To the user, it appears that some features of the application randomly stop working, or the application mysteriously crashes.</span></span>

<span data-ttu-id="c8f94-124">Tenga en cuenta que las versiones de CLR iguales o posteriores a la versión 4,0 no suelen ser susceptibles del problema de control de versiones porque están diseñadas para coexistir entre sí y con la mayoría de las versiones anteriores a 4,0 de CLR (con la excepción de la versión 1,0, que no pueden coexistir con otras versiones).</span><span class="sxs-lookup"><span data-stu-id="c8f94-124">Note that versions of the CLR equal to or later than version 4.0 are not generally susceptible to the versioning problem because they are designed to coexist with each other and with most pre-4.0 versions of the CLR (with the exception of version 1.0, which cannot coexist with other versions).</span></span> <span data-ttu-id="c8f94-125">Sin embargo, pueden surgir problemas distintos de los conflictos de versión, como se describe en el resto de este tema.</span><span class="sxs-lookup"><span data-stu-id="c8f94-125">However, issues other than version conflicts can arise as discussed in the remainder of this topic.</span></span>

## <a name="performance-issues"></a><span data-ttu-id="c8f94-126">Problemas de rendimiento</span><span class="sxs-lookup"><span data-stu-id="c8f94-126">Performance Issues</span></span>

<span data-ttu-id="c8f94-127">Pueden surgir problemas de rendimiento con los tiempos de ejecución que imponen una penalización significativa del rendimiento cuando se cargan en un proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-127">Performance issues can arise with runtimes that impose a significant performance penalty when they are loaded into a process.</span></span> <span data-ttu-id="c8f94-128">La reducción del rendimiento puede ser en forma de uso de memoria, uso de CPU, tiempo transcurrido o incluso el consumo de espacio de direcciones.</span><span class="sxs-lookup"><span data-stu-id="c8f94-128">The performance penalty can be in the form of memory usage, CPU usage, elapsed time, or even address space consumption.</span></span> <span data-ttu-id="c8f94-129">Se sabe que CLR, JavaScript/ECMAScript y Java son tiempos de ejecución de gran impacto.</span><span class="sxs-lookup"><span data-stu-id="c8f94-129">The CLR, JavaScript/ECMAScript, and Java are known to be high-impact runtimes.</span></span> <span data-ttu-id="c8f94-130">Dado que las extensiones en proceso se pueden cargar en muchos procesos y, a menudo, se realizan en momentos sensibles al rendimiento (por ejemplo, cuando se prepara un menú para mostrar el usuario), los tiempos de ejecución de gran impacto pueden afectar negativamente a la capacidad de respuesta global.</span><span class="sxs-lookup"><span data-stu-id="c8f94-130">Since in-process extensions can be loaded into many processes, and are often done so at performance-sensitive moments (such as when preparing a menu to be displayed the user), high-impact runtimes can negatively impact overall responsiveness.</span></span>

<span data-ttu-id="c8f94-131">Un tiempo de ejecución de gran impacto que consume recursos significativos puede producir un error en el proceso de host o en otra extensión en proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-131">A high-impact runtime that consumes significant resources can cause a failure in the host process or another in-process extension.</span></span> <span data-ttu-id="c8f94-132">Por ejemplo, un tiempo de ejecución de gran impacto que consume cientos de megabytes de espacio de direcciones para su montón puede dar lugar a que la aplicación host no pueda cargar un conjunto de resultados grande.</span><span class="sxs-lookup"><span data-stu-id="c8f94-132">For example, a high-impact runtime that consumes hundreds of megabytes of address space for its heap can result in the host application being unable to load a large dataset.</span></span> <span data-ttu-id="c8f94-133">Además, dado que las extensiones en proceso se pueden cargar en varios procesos, el consumo elevado de recursos en una única extensión puede multiplicar rápidamente a un alto consumo de recursos en todo el sistema.</span><span class="sxs-lookup"><span data-stu-id="c8f94-133">Furthermore, because in-process extensions can be loaded into multiple processes, high resource consumption in a single extension can quickly multiply into high resource consumption across the entire system.</span></span>

<span data-ttu-id="c8f94-134">Si un Runtime permanece cargado o sigue consumiendo recursos incluso cuando se ha descargado la extensión que usa ese Runtime, ese tiempo de ejecución no es apto para su uso en una extensión.</span><span class="sxs-lookup"><span data-stu-id="c8f94-134">If a runtime remains loaded or otherwise continues to consume resources even when the extension that uses that runtime has unloaded, then that runtime is not suitable for use in an extension.</span></span>

## <a name="issues-specific-to-the-net-framework"></a><span data-ttu-id="c8f94-135">Problemas específicos del .NET Framework</span><span class="sxs-lookup"><span data-stu-id="c8f94-135">Issues Specific to the .NET Framework</span></span>

<span data-ttu-id="c8f94-136">En las secciones siguientes se describen ejemplos de problemas encontrados con el uso de código administrado para extensiones.</span><span class="sxs-lookup"><span data-stu-id="c8f94-136">The following sections discuss examples of issues found with using managed code for extensions.</span></span> <span data-ttu-id="c8f94-137">No se trata de una lista completa de todos los posibles problemas que pueden surgir.</span><span class="sxs-lookup"><span data-stu-id="c8f94-137">They are not a complete list of all possible issues that you might encounter.</span></span> <span data-ttu-id="c8f94-138">Los problemas descritos aquí son los motivos por los que no se admite el código administrado en las extensiones y los puntos que se deben tener en cuenta al evaluar el uso de otros runtimes.</span><span class="sxs-lookup"><span data-stu-id="c8f94-138">The issues discussed here are both reasons that managed code is not supported in extensions and points to consider when you evaluate the use of other runtimes.</span></span>

### <a name="re-entrancy"></a><span data-ttu-id="c8f94-139">Volver a entrar</span><span class="sxs-lookup"><span data-stu-id="c8f94-139">Re-entrancy</span></span>

<span data-ttu-id="c8f94-140">Cuando CLR bloquea un subproceso de apartamento de un solo subproceso (STA), por ejemplo, debido a un monitor. Enter, WaitHandle. WaitOne o una instrucción [**Lock**](https://msdn.microsoft.com/library/c5kehkcz(v=VS.71).aspx) , CLR, en su configuración estándar, escribe un bucle de mensajes anidado mientras espera.</span><span class="sxs-lookup"><span data-stu-id="c8f94-140">When the CLR blocks a single-threaded apartment (STA) thread, for example, due to a Monitor.Enter, WaitHandle.WaitOne, or contended [**lock**](https://msdn.microsoft.com/library/c5kehkcz(v=VS.71).aspx) statement, the CLR, in its standard configuration, enters a nested message loop while it waits.</span></span> <span data-ttu-id="c8f94-141">Muchos métodos de extensión están prohibidos para el procesamiento de mensajes, y esta reentrada imprevisible e inesperada puede dar lugar a un comportamiento anómalo que es difícil de reproducir y diagnosticar.</span><span class="sxs-lookup"><span data-stu-id="c8f94-141">Many extension methods are prohibited from processing messages, and this unpredictable and unexpected reentrancy can result in anomalous behavior which is difficult to reproduce and diagnose.</span></span>

### <a name="the-multithreaded-apartment"></a><span data-ttu-id="c8f94-142">Apartamento multiproceso</span><span class="sxs-lookup"><span data-stu-id="c8f94-142">The Multithreaded Apartment</span></span>

<span data-ttu-id="c8f94-143">CLR crea *contenedores RCW (Runtime Callable wrappers* ) para objetos del modelo de objetos componentes (com).</span><span class="sxs-lookup"><span data-stu-id="c8f94-143">The CLR creates *Runtime Callable Wrappers* for Component Object Model (COM) objects.</span></span> <span data-ttu-id="c8f94-144">Los mismos contenedores a los que se puede llamar en tiempo de ejecución se destruyen más adelante por el finalizador de CLR, que forma parte del contenedor multiproceso (MTA).</span><span class="sxs-lookup"><span data-stu-id="c8f94-144">These same Runtime Callable Wrappers are destroyed later by the CLR's finalizer, which is part of the multithreaded apartment (MTA).</span></span> <span data-ttu-id="c8f94-145">Mover el proxy del STA al MTA requiere serialización, pero no se pueden calcular las referencias de todas las interfaces utilizadas por las extensiones.</span><span class="sxs-lookup"><span data-stu-id="c8f94-145">Moving the proxy from the STA to the MTA requires marshaling, but not all interfaces used by extensions can be marshalled.</span></span>

### <a name="non-deterministic-object-lifetimes"></a><span data-ttu-id="c8f94-146">Duración de los objetos no deterministas</span><span class="sxs-lookup"><span data-stu-id="c8f94-146">Non-Deterministic Object Lifetimes</span></span>

<span data-ttu-id="c8f94-147">CLR tiene garantías de duración de objetos más débiles que el código nativo.</span><span class="sxs-lookup"><span data-stu-id="c8f94-147">The CLR has weaker object lifetime guarantees than native code.</span></span> <span data-ttu-id="c8f94-148">Muchas extensiones tienen requisitos de recuento de referencias en objetos e interfaces, y el modelo de recolección de elementos no utilizados que emplea CLR no puede cumplir estos requisitos.</span><span class="sxs-lookup"><span data-stu-id="c8f94-148">Many extensions have reference count requirements on objects and interfaces, and the garbage-collection model employed by the CLR cannot fulfill these requirements.</span></span>

-   <span data-ttu-id="c8f94-149">Si un objeto CLR obtiene una referencia a un objeto COM, la referencia de objeto COM retenida por el contenedor al que se puede llamar en tiempo de ejecución no se libera hasta que el contenedor al que se puede llamar en tiempo de ejecución se recolecta como elemento no utilizado.</span><span class="sxs-lookup"><span data-stu-id="c8f94-149">If a CLR object obtains a reference to a COM object, the COM object reference held by the Runtime Callable Wrapper is not released until the Runtime Callable Wrapper is garbage-collected.</span></span> <span data-ttu-id="c8f94-150">El comportamiento de la versión no determinista puede entrar en conflicto con algunos contratos de interfaz.</span><span class="sxs-lookup"><span data-stu-id="c8f94-150">Nondeterministic release behavior can conflict with some interface contracts.</span></span> <span data-ttu-id="c8f94-151">Por ejemplo, el método [**IPersistPropertyBag:: Load**](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa768206(v=vs.85)) requiere que el objeto no retenga ninguna referencia al contenedor de propiedades cuando se devuelva el método **Load** .</span><span class="sxs-lookup"><span data-stu-id="c8f94-151">For example, the [**IPersistPropertyBag::Load**](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa768206(v=vs.85)) method requires that no reference to the property bag be retained by the object when the **Load** method returns.</span></span>
-   <span data-ttu-id="c8f94-152">Si se devuelve una referencia de objeto CLR a código nativo, el contenedor al que se puede llamar en tiempo de ejecución renuncia a su referencia al objeto CLR cuando se realiza la llamada final del contenedor al que se puede llamar en [**tiempo de ejecución**](/windows/win32/api/unknwn/nf-unknwn-iunknown-release) , pero el objeto CLR subyacente no se finaliza hasta que no se recolecta como elemento no utilizado.</span><span class="sxs-lookup"><span data-stu-id="c8f94-152">If a CLR object reference is returned to native code, the Runtime Callable Wrapper relinquishes its reference to the CLR object when the Runtime Callable Wrapper's final call to [**Release**](/windows/win32/api/unknwn/nf-unknwn-iunknown-release) is made, but the underlying CLR object is not finalized until it is garbage-collected.</span></span> <span data-ttu-id="c8f94-153">La finalización no determinista puede entrar en conflicto con algunos contratos de interfaz.</span><span class="sxs-lookup"><span data-stu-id="c8f94-153">Nondeterministic finalization can conflict with some interface contracts.</span></span> <span data-ttu-id="c8f94-154">Por ejemplo, se requieren controladores de miniaturas para liberar todos los recursos inmediatamente cuando su recuento de referencias desciende a cero.</span><span class="sxs-lookup"><span data-stu-id="c8f94-154">For example, thumbnail handlers are required to release all resources immediately when their reference count drops to zero.</span></span>

## <a name="acceptable-uses-of-managed-code-and-other-runtimes"></a><span data-ttu-id="c8f94-155">Usos aceptables de código administrado y otros runtimes</span><span class="sxs-lookup"><span data-stu-id="c8f94-155">Acceptable Uses of Managed Code and Other Runtimes</span></span>

<span data-ttu-id="c8f94-156">Es aceptable usar código administrado y otros tiempos de ejecución para implementar extensiones fuera de proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-156">It is acceptable to use managed code and other runtimes to implement out-of-process extensions.</span></span> <span data-ttu-id="c8f94-157">Entre los ejemplos de extensiones de Shell fuera de proceso se incluyen los siguientes:</span><span class="sxs-lookup"><span data-stu-id="c8f94-157">Examples of out-of-process Shell extensions include the following:</span></span>

-   <span data-ttu-id="c8f94-158">Controladores de vista previa</span><span class="sxs-lookup"><span data-stu-id="c8f94-158">Preview handlers</span></span>
-   <span data-ttu-id="c8f94-159">Acciones basadas en la línea de comandos, como las registradas en  \\  \\ subclaves de **comando** Verb del shell.</span><span class="sxs-lookup"><span data-stu-id="c8f94-159">Command-line-based actions such as those registered under **shell**\\*verb*\\**command** subkeys.</span></span>
-   <span data-ttu-id="c8f94-160">Objetos COM implementados en un servidor local, para los puntos de extensión de Shell que permiten la activación fuera de proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-160">COM objects implemented in a local server, for Shell extension points that allow out-of-process activation.</span></span>

<span data-ttu-id="c8f94-161">Algunas extensiones se pueden implementar como extensiones en proceso o fuera de proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-161">Some extensions can be implemented either as in-process or out-of-process extensions.</span></span> <span data-ttu-id="c8f94-162">Puede implementar estas extensiones como extensiones fuera de proceso si no cumplen estos requisitos para las extensiones en proceso.</span><span class="sxs-lookup"><span data-stu-id="c8f94-162">You can implement these extensions as out-of-process extensions if they do not meet these requirements for in-process extensions.</span></span> <span data-ttu-id="c8f94-163">La lista siguiente muestra ejemplos de extensiones que se pueden implementar como extensiones en proceso o fuera de proceso:</span><span class="sxs-lookup"><span data-stu-id="c8f94-163">The following list shows examples of extensions that can be implemented as either in-process or out-of-process extensions:</span></span>

-   <span data-ttu-id="c8f94-164">[**IExecuteCommand**](/windows/win32/api/shobjidl_core/nn-shobjidl_core-iexecutecommand) asociado a una entrada **DelegateExecute** registrada en una  \\  \\ subclave del **comando** Verb del shell.</span><span class="sxs-lookup"><span data-stu-id="c8f94-164">[**IExecuteCommand**](/windows/win32/api/shobjidl_core/nn-shobjidl_core-iexecutecommand) associated with a **DelegateExecute** entry registered under a **shell**\\*verb*\\**command** subkey.</span></span>
-   <span data-ttu-id="c8f94-165">[**IDropTarget**](/windows/win32/api/oleidl/nn-oleidl-idroptarget) asociado al CLSID registrado en una  \\  \\ subclave **DropTarget** de verbo de Shell.</span><span class="sxs-lookup"><span data-stu-id="c8f94-165">[**IDropTarget**](/windows/win32/api/oleidl/nn-oleidl-idroptarget) associated with the CLSID registered under a **shell**\\*verb*\\**DropTarget** subkey.</span></span>
-   <span data-ttu-id="c8f94-166">[**IExplorerCommandState**](/windows/win32/api/shobjidl_core/nn-shobjidl_core-iexplorercommandstate) asociado a una entrada **CommandStateHandler** registrada en una  \\ subclave *Verb* del shell.</span><span class="sxs-lookup"><span data-stu-id="c8f94-166">[**IExplorerCommandState**](/windows/win32/api/shobjidl_core/nn-shobjidl_core-iexplorercommandstate) associated with a **CommandStateHandler** entry registered under a **shell**\\*verb* subkey.</span></span>

 

 
