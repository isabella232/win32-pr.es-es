---
description: Describe dos tipos de almacenamiento en disco y describe los estilos de partición.
ms.assetid: 5d511654-92e0-4236-80e7-bb2417403186
title: Discos básicos y dinámicos
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: cd3b52767983c7707f619b83c93e987b51879fdd
ms.sourcegitcommit: 3d9dce1bd6c84e2b51759e940aa95aa9b459cd20
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 03/04/2021
ms.locfileid: "104003446"
---
# <a name="basic-and-dynamic-disks"></a><span data-ttu-id="ff697-103">Discos básicos y dinámicos</span><span class="sxs-lookup"><span data-stu-id="ff697-103">Basic and Dynamic Disks</span></span>

<span data-ttu-id="ff697-104">Antes de crear particiones de una unidad u obtener información acerca del diseño de la partición de una unidad, primero debe comprender las características y limitaciones de los tipos de almacenamiento en disco básico y dinámico.</span><span class="sxs-lookup"><span data-stu-id="ff697-104">Before partitioning a drive or getting information about the partition layout of a drive, you must first understand the features and limitations of basic and dynamic disk storage types.</span></span>

<span data-ttu-id="ff697-105">Para los fines de este tema, el término *volumen* se usa para hacer referencia al concepto de una partición de disco formateada con un sistema de archivos válido (normalmente NTFS) que usa el sistema operativo Windows para almacenar archivos.</span><span class="sxs-lookup"><span data-stu-id="ff697-105">For the purposes of this topic, the term *volume* is used to refer to the concept of a disk partition formatted with a valid file system, most commonly NTFS, that is used by the Windows operating system to store files.</span></span> <span data-ttu-id="ff697-106">Un volumen tiene un nombre de ruta de acceso de Win32, se puede enumerar mediante las funciones [**FindFirstVolume**](/windows/desktop/api/FileAPI/nf-fileapi-findfirstvolumew) y [**FindNextVolume**](/windows/desktop/api/FileAPI/nf-fileapi-findnextvolumew) y, normalmente, tiene una letra de unidad asignada, como C:.</span><span class="sxs-lookup"><span data-stu-id="ff697-106">A volume has a Win32 path name, can be enumerated by the [**FindFirstVolume**](/windows/desktop/api/FileAPI/nf-fileapi-findfirstvolumew) and [**FindNextVolume**](/windows/desktop/api/FileAPI/nf-fileapi-findnextvolumew) functions, and usually has a drive letter assigned to it, such as C:.</span></span> <span data-ttu-id="ff697-107">Para obtener más información acerca de los volúmenes y los sistemas de archivos, consulte [sistemas de archivos](file-systems.md).</span><span class="sxs-lookup"><span data-stu-id="ff697-107">For more information about volumes and file systems, see [File Systems](file-systems.md).</span></span>

<span data-ttu-id="ff697-108">En este tema:</span><span class="sxs-lookup"><span data-stu-id="ff697-108">In this topic:</span></span>

-   [<span data-ttu-id="ff697-109">Discos básicos</span><span class="sxs-lookup"><span data-stu-id="ff697-109">Basic Disks</span></span>](#basic-disks)
-   [<span data-ttu-id="ff697-110">Discos dinámicos</span><span class="sxs-lookup"><span data-stu-id="ff697-110">Dynamic Disks</span></span>](#dynamic-disks)
-   [<span data-ttu-id="ff697-111">Estilos de partición</span><span class="sxs-lookup"><span data-stu-id="ff697-111">Partition Styles</span></span>](#partition-styles)
    -   [<span data-ttu-id="ff697-112">registro de arranque maestro</span><span class="sxs-lookup"><span data-stu-id="ff697-112">Master Boot Record</span></span>](#master-boot-record)
    -   [<span data-ttu-id="ff697-113">tabla de particiones GUID</span><span class="sxs-lookup"><span data-stu-id="ff697-113">GUID Partition Table</span></span>](#guid-partition-table)
-   [<span data-ttu-id="ff697-114">Detección del tipo de disco</span><span class="sxs-lookup"><span data-stu-id="ff697-114">Detecting the Type of Disk</span></span>](#detecting-the-type-of-disk)
-   [<span data-ttu-id="ff697-115">Temas relacionados</span><span class="sxs-lookup"><span data-stu-id="ff697-115">Related topics</span></span>](#related-topics)

<span data-ttu-id="ff697-116">Hay dos tipos de discos al hacer referencia a los tipos de almacenamiento en este contexto: *discos básicos* y *discos dinámicos*.</span><span class="sxs-lookup"><span data-stu-id="ff697-116">There are two types of disks when referring to storage types in this context: *basic disks* and *dynamic disks*.</span></span> <span data-ttu-id="ff697-117">Tenga en cuenta que los tipos de almacenamiento que se describen aquí no son los mismos que los discos físicos o los estilos de partición, que están relacionados, pero son conceptos independientes.</span><span class="sxs-lookup"><span data-stu-id="ff697-117">Note that the storage types discussed here are not the same as physical disks or partition styles, which are related but separate concepts.</span></span> <span data-ttu-id="ff697-118">Por ejemplo, si se hace referencia a un disco básico no implica un estilo de partición determinado, también debe especificarse el estilo de partición utilizado para el disco en debate.</span><span class="sxs-lookup"><span data-stu-id="ff697-118">For example, referring to a basic disk does not imply a particular partition style—the partition style used for the disk under discussion would also need to be specified.</span></span> <span data-ttu-id="ff697-119">Para obtener una descripción simplificada de cómo se relaciona un tipo de almacenamiento en disco básico con un disco duro físico, consulte [discos y particiones de disco](disk-devices-and-partitions.md).</span><span class="sxs-lookup"><span data-stu-id="ff697-119">For a simplified description of how a basic disk storage type relates to a physical hard disk, see [Disk Devices and Partitions](disk-devices-and-partitions.md).</span></span>

## <a name="basic-disks"></a><span data-ttu-id="ff697-120">Discos básicos</span><span class="sxs-lookup"><span data-stu-id="ff697-120">Basic Disks</span></span>

<span data-ttu-id="ff697-121">Los *discos básicos* son los tipos de almacenamiento que se usan con más frecuencia con Windows.</span><span class="sxs-lookup"><span data-stu-id="ff697-121">*Basic disks* are the storage types most often used with Windows.</span></span> <span data-ttu-id="ff697-122">El término *disco básico* hace referencia a un disco que contiene particiones, como particiones primarias y unidades lógicas, y que a su vez están formateadas con un sistema de archivos para convertirse en un volumen para el almacenamiento de archivos.</span><span class="sxs-lookup"><span data-stu-id="ff697-122">The term *basic disk* refers to a disk that contains partitions, such as primary partitions and logical drives, and these in turn are usually formatted with a file system to become a volume for file storage.</span></span> <span data-ttu-id="ff697-123">Los discos básicos proporcionan una solución de almacenamiento simple que puede dar cabida a una matriz útil de escenarios de requisitos de almacenamiento variables.</span><span class="sxs-lookup"><span data-stu-id="ff697-123">Basic disks provide a simple storage solution that can accommodate a useful array of changing storage requirement scenarios.</span></span> <span data-ttu-id="ff697-124">Los discos básicos también admiten discos en clúster, discos de Instituto de ingenieros de electricidad y electrónica (IEEE) 1394 y unidades extraíbles de bus serie universal (USB).</span><span class="sxs-lookup"><span data-stu-id="ff697-124">Basic disks also support clustered disks, Institute of Electrical and Electronics Engineers (IEEE) 1394 disks, and universal serial bus (USB) removable drives.</span></span> <span data-ttu-id="ff697-125">Por compatibilidad con versiones anteriores, los discos básicos suelen usar el mismo estilo de partición de registro de arranque maestro (MBR) que los discos utilizados por el sistema operativo Microsoft MS-DOS y todas las versiones de Windows, pero también pueden admitir particiones de tabla de particiones **GUID** (GPT) en sistemas que lo admitan.</span><span class="sxs-lookup"><span data-stu-id="ff697-125">For backward compatibility, basic disks usually use the same Master Boot Record (MBR) partition style as the disks used by the Microsoft MS-DOS operating system and all versions of Windows but can also support **GUID** Partition Table (GPT) partitions on systems that support it.</span></span> <span data-ttu-id="ff697-126">Para obtener más información acerca de los estilos de partición MBR y GPT, consulte la sección [estilos de partición](#partition-styles) .</span><span class="sxs-lookup"><span data-stu-id="ff697-126">For more information about MBR and GPT partition styles, see the [Partition Styles](#partition-styles) section.</span></span>

<span data-ttu-id="ff697-127">Puedes agregar más espacio a las particiones existentes principales y unidades lógicas ampliándolas con espacio sin asignar, contiguo y adyacente en el mismo disco.</span><span class="sxs-lookup"><span data-stu-id="ff697-127">You can add more space to existing primary partitions and logical drives by extending them into adjacent, contiguous unallocated space on the same disk.</span></span> <span data-ttu-id="ff697-128">Para extender un volumen básico, debe estar formateado con el sistema de archivos NTFS.</span><span class="sxs-lookup"><span data-stu-id="ff697-128">To extend a basic volume, it must be formatted with the NTFS file system.</span></span> <span data-ttu-id="ff697-129">Puedes ampliar una unidad lógica en el espacio disponible contiguo en la partición extendida que lo contiene.</span><span class="sxs-lookup"><span data-stu-id="ff697-129">You can extend a logical drive within contiguous free space in the extended partition that contains it.</span></span> <span data-ttu-id="ff697-130">Si se extiende a una unidad lógica más allá del espacio disponible en la partición extendida, la partición extendida crece para incluir la unidad lógica siempre que la partición extendida vaya seguida de un espacio contiguo y sin asignar.</span><span class="sxs-lookup"><span data-stu-id="ff697-130">If you extend a logical drive beyond the free space available in the extended partition, the extended partition grows to contain the logical drive as long as the extended partition is followed by contiguous unallocated space.</span></span> <span data-ttu-id="ff697-131">Para obtener más información, vea [Cómo funcionan los discos y volúmenes básicos](/previous-versions/windows/it-pro/windows-server-2003/cc739412(v=ws.10)).</span><span class="sxs-lookup"><span data-stu-id="ff697-131">For more information, see [How Basic Disks and Volumes Work](/previous-versions/windows/it-pro/windows-server-2003/cc739412(v=ws.10)).</span></span>

<span data-ttu-id="ff697-132">Las siguientes operaciones solo se pueden realizar en discos básicos:</span><span class="sxs-lookup"><span data-stu-id="ff697-132">The following operations can be performed only on basic disks:</span></span>

-   <span data-ttu-id="ff697-133">Crear y eliminar particiones principales y extendidas.</span><span class="sxs-lookup"><span data-stu-id="ff697-133">Create and delete primary and extended partitions.</span></span>
-   <span data-ttu-id="ff697-134">Crear y eliminar unidades lógicas dentro de una partición extendida.</span><span class="sxs-lookup"><span data-stu-id="ff697-134">Create and delete logical drives within an extended partition.</span></span>
-   <span data-ttu-id="ff697-135">Dar formato a una partición y marcarla como activa.</span><span class="sxs-lookup"><span data-stu-id="ff697-135">Format a partition and mark it as active.</span></span>

## <a name="dynamic-disks"></a><span data-ttu-id="ff697-136">Discos dinámicos</span><span class="sxs-lookup"><span data-stu-id="ff697-136">Dynamic Disks</span></span>

> [!NOTE]
> <span data-ttu-id="ff697-137">En el caso de todos los usos excepto los volúmenes de arranque reflejado (mediante un volumen reflejado para hospedar el sistema operativo), los discos dinámicos están desusados.</span><span class="sxs-lookup"><span data-stu-id="ff697-137">For all usages except mirror boot volumes (using a mirror volume to host the operating system), dynamic disks are deprecated.</span></span> <span data-ttu-id="ff697-138">En el caso de los datos que requieren resistencia frente a errores de unidad, use espacios de almacenamiento, una solución de virtualización de almacenamiento resistente.</span><span class="sxs-lookup"><span data-stu-id="ff697-138">For data that requires resiliency against drive failure, use Storage Spaces, a resilient storage virtualization solution.</span></span> <span data-ttu-id="ff697-139">Para obtener más información, consulte [Introducción a los espacios de almacenamiento](/windows-server/storage/storage-spaces/overview).</span><span class="sxs-lookup"><span data-stu-id="ff697-139">For more info, see [Storage Spaces Overview](/windows-server/storage/storage-spaces/overview).</span></span>

<span data-ttu-id="ff697-140">Los *discos dinámicos* proporcionan características que no son de los discos básicos, como la capacidad de crear volúmenes que abarquen varios discos (volúmenes distribuidos y seccionados) y la capacidad de crear volúmenes tolerantes a errores (volúmenes reflejados y RAID-5).</span><span class="sxs-lookup"><span data-stu-id="ff697-140">*Dynamic disks* provide features that basic disks do not, such as the ability to create volumes that span multiple disks (spanned and striped volumes) and the ability to create fault-tolerant volumes (mirrored and RAID-5 volumes).</span></span> <span data-ttu-id="ff697-141">Al igual que los discos básicos, los discos dinámicos pueden usar los estilos de partición MBR o GPT en los sistemas que admiten ambos.</span><span class="sxs-lookup"><span data-stu-id="ff697-141">Like basic disks, dynamic disks can use the MBR or GPT partition styles on systems that support both.</span></span> <span data-ttu-id="ff697-142">Todos los volúmenes de los discos dinámicos se conocen como volúmenes dinámicos.</span><span class="sxs-lookup"><span data-stu-id="ff697-142">All volumes on dynamic disks are known as dynamic volumes.</span></span> <span data-ttu-id="ff697-143">Los discos dinámicos ofrecen una mayor flexibilidad para la administración de volúmenes porque usan una base de datos para realizar un seguimiento de la información acerca de los volúmenes dinámicos del disco y de otros discos dinámicos del equipo.</span><span class="sxs-lookup"><span data-stu-id="ff697-143">Dynamic disks offer greater flexibility for volume management because they use a database to track information about dynamic volumes on the disk and about other dynamic disks in the computer.</span></span> <span data-ttu-id="ff697-144">Dado que cada disco dinámico de un equipo almacena una réplica de la base de datos de disco dinámico, por ejemplo, una base de datos de disco dinámico dañada puede reparar un disco dinámico mediante la base de datos de otro disco dinámico.</span><span class="sxs-lookup"><span data-stu-id="ff697-144">Because each dynamic disk in a computer stores a replica of the dynamic disk database, for example, a corrupted dynamic disk database can repair one dynamic disk by using the database on another dynamic disk.</span></span> <span data-ttu-id="ff697-145">La ubicación de la base de datos viene determinada por el estilo de partición del disco.</span><span class="sxs-lookup"><span data-stu-id="ff697-145">The location of the database is determined by the partition style of the disk.</span></span> <span data-ttu-id="ff697-146">En las particiones MBR, la base de datos se encuentra en el último megabyte (MB) del disco.</span><span class="sxs-lookup"><span data-stu-id="ff697-146">On MBR partitions, the database is contained in the last 1 megabyte (MB) of the disk.</span></span> <span data-ttu-id="ff697-147">En las particiones GPT, la base de datos se encuentra en una partición reservada de 1 MB (oculta).</span><span class="sxs-lookup"><span data-stu-id="ff697-147">On GPT partitions, the database is contained in a 1-MB reserved (hidden) partition.</span></span>

<span data-ttu-id="ff697-148">Los discos dinámicos son una forma independiente de administración de volúmenes que permite a los volúmenes tener extensiones no contiguas en uno o más discos físicos.</span><span class="sxs-lookup"><span data-stu-id="ff697-148">Dynamic disks are a separate form of volume management that allows volumes to have noncontiguous extents on one or more physical disks.</span></span> <span data-ttu-id="ff697-149">Los discos dinámicos y los volúmenes se basan en el administrador de discos lógicos (LDM) y el servicio de disco virtual (VDS) y sus características asociadas.</span><span class="sxs-lookup"><span data-stu-id="ff697-149">Dynamic disks and volumes rely on the Logical Disk Manager (LDM) and Virtual Disk Service (VDS) and their associated features.</span></span> <span data-ttu-id="ff697-150">Estas características permiten realizar tareas como la conversión de discos básicos en discos dinámicos y la creación de volúmenes tolerantes a errores.</span><span class="sxs-lookup"><span data-stu-id="ff697-150">These features enable you to perform tasks such as converting basic disks into dynamic disks, and creating fault-tolerant volumes.</span></span> <span data-ttu-id="ff697-151">Para fomentar el uso de discos dinámicos, se ha quitado la compatibilidad con volúmenes de varias particiones de los discos básicos y ahora se admite exclusivamente en discos dinámicos.</span><span class="sxs-lookup"><span data-stu-id="ff697-151">To encourage the use of dynamic disks, multi-partition volume support was removed from basic disks, and is now exclusively supported on dynamic disks.</span></span>

<span data-ttu-id="ff697-152">Las siguientes operaciones solo se pueden realizar en discos dinámicos:</span><span class="sxs-lookup"><span data-stu-id="ff697-152">The following operations can be performed only on dynamic disks:</span></span>

-   <span data-ttu-id="ff697-153">Crear y eliminar volúmenes simples, distribuidos, seccionados, reflejados y RAID-5.</span><span class="sxs-lookup"><span data-stu-id="ff697-153">Create and delete simple, spanned, striped, mirrored, and RAID-5 volumes.</span></span>
-   <span data-ttu-id="ff697-154">Extender un volumen simple o distribuido.</span><span class="sxs-lookup"><span data-stu-id="ff697-154">Extend a simple or spanned volume.</span></span>
-   <span data-ttu-id="ff697-155">Quitar un reflejo de un volumen reflejado o dividir el volumen reflejado en dos volúmenes.</span><span class="sxs-lookup"><span data-stu-id="ff697-155">Remove a mirror from a mirrored volume or break the mirrored volume into two volumes.</span></span>
-   <span data-ttu-id="ff697-156">Reparar volúmenes reflejados o RAID-5.</span><span class="sxs-lookup"><span data-stu-id="ff697-156">Repair mirrored or RAID-5 volumes.</span></span>
-   <span data-ttu-id="ff697-157">Reactivar un disco que falta o que está sin conexión.</span><span class="sxs-lookup"><span data-stu-id="ff697-157">Reactivate a missing or offline disk.</span></span>

<span data-ttu-id="ff697-158">Otra diferencia entre los discos básicos y los dinámicos es que los volúmenes de discos dinámicos pueden estar compuestos por un conjunto de extensiones no contiguas en uno o varios discos físicos.</span><span class="sxs-lookup"><span data-stu-id="ff697-158">Another difference between basic and dynamic disks is that dynamic disk volumes can be composed of a set of noncontiguous extents on one or multiple physical disks.</span></span> <span data-ttu-id="ff697-159">Por el contrario, un volumen en un disco básico se compone de un conjunto de extensiones contiguas en un único disco.</span><span class="sxs-lookup"><span data-stu-id="ff697-159">By contrast, a volume on a basic disk consists of one set of contiguous extents on a single disk.</span></span> <span data-ttu-id="ff697-160">Debido a la ubicación y el tamaño del espacio en disco que necesita la base de datos LDM, Windows no puede convertir un disco básico a un disco dinámico a menos que haya al menos 1 MB de espacio sin usar en el disco.</span><span class="sxs-lookup"><span data-stu-id="ff697-160">Because of the location and size of the disk space needed by the LDM database, Windows cannot convert a basic disk to a dynamic disk unless there is at least 1 MB of unused space on the disk.</span></span>

<span data-ttu-id="ff697-161">Independientemente de si los discos dinámicos de un sistema usan el estilo de partición MBR o GPT, puede crear hasta 2.000 volúmenes dinámicos en un sistema, aunque el número recomendado de volúmenes dinámicos es 32 o menos.</span><span class="sxs-lookup"><span data-stu-id="ff697-161">Regardless of whether the dynamic disks on a system use the MBR or GPT partition style, you can create up to 2,000 dynamic volumes on a system, although the recommended number of dynamic volumes is 32 or less.</span></span> <span data-ttu-id="ff697-162">Para obtener información detallada y otras consideraciones sobre el uso de volúmenes y discos dinámicos, consulte [volúmenes y discos dinámicos](/previous-versions/windows/it-pro/windows-server-2003/cc757696(v=ws.10)).</span><span class="sxs-lookup"><span data-stu-id="ff697-162">For details and other considerations about using dynamic disks and volumes, see [Dynamic disks and volumes](/previous-versions/windows/it-pro/windows-server-2003/cc757696(v=ws.10)).</span></span>

<span data-ttu-id="ff697-163">Para obtener más características de y escenarios de uso de discos dinámicos, consulte [¿Qué son los discos y volúmenes dinámicos?](/previous-versions/windows/it-pro/windows-server-2003/cc737048(v=ws.10)).</span><span class="sxs-lookup"><span data-stu-id="ff697-163">For more features of and usage scenarios for dynamic disks, see [What Are Dynamic Disks and Volumes?](/previous-versions/windows/it-pro/windows-server-2003/cc737048(v=ws.10)).</span></span>

<span data-ttu-id="ff697-164">Las operaciones comunes a los discos básicos y dinámicos son las siguientes:</span><span class="sxs-lookup"><span data-stu-id="ff697-164">The operations common to basic and dynamic disks are the following:</span></span>

-   <span data-ttu-id="ff697-165">Admite los estilos de partición MBR y GPT.</span><span class="sxs-lookup"><span data-stu-id="ff697-165">Support both MBR and GPT partition styles.</span></span>
-   <span data-ttu-id="ff697-166">Compruebe las propiedades del disco, como la capacidad, el espacio libre disponible y el estado actual.</span><span class="sxs-lookup"><span data-stu-id="ff697-166">Check disk properties, such as capacity, available free space, and current status.</span></span>
-   <span data-ttu-id="ff697-167">Ver las propiedades de la partición, como desplazamiento, longitud, tipo y, si la partición se puede usar como el volumen del sistema en el arranque.</span><span class="sxs-lookup"><span data-stu-id="ff697-167">View partition properties, such as offset, length, type, and if the partition can be used as the system volume at boot.</span></span>
-   <span data-ttu-id="ff697-168">Ver las propiedades del volumen, como el tamaño, la asignación de la letra de la unidad, la etiqueta, el tipo, el nombre de la ruta de acceso de Win32, el tipo de partición y el sistema de archivos.</span><span class="sxs-lookup"><span data-stu-id="ff697-168">View volume properties, such as size, drive-letter assignment, label, type, Win32 path name, partition type, and file system.</span></span>
-   <span data-ttu-id="ff697-169">Establezca asignaciones de letra de unidad para volúmenes de disco o particiones, y para dispositivos de CD-ROM.</span><span class="sxs-lookup"><span data-stu-id="ff697-169">Establish drive-letter assignments for disk volumes or partitions, and for CD-ROM devices.</span></span>
-   <span data-ttu-id="ff697-170">Convertir un disco básico en un disco dinámico o en un disco dinámico en un disco básico.</span><span class="sxs-lookup"><span data-stu-id="ff697-170">Convert a basic disk to a dynamic disk, or a dynamic disk to a basic disk.</span></span>

<span data-ttu-id="ff697-171">A menos que se especifique lo contrario, Windows particiona inicialmente una unidad como un disco básico de forma predeterminada.</span><span class="sxs-lookup"><span data-stu-id="ff697-171">Unless specified otherwise, Windows initially partitions a drive as a basic disk by default.</span></span> <span data-ttu-id="ff697-172">Debe convertir explícitamente un disco básico a un disco dinámico.</span><span class="sxs-lookup"><span data-stu-id="ff697-172">You must explicitly convert a basic disk to a dynamic disk.</span></span> <span data-ttu-id="ff697-173">Sin embargo, hay consideraciones de espacio en disco que se deben tener en cuenta antes de intentar hacerlo.</span><span class="sxs-lookup"><span data-stu-id="ff697-173">However, there are disk space considerations that must be accounted for before you attempt to do this.</span></span> <span data-ttu-id="ff697-174">Para obtener más información, consulte [How to Convert to Basic and Dynamic Disks in Windows XP Professional](https://support.microsoft.com/kb/309044).</span><span class="sxs-lookup"><span data-stu-id="ff697-174">For more information, see [How To Convert to Basic and Dynamic Disks in Windows XP Professional](https://support.microsoft.com/kb/309044).</span></span>

## <a name="partition-styles"></a><span data-ttu-id="ff697-175">Estilos de partición</span><span class="sxs-lookup"><span data-stu-id="ff697-175">Partition Styles</span></span>

<span data-ttu-id="ff697-176">Los *estilos de partición*, que a veces se denominan *esquemas de partición*, son un término que hace referencia a la estructura subyacente concreta del diseño del disco y cómo se organizan realmente las particiones, cuáles son las capacidades y también cuáles son las limitaciones.</span><span class="sxs-lookup"><span data-stu-id="ff697-176">*Partition styles*, also sometimes called *partition schemes*, is a term that refers to the particular underlying structure of the disk layout and how the partitioning is actually arranged, what the capabilities are, and also what the limitations are.</span></span> <span data-ttu-id="ff697-177">Para arrancar Windows, las implementaciones de BIOS en equipos basados en x86 y x64 requieren un disco básico que debe contener al menos una partición de registro de arranque maestro (MBR) marcada como activa, donde la información sobre el sistema operativo Windows (pero no necesariamente toda la instalación del sistema operativo) y dónde se almacena la información acerca de las particiones en el disco.</span><span class="sxs-lookup"><span data-stu-id="ff697-177">To boot Windows, the BIOS implementations in x86-based and x64-based computers require a basic disk that must contain at least one master boot record (MBR) partition marked as active where information about the Windows operating system (but not necessarily the entire operating system installation) and where information about the partitions on the disk are stored.</span></span> <span data-ttu-id="ff697-178">Esta información se coloca en lugares independientes, y estos dos lugares pueden encontrarse en particiones independientes o en una sola partición.</span><span class="sxs-lookup"><span data-stu-id="ff697-178">This information is placed in separate places, and these two places may be located in separate partitions or in a single partition.</span></span> <span data-ttu-id="ff697-179">El resto del almacenamiento en disco físico se puede configurar como varias combinaciones de los dos estilos de partición disponibles, que se describen en las secciones siguientes.</span><span class="sxs-lookup"><span data-stu-id="ff697-179">All other physical disk storage can be set up as various combinations of the two available partition styles, described in the following sections.</span></span> <span data-ttu-id="ff697-180">Para obtener más información sobre otros tipos de sistema, vea el tema de TechNet sobre los [estilos de partición](/previous-versions/windows/it-pro/windows-server-2003/cc738081(v=ws.10)).</span><span class="sxs-lookup"><span data-stu-id="ff697-180">For more information about other system types, see the TechNet topic on [partition styles](/previous-versions/windows/it-pro/windows-server-2003/cc738081(v=ws.10)).</span></span>

<span data-ttu-id="ff697-181">Los discos dinámicos siguen escenarios de uso ligeramente diferentes, como se ha descrito anteriormente, y la forma en que usan los dos estilos de partición se ve afectada por ese uso.</span><span class="sxs-lookup"><span data-stu-id="ff697-181">Dynamic disks follow slightly different usage scenarios, as previously outlined, and the way they utilize the two partition styles is affected by that usage.</span></span> <span data-ttu-id="ff697-182">Dado que los discos dinámicos no se suelen usar para contener volúmenes de arranque del sistema, este debate se simplifica para excluir escenarios de casos especiales.</span><span class="sxs-lookup"><span data-stu-id="ff697-182">Because dynamic disks are not generally used to contain system boot volumes, this discussion is simplified to exclude special-case scenarios.</span></span> <span data-ttu-id="ff697-183">Para obtener información más detallada sobre los diseños de bloques de datos de particiones y los escenarios de uso de disco básico o dinámico relacionados con los estilos de partición, vea cómo funcionan los [discos y volúmenes básicos](/previous-versions/windows/it-pro/windows-server-2003/cc739412(v=ws.10)) y [Cómo funcionan los discos dinámicos y los volúmenes](/previous-versions/windows/it-pro/windows-server-2003/cc758035(v=ws.10)).</span><span class="sxs-lookup"><span data-stu-id="ff697-183">For more detailed information about partition data block layouts, and basic or dynamic disk usage scenarios related to partition styles, see [How Basic Disks and Volumes Work](/previous-versions/windows/it-pro/windows-server-2003/cc739412(v=ws.10)) and [How Dynamic Disks and Volumes Work](/previous-versions/windows/it-pro/windows-server-2003/cc758035(v=ws.10)).</span></span>

### <a name="master-boot-record"></a><span data-ttu-id="ff697-184">registro de arranque maestro</span><span class="sxs-lookup"><span data-stu-id="ff697-184">Master Boot Record</span></span>

<span data-ttu-id="ff697-185">Todos los equipos basados en x86 y x64 que ejecutan Windows pueden usar el estilo de partición conocido como *registro de arranque maestro* (MBR).</span><span class="sxs-lookup"><span data-stu-id="ff697-185">All x86-based and x64-based computers running Windows can use the partition style known as *master boot record* (MBR).</span></span> <span data-ttu-id="ff697-186">El estilo de partición MBR contiene una tabla de particiones que describe dónde se encuentran las particiones en el disco.</span><span class="sxs-lookup"><span data-stu-id="ff697-186">The MBR partition style contains a partition table that describes where the partitions are located on the disk.</span></span> <span data-ttu-id="ff697-187">Dado que MBR es el único estilo de partición disponible en equipos basados en x86 anteriores a Windows Server 2003 con Service Pack 1 (SP1), no es necesario elegir este estilo.</span><span class="sxs-lookup"><span data-stu-id="ff697-187">Because MBR is the only partition style available on x86-based computers prior to Windows Server 2003 with Service Pack 1 (SP1), you do not need to choose this style.</span></span> <span data-ttu-id="ff697-188">Se usa automáticamente.</span><span class="sxs-lookup"><span data-stu-id="ff697-188">It is used automatically.</span></span>

<span data-ttu-id="ff697-189">Puede crear hasta cuatro particiones en un disco básico mediante el esquema de partición MBR: cuatro particiones primarias o tres principales y una extendida.</span><span class="sxs-lookup"><span data-stu-id="ff697-189">You can create up to four partitions on a basic disk using the MBR partition scheme: either four primary partitions, or three primary and one extended.</span></span> <span data-ttu-id="ff697-190">La partición extendida puede contener una o más unidades lógicas.</span><span class="sxs-lookup"><span data-stu-id="ff697-190">The extended partition can contain one or more logical drives.</span></span> <span data-ttu-id="ff697-191">En la ilustración siguiente se muestra un diseño de ejemplo de tres particiones primarias y una partición extendida en un disco básico mediante MBR.</span><span class="sxs-lookup"><span data-stu-id="ff697-191">The following figure illustrates an example layout of three primary partitions and one extended partition on a basic disk using MBR.</span></span> <span data-ttu-id="ff697-192">La partición extendida contiene cuatro unidades lógicas extendidas.</span><span class="sxs-lookup"><span data-stu-id="ff697-192">The extended partition contains four extended logical drives within it.</span></span> <span data-ttu-id="ff697-193">La partición extendida puede o no estar ubicada al final del disco, pero siempre es un único espacio contiguo para las unidades lógicas 1-n.</span><span class="sxs-lookup"><span data-stu-id="ff697-193">The extended partition may or may not be located at the end of the disk, but it is always a single contiguous space for logical drives 1-n.</span></span>

![tres particiones primarias y una partición extendida en un disco básico mediante MBR](images/basic-mbr.png)

<span data-ttu-id="ff697-195">Se puede dar formato a cada partición, ya sea principal o extendida, para que sea un volumen de Windows, con una correlación uno a uno de volumen a partición.</span><span class="sxs-lookup"><span data-stu-id="ff697-195">Each partition, whether primary or extended, can be formatted to be a Windows volume, with a one-to-one correlation of volume-to-partition.</span></span> <span data-ttu-id="ff697-196">En otras palabras, una sola partición no puede contener más de un único volumen.</span><span class="sxs-lookup"><span data-stu-id="ff697-196">In other words, a single partition cannot contain more than a single volume.</span></span> <span data-ttu-id="ff697-197">En este ejemplo, hay un total de siete volúmenes disponibles para el almacenamiento de archivos en Windows.</span><span class="sxs-lookup"><span data-stu-id="ff697-197">In this example, there would be a total of seven volumes available to Windows for file storage.</span></span> <span data-ttu-id="ff697-198">Una partición sin formato no está disponible para el almacenamiento de archivos en Windows.</span><span class="sxs-lookup"><span data-stu-id="ff697-198">An unformatted partition is not available for file storage in Windows.</span></span>

<span data-ttu-id="ff697-199">El diseño MBR del disco dinámico es muy similar al diseño MBR del disco básico, excepto en que solo se permite una partición principal (denominada partición LDM), no se permite la creación de particiones extendida y hay una partición oculta al final del disco para la base de datos LDM.</span><span class="sxs-lookup"><span data-stu-id="ff697-199">The dynamic disk MBR layout looks very similar to the basic disk MBR layout, except that only one primary partition is allowed (referred to as the LDM partition), no extended partitioning is allowed, and there is a hidden partition at the end of the disk for the LDM database.</span></span> <span data-ttu-id="ff697-200">Para obtener más información sobre el LDM, consulte la sección [discos dinámicos](#basic-and-dynamic-disks) .</span><span class="sxs-lookup"><span data-stu-id="ff697-200">For more information on the LDM, see the [Dynamic Disks](#basic-and-dynamic-disks) section.</span></span>

### <a name="guid-partition-table"></a><span data-ttu-id="ff697-201">tabla de particiones GUID</span><span class="sxs-lookup"><span data-stu-id="ff697-201">GUID Partition Table</span></span>

<span data-ttu-id="ff697-202">Los sistemas que ejecutan Windows Server 2003 con SP1 y versiones posteriores pueden utilizar un estilo de partición conocido como tabla de particiones de identificador único global (**GUID**), además del estilo de partición MBR.</span><span class="sxs-lookup"><span data-stu-id="ff697-202">Systems running Windows Server 2003 with SP1 and later can use a partition style known as the globally unique identifier (**GUID**) partition table (GPT) in addition to the MBR partition style.</span></span> <span data-ttu-id="ff697-203">Un disco básico que usa el estilo de partición GPT puede tener hasta 128 particiones principales, mientras que los discos dinámicos tendrán una sola partición LDM como con la creación de particiones MBR.</span><span class="sxs-lookup"><span data-stu-id="ff697-203">A basic disk using the GPT partition style can have up to 128 primary partitions, while dynamic disks will have a single LDM partition as with MBR partitioning.</span></span> <span data-ttu-id="ff697-204">Dado que los discos básicos que usan la creación de particiones GPT no limitan a cuatro particiones, no es necesario crear particiones extendidas ni unidades lógicas.</span><span class="sxs-lookup"><span data-stu-id="ff697-204">Because basic disks using GPT partitioning do not limit you to four partitions, you do not need to create extended partitions or logical drives.</span></span>

<span data-ttu-id="ff697-205">El estilo de partición GPT también tiene las siguientes propiedades:</span><span class="sxs-lookup"><span data-stu-id="ff697-205">The GPT partition style also has the following properties:</span></span>

-   <span data-ttu-id="ff697-206">Permite particiones de más de 2 terabytes.</span><span class="sxs-lookup"><span data-stu-id="ff697-206">Allows partitions larger than 2 terabytes.</span></span>
-   <span data-ttu-id="ff697-207">Confiabilidad agregada de la protección de replicación y de comprobación de redundancia cíclica (CRC) de la tabla de particiones.</span><span class="sxs-lookup"><span data-stu-id="ff697-207">Added reliability from replication and cyclic redundancy check (CRC) protection of the partition table.</span></span>
-   <span data-ttu-id="ff697-208">Compatibilidad con **GUID** de tipo de partición adicionales definidos por fabricantes de equipos originales (OEM), fabricantes de software independientes (ISV) y otros sistemas operativos.</span><span class="sxs-lookup"><span data-stu-id="ff697-208">Support for additional partition type **GUID** s defined by original equipment manufacturers (OEMs), independent software vendors (ISVs), and other operating systems.</span></span>

<span data-ttu-id="ff697-209">En la ilustración siguiente se muestra el diseño de particiones GPT para un disco básico.</span><span class="sxs-lookup"><span data-stu-id="ff697-209">The GPT partitioning layout for a basic disk is illustrated in the following figure.</span></span>

![diseño de GPT](images/basic-gpt.png)

<span data-ttu-id="ff697-211">El área MBR de protección existe en un diseño de partición GPT para la compatibilidad con versiones anteriores de las utilidades de administración de discos que operan en MBR.</span><span class="sxs-lookup"><span data-stu-id="ff697-211">The protective MBR area exists on a GPT partition layout for backward compatibility with disk management utilities that operate on MBR.</span></span> <span data-ttu-id="ff697-212">El encabezado GPT define el intervalo de direcciones de bloques lógicos que pueden usar las entradas de partición.</span><span class="sxs-lookup"><span data-stu-id="ff697-212">The GPT header defines the range of logical block addresses that are usable by partition entries.</span></span> <span data-ttu-id="ff697-213">El encabezado GPT también define su ubicación en el disco, su **GUID** y una suma de comprobación de la redundancia cíclica de 32 bits (CRC32) que se usa para comprobar la integridad del encabezado GPT.</span><span class="sxs-lookup"><span data-stu-id="ff697-213">The GPT header also defines its location on the disk, its **GUID**, and a 32-bit cyclic redundancy check (CRC32) checksum that is used to verify the integrity of the GPT header.</span></span> <span data-ttu-id="ff697-214">Cada entrada de partición **GUID** comienza con un GUID de tipo de partición.</span><span class="sxs-lookup"><span data-stu-id="ff697-214">Each **GUID** partition entry begins with a partition type GUID.</span></span> <span data-ttu-id="ff697-215">El **GUID** de tipo de partición de 16 bytes, que es similar a un identificador del sistema en la tabla de particiones de un disco MBR, identifica el tipo de datos que contiene la partición e identifica cómo se usa la partición, por ejemplo, si se trata de un disco básico o dinámico.</span><span class="sxs-lookup"><span data-stu-id="ff697-215">The 16-byte partition type **GUID**, which is similar to a System ID in the partition table of an MBR disk, identifies the type of data that the partition contains and identifies how the partition is used, for example if it is a basic disk or a dynamic disk.</span></span> <span data-ttu-id="ff697-216">Tenga en cuenta que cada entrada de partición **GUID** tiene una copia de seguridad.</span><span class="sxs-lookup"><span data-stu-id="ff697-216">Note that each **GUID** partition entry has a backup copy.</span></span>

<span data-ttu-id="ff697-217">El diseño de particiones **GPT** de disco dinámico es similar a este ejemplo de disco básico, pero como se indicó anteriormente solo tiene una entrada de partición LDM en lugar de 1-n particiones principales, como se permite en los discos básicos.</span><span class="sxs-lookup"><span data-stu-id="ff697-217">Dynamic disk **GPT** partition layouts looks similar to this basic disk example, but as stated previously have only one LDM partition entry rather than 1-n primary partitions as allowed on basic disks.</span></span> <span data-ttu-id="ff697-218">También hay una partición de base de datos LDM oculta con una entrada de partición **GUID** correspondiente.</span><span class="sxs-lookup"><span data-stu-id="ff697-218">There is also a hidden LDM database partition with a corresponding **GUID** partition entry for it.</span></span> <span data-ttu-id="ff697-219">Para obtener más información sobre el LDM, consulte la sección [discos dinámicos](#basic-and-dynamic-disks) .</span><span class="sxs-lookup"><span data-stu-id="ff697-219">For more information on the LDM, see the [Dynamic Disks](#basic-and-dynamic-disks) section.</span></span>

## <a name="detecting-the-type-of-disk"></a><span data-ttu-id="ff697-220">Detección del tipo de disco</span><span class="sxs-lookup"><span data-stu-id="ff697-220">Detecting the Type of Disk</span></span>

<span data-ttu-id="ff697-221">No hay ninguna función específica para detectar mediante programación el tipo de disco en el que se encuentra un archivo o un directorio determinado.</span><span class="sxs-lookup"><span data-stu-id="ff697-221">There is no specific function to programmatically detect the type of disk a particular file or directory is located on.</span></span> <span data-ttu-id="ff697-222">Hay un método indirecto.</span><span class="sxs-lookup"><span data-stu-id="ff697-222">There is an indirect method.</span></span>

* <span data-ttu-id="ff697-223">Pase el archivo o la ruta de acceso del directorio a [**GetVolumePathName**](/windows/desktop/api/FileAPI/nf-fileapi-getvolumepathnamew) para obtener el punto de montaje.</span><span class="sxs-lookup"><span data-stu-id="ff697-223">Pass the file or directory path to [**GetVolumePathName**](/windows/desktop/api/FileAPI/nf-fileapi-getvolumepathnamew) to obtain the mount point.</span></span>
* <span data-ttu-id="ff697-224">Pase el punto de montaje a [**GetVolumeNameForVolumeMountPoint**](/windows/desktop/api/FileAPI/nf-fileapi-getvolumenameforvolumemountpointw) para obtener el nombre del volumen.</span><span class="sxs-lookup"><span data-stu-id="ff697-224">Pass the mount point to [**GetVolumeNameForVolumeMountPoint**](/windows/desktop/api/FileAPI/nf-fileapi-getvolumenameforvolumemountpointw) to obtain the volume name.</span></span>
* <span data-ttu-id="ff697-225">Quite la barra diagonal inversa final del nombre del volumen.</span><span class="sxs-lookup"><span data-stu-id="ff697-225">Remove the trailing backslash from the volume name.</span></span>
* <span data-ttu-id="ff697-226">Pase el nombre del volumen sin la barra diagonal inversa hacia [**CreateFile**](/windows/desktop/api/FileAPI/nf-fileapi-createfilea) para abrir el volumen.</span><span class="sxs-lookup"><span data-stu-id="ff697-226">Pass the volume name without the trailing backslash to [**CreateFile**](/windows/desktop/api/FileAPI/nf-fileapi-createfilea) to open the volume.</span></span>
* <span data-ttu-id="ff697-227">Use [**ioctl \_ volumen \_ obtener \_ \_ \_ extensiones de disco de volumen**](/windows/desktop/api/WinIoCtl/ni-winioctl-ioctl_volume_get_volume_disk_extents) con el identificador de volumen para obtener los números de disco.</span><span class="sxs-lookup"><span data-stu-id="ff697-227">Use [**IOCTL\_VOLUME\_GET\_VOLUME\_DISK\_EXTENTS**](/windows/desktop/api/WinIoCtl/ni-winioctl-ioctl_volume_get_volume_disk_extents) with the volume handle to obtain the disk numbers.</span></span>
* <span data-ttu-id="ff697-228">Use los números de disco para construir las rutas de acceso del disco, como " \\ \\ ? \\ PhysicalDrive *X*".</span><span class="sxs-lookup"><span data-stu-id="ff697-228">Use the disk numbers to construct the disk paths, such as "\\\\?\\PhysicalDrive *X*".</span></span>
* <span data-ttu-id="ff697-229">Pase cada ruta de acceso del disco a [**CreateFile**](/windows/desktop/api/FileAPI/nf-fileapi-createfilea) para abrir el disco.</span><span class="sxs-lookup"><span data-stu-id="ff697-229">Pass each disk path to [**CreateFile**](/windows/desktop/api/FileAPI/nf-fileapi-createfilea) to open the disk.</span></span>
* <span data-ttu-id="ff697-230">Use [**ioctl \_ disco \_ Get \_ Drive \_ layout \_**](/windows/desktop/api/WinIoCtl/ni-winioctl-ioctl_disk_get_drive_layout_ex) para obtener la lista de particiones.</span><span class="sxs-lookup"><span data-stu-id="ff697-230">Use [**IOCTL\_DISK\_GET\_DRIVE\_LAYOUT\_EX**](/windows/desktop/api/WinIoCtl/ni-winioctl-ioctl_disk_get_drive_layout_ex) to obtain the partition list.</span></span>
* <span data-ttu-id="ff697-231">Compruebe el valor de **origen partitiontype** para cada entrada de la lista de particiones.</span><span class="sxs-lookup"><span data-stu-id="ff697-231">Check the **PartitionType** for each entry in the partition list.</span></span>

## <a name="related-topics"></a><span data-ttu-id="ff697-232">Temas relacionados</span><span class="sxs-lookup"><span data-stu-id="ff697-232">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="ff697-233">Acerca de la administración de volúmenes</span><span class="sxs-lookup"><span data-stu-id="ff697-233">About Volume Management</span></span>](about-volume-management.md)
</dt> <dt>

<span data-ttu-id="ff697-234">[Referencia técnica de discos y volúmenes básicos](/previous-versions/windows/it-pro/windows-server-2003/cc784732(v=ws.10))</span><span class="sxs-lookup"><span data-stu-id="ff697-234">[Basic Disks and Volumes Technical Reference](/previous-versions/windows/it-pro/windows-server-2003/cc784732(v=ws.10))</span></span>
</dt> <dt>

<span data-ttu-id="ff697-235">[Referencia técnica de volúmenes y discos dinámicos](/previous-versions/windows/it-pro/windows-server-2003/cc785638(v=ws.10))</span><span class="sxs-lookup"><span data-stu-id="ff697-235">[Dynamic Disks and Volumes Technical Reference](/previous-versions/windows/it-pro/windows-server-2003/cc785638(v=ws.10))</span></span>
</dt> <dt>

[<span data-ttu-id="ff697-236">Almacenamiento básico frente a almacenamiento dinámico en Windows XP</span><span class="sxs-lookup"><span data-stu-id="ff697-236">Basic Storage Versus Dynamic Storage in Windows XP</span></span>]( https://support.microsoft.com/kb/314343/)
</dt> </dl>

 

 
