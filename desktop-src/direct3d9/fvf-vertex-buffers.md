---
description: 'Al establecer el parámetro FVF del método IDirect3DDevice9:: CreateVertexBuffer en un valor distinto de cero, que debe ser un código FVF válido, indica que el contenido del búfer se caracteriza por un código FVF.'
ms.assetid: 7cab559f-3e9d-46bd-b00f-439e0922aeeb
title: Búferes de vértices de FVF (Direct3D 9)
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: a86201c3ddc1cab6d492539caccc61c1430b3a2c
ms.sourcegitcommit: a47bd86f517de76374e4fff33cfeb613eb259a7e
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 01/06/2021
ms.locfileid: "105714675"
---
# <a name="fvf-vertex-buffers-direct3d-9"></a><span data-ttu-id="69e0a-103">Búferes de vértices de FVF (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="69e0a-103">FVF Vertex Buffers (Direct3D 9)</span></span>

<span data-ttu-id="69e0a-104">Al establecer el parámetro FVF del método [**IDirect3DDevice9:: CreateVertexBuffer**](/windows/desktop/api) en un valor distinto de cero, que debe ser un código FVF válido, indica que el contenido del búfer se caracteriza por un código FVF.</span><span class="sxs-lookup"><span data-stu-id="69e0a-104">Setting the FVF parameter of the [**IDirect3DDevice9::CreateVertexBuffer**](/windows/desktop/api) method to a nonzero value, which must be a valid FVF code, indicates that the buffer content is to be characterized by an FVF code.</span></span> <span data-ttu-id="69e0a-105">Un búfer de vértices que se crea con un código FVF se denomina búfer de vértices FVF.</span><span class="sxs-lookup"><span data-stu-id="69e0a-105">A vertex buffer that is created with an FVF code is referred to as an FVF vertex buffer.</span></span> <span data-ttu-id="69e0a-106">Algunos métodos o usos de [**IDirect3DDevice9**](/windows/desktop/api) requieren búferes de vértices de FVF y otros requieren búferes de vértices que no sean de FVF.</span><span class="sxs-lookup"><span data-stu-id="69e0a-106">Some methods or uses of [**IDirect3DDevice9**](/windows/desktop/api) require FVF vertex buffers, and others require non-FVF vertex buffers.</span></span> <span data-ttu-id="69e0a-107">Los búferes de vértices FVF son necesarios como el argumento de búfer de vértice de destino para [**IDirect3DDevice9::P rocessvertices**](/windows/win32/api/d3d9helper/nf-d3d9helper-idirect3ddevice9-processvertices).</span><span class="sxs-lookup"><span data-stu-id="69e0a-107">FVF vertex buffers are required as the destination vertex buffer argument for [**IDirect3DDevice9::ProcessVertices**](/windows/win32/api/d3d9helper/nf-d3d9helper-idirect3ddevice9-processvertices).</span></span>

<span data-ttu-id="69e0a-108">Los búferes de vértices de FVF se pueden enlazar a un flujo de datos de origen para cualquier número de flujo.</span><span class="sxs-lookup"><span data-stu-id="69e0a-108">FVF vertex buffers can be bound to a source data stream for any stream number.</span></span>

<span data-ttu-id="69e0a-109">La presencia del \_ componente XYZRHW de D3DFVF en búferes de vértices de FVF indica que se han procesado los vértices de ese búfer.</span><span class="sxs-lookup"><span data-stu-id="69e0a-109">The presence of the D3DFVF\_XYZRHW component on FVF vertex buffers indicates that the vertices in that buffer have been processed.</span></span> <span data-ttu-id="69e0a-110">Búferes de vértices usados para [**IDirect3DDevice9::P rocessvertices**](/windows/win32/api/d3d9helper/nf-d3d9helper-idirect3ddevice9-processvertices) los búferes de vértice de destino deben procesarse después.</span><span class="sxs-lookup"><span data-stu-id="69e0a-110">Vertex buffers used for [**IDirect3DDevice9::ProcessVertices**](/windows/win32/api/d3d9helper/nf-d3d9helper-idirect3ddevice9-processvertices) destination vertex buffers must be post-processed.</span></span> <span data-ttu-id="69e0a-111">Los búferes de vértices usados para las entradas de sombreador de funciones fijas pueden ser preprocesados o postprocesamiento.</span><span class="sxs-lookup"><span data-stu-id="69e0a-111">Vertex buffers used for fixed function shader inputs can be either preprocessed or postprocessed.</span></span> <span data-ttu-id="69e0a-112">Si el búfer de vértices se procesa después, el sombreador se omite realmente y los datos se pasan directamente al módulo de configuración y recorte primitivo.</span><span class="sxs-lookup"><span data-stu-id="69e0a-112">If the vertex buffer is post-processed, then the shader is effectively bypassed and the data is passed directly to the primitive clipping and setup module.</span></span>

<span data-ttu-id="69e0a-113">Los búferes de vértices de FVF se pueden usar con sombreadores de vértices.</span><span class="sxs-lookup"><span data-stu-id="69e0a-113">FVF vertex buffers can be used with vertex shaders.</span></span> <span data-ttu-id="69e0a-114">Además, los flujos de vértices pueden representar los mismos formatos de vértices que los búferes de vértices que no son FVF.</span><span class="sxs-lookup"><span data-stu-id="69e0a-114">Also, vertex streams can represent the same vertex formats that non-FVF vertex buffers can.</span></span> <span data-ttu-id="69e0a-115">No tienen que usarse para introducir datos de búferes de vértices independientes.</span><span class="sxs-lookup"><span data-stu-id="69e0a-115">They do not have to be used to input data from separate vertex buffers.</span></span> <span data-ttu-id="69e0a-116">La flexibilidad adicional de los nuevos flujos de vértices permite a las aplicaciones que necesitan mantener sus datos separados para trabajar mejor, pero no es necesario.</span><span class="sxs-lookup"><span data-stu-id="69e0a-116">The additional flexibility of the new vertex streams enables applications that need to keep their data separate to work better, but it is not required.</span></span> <span data-ttu-id="69e0a-117">Si la aplicación puede mantener los datos intercalados por adelantado, eso es un aumento del rendimiento.</span><span class="sxs-lookup"><span data-stu-id="69e0a-117">If the application can maintain interleaved data in advance, then that is a performance boost.</span></span> <span data-ttu-id="69e0a-118">Si la aplicación va a intercalar los datos solo antes de cada llamada de representación, debe habilitar la API o el hardware para hacer esto con varias secuencias.</span><span class="sxs-lookup"><span data-stu-id="69e0a-118">If the application will only interleave the data before every rendering call, then it should enable the API or hardware to do this with multiple streams.</span></span>

<span data-ttu-id="69e0a-119">Las cosas más importantes con el rendimiento del vértice es usar un vértice de 32 bytes y mantener una buena ordenación de la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="69e0a-119">The most important things with vertex performance is to use a 32-byte vertex, and to maintain good cache ordering.</span></span>

## <a name="related-topics"></a><span data-ttu-id="69e0a-120">Temas relacionados</span><span class="sxs-lookup"><span data-stu-id="69e0a-120">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="69e0a-121">Búferes de vértices</span><span class="sxs-lookup"><span data-stu-id="69e0a-121">Vertex Buffers</span></span>](vertex-buffers.md)
</dt> </dl>

 

 
