---
title: D1120 Intento no válido de dibujar mapa de bits
ms.assetid: d283d320-b812-4688-8d13-9c7718255bd8
description: 'Más información sobre: D1120: Intento no válido de dibujar mapa de bits'
keywords:
- D1120 Intento no válido de dibujar mapa de bits direct2D
topic_type:
- apiref
api_name:
- D1120 Invalid Attempt to Draw Bitmap
api_type:
- NA
ms.topic: reference
ms.date: 05/31/2018
ms.custom: seodec18
ms.openlocfilehash: 51db6fdabfaa925d871bdb3c7bed1596bd9363f9
ms.sourcegitcommit: b6fe9acffad983c14864b8fe0296f6025cb1f961
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 04/26/2021
ms.locfileid: "107996752"
---
# <a name="d1120-invalid-attempt-to-draw-bitmap"></a><span data-ttu-id="a9bdd-104">D1120: Intento no válido de dibujar mapa de bits</span><span class="sxs-lookup"><span data-stu-id="a9bdd-104">D1120: Invalid Attempt to Draw Bitmap</span></span>

<span data-ttu-id="a9bdd-105">Error en una operación porque el mapa de bits tiene la opción D2D1 \_ BITMAP OPTIONS \_ CANNOT \_ \_ DRAW.</span><span class="sxs-lookup"><span data-stu-id="a9bdd-105">An operation failed because the bitmap has the D2D1\_BITMAP\_OPTIONS\_CANNOT\_DRAW option.</span></span>





 

## <a name="possible-causes"></a><span data-ttu-id="a9bdd-106">Causas posibles</span><span class="sxs-lookup"><span data-stu-id="a9bdd-106">Possible Causes</span></span>

<span data-ttu-id="a9bdd-107">Este mensaje se produce cuando una aplicación intenta dibujar un mapa de bits marcado con D2D1 \_ BITMAP \_ OPTIONS CANNOT \_ \_ DRAW.</span><span class="sxs-lookup"><span data-stu-id="a9bdd-107">This message occurs when an application attempts to draw a bitmap flagged with D2D1\_BITMAP\_OPTIONS\_CANNOT\_DRAW.</span></span> <span data-ttu-id="a9bdd-108">Esto incluye llamadas directas a DrawBitmap, así como el dibujo de primitivas mediante un pincel de mapa de bits.</span><span class="sxs-lookup"><span data-stu-id="a9bdd-108">This includes direct calls to DrawBitmap, as well as the drawing of primitives using a bitmap brush.</span></span>

## <a name="possible-fixes"></a><span data-ttu-id="a9bdd-109">Posibles correcciones</span><span class="sxs-lookup"><span data-stu-id="a9bdd-109">Possible Fixes</span></span>

<span data-ttu-id="a9bdd-110">La marca CANNOT DRAW se usa para anotar correctamente los recursos que solo se pueden usar como destinos, como los búferes de \_ reserva de la cadena de intercambio.</span><span class="sxs-lookup"><span data-stu-id="a9bdd-110">The CANNOT\_DRAW flag is used for correctly annotating resources which are usable as targets only, such as swap chain backbuffers.</span></span> <span data-ttu-id="a9bdd-111">Sin embargo, también se permite omitir la marca CANNOT DRAW para algunos mapas de bits de destino si también se pueden \_ usar como origen.</span><span class="sxs-lookup"><span data-stu-id="a9bdd-111">However, it is also permitted to omit the CANNOT\_DRAW flag for some target bitmaps if they may also be used as a source.</span></span> <span data-ttu-id="a9bdd-112">Si este es el caso, simplemente omita la marca \_ CANNOT DRAW.</span><span class="sxs-lookup"><span data-stu-id="a9bdd-112">If this is the case, simply omit the CANNOT\_DRAW flag.</span></span> <span data-ttu-id="a9bdd-113">Si el mapa de bits debe anotarse con CANNOT DRAW debido a su uso exclusivo como destino o para permitir la capacidad de bloqueo de la CPU, considere la posibilidad de usar una textura de almacenamiento provisional con el mismo tamaño que el origen deseado, pero sin la designación \_ \_ CANNOT DRAW.</span><span class="sxs-lookup"><span data-stu-id="a9bdd-113">If the bitmap must be annotated with CANNOT\_DRAW because of its exclusive use as a target or to allow CPU-lockability, consider using a staging texture with the same size as the desired source but without the CANNOT\_DRAW designation.</span></span> <span data-ttu-id="a9bdd-114">Copie en la textura de almacenamiento provisional mediante ID3D11DeviceContext::CopyResource y use la textura de almacenamiento provisional como origen para la operación de dibujo.</span><span class="sxs-lookup"><span data-stu-id="a9bdd-114">Copy to the staging texture using ID3D11DeviceContext::CopyResource, and use the staging texture as a source for the drawing operation.</span></span> <span data-ttu-id="a9bdd-115">El uso de una textura de almacenamiento provisional da lugar a implicaciones en el rendimiento; pueden ser aceptables si el mapa de bits de origen no cambia con frecuencia.</span><span class="sxs-lookup"><span data-stu-id="a9bdd-115">The use of a staging texture gives rise to performance implications; they may be acceptable if the source bitmap does not change often.</span></span>

 

 




