---
title: Contextos de representación
description: Un contexto de representación de OpenGL es un puerto a través del cual pasan todos los comandos de OpenGL. Todos los subprocesos que realizan llamadas OpenGL deben tener un contexto de representación actual. Los contextos de representación vinculan OpenGL a los sistemas de ventanas de Windows.
ms.assetid: 9fbbb0be-2db4-4bfc-9a5c-a43d71554abc
keywords:
- OpenGL en Windows, contextos de representación
- contextos de representación OpenGL
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 53ac2ac6c5948da2b7372d600377666cd9e4e074
ms.sourcegitcommit: 2d531328b6ed82d4ad971a45a5131b430c5866f7
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 09/16/2019
ms.locfileid: "104357349"
---
# <a name="rendering-contexts"></a><span data-ttu-id="a64f8-107">Contextos de representación</span><span class="sxs-lookup"><span data-stu-id="a64f8-107">Rendering Contexts</span></span>

<span data-ttu-id="a64f8-108">Un *contexto de representación* de OpenGL es un puerto a través del cual pasan todos los comandos de OpenGL.</span><span class="sxs-lookup"><span data-stu-id="a64f8-108">An OpenGL *rendering context* is a port through which all OpenGL commands pass.</span></span> <span data-ttu-id="a64f8-109">Todos los subprocesos que realizan llamadas OpenGL deben tener un contexto de representación actual.</span><span class="sxs-lookup"><span data-stu-id="a64f8-109">Every thread that makes OpenGL calls must have a current rendering context.</span></span> <span data-ttu-id="a64f8-110">Los contextos de representación vinculan OpenGL a los sistemas de ventanas de Windows.</span><span class="sxs-lookup"><span data-stu-id="a64f8-110">Rendering contexts link OpenGL to the Windows windowing systems.</span></span>

<span data-ttu-id="a64f8-111">Una aplicación especifica un contexto de dispositivo de Windows cuando crea un contexto de representación.</span><span class="sxs-lookup"><span data-stu-id="a64f8-111">An application specifies a Windows device context when it creates a rendering context.</span></span> <span data-ttu-id="a64f8-112">Este contexto de representación es adecuado para dibujar en el dispositivo al que hace referencia el contexto de dispositivo especificado.</span><span class="sxs-lookup"><span data-stu-id="a64f8-112">This rendering context is suitable for drawing on the device that the specified device context references.</span></span> <span data-ttu-id="a64f8-113">En concreto, el contexto de representación tiene el mismo formato de píxel que el contexto del dispositivo.</span><span class="sxs-lookup"><span data-stu-id="a64f8-113">In particular, the rendering context has the same pixel format as the device context.</span></span> <span data-ttu-id="a64f8-114">Para obtener más información, vea [representar funciones de contexto](rendering-context-functions.md).</span><span class="sxs-lookup"><span data-stu-id="a64f8-114">For more information, see [Rendering Context Functions](rendering-context-functions.md).</span></span>

<span data-ttu-id="a64f8-115">A pesar de esta relación, un contexto de representación no es el mismo que un contexto de dispositivo.</span><span class="sxs-lookup"><span data-stu-id="a64f8-115">Despite this relationship, a rendering context is not the same as a device context.</span></span> <span data-ttu-id="a64f8-116">Un contexto de dispositivo contiene información pertinente para el componente de gráficos (GDI) de Windows.</span><span class="sxs-lookup"><span data-stu-id="a64f8-116">A device context contains information pertinent to the graphics component (GDI) of Windows.</span></span> <span data-ttu-id="a64f8-117">Un contexto de representación contiene información pertinente para OpenGL.</span><span class="sxs-lookup"><span data-stu-id="a64f8-117">A rendering context contains information pertinent to OpenGL.</span></span> <span data-ttu-id="a64f8-118">Un contexto de dispositivo debe especificarse explícitamente en una llamada GDI.</span><span class="sxs-lookup"><span data-stu-id="a64f8-118">A device context must be explicitly specified in a GDI call.</span></span> <span data-ttu-id="a64f8-119">Un contexto de representación está implícito en una llamada de OpenGL.</span><span class="sxs-lookup"><span data-stu-id="a64f8-119">A rendering context is implicit in an OpenGL call.</span></span> <span data-ttu-id="a64f8-120">Debe establecer el formato de píxel del contexto del dispositivo antes de crear un contexto de representación.</span><span class="sxs-lookup"><span data-stu-id="a64f8-120">You should set a device context's pixel format before creating a rendering context.</span></span>

<span data-ttu-id="a64f8-121">Un subproceso que realiza llamadas OpenGL debe tener un contexto de representación actual.</span><span class="sxs-lookup"><span data-stu-id="a64f8-121">A thread that makes OpenGL calls must have a current rendering context.</span></span> <span data-ttu-id="a64f8-122">Si una aplicación realiza llamadas OpenGL desde un subproceso que carece de contexto de representación actual, no sucede nada; la llamada no tiene ningún efecto.</span><span class="sxs-lookup"><span data-stu-id="a64f8-122">If an application makes OpenGL calls from a thread that lacks a current rendering context, nothing happens; the call has no effect.</span></span> <span data-ttu-id="a64f8-123">Normalmente, una aplicación crea un contexto de representación, lo establece como el contexto de representación actual del subproceso y, a continuación, llama a las funciones de OpenGL.</span><span class="sxs-lookup"><span data-stu-id="a64f8-123">An application commonly creates a rendering context, sets it as a thread's current rendering context, and then calls OpenGL functions.</span></span> <span data-ttu-id="a64f8-124">Cuando finaliza la llamada a funciones de OpenGL, la aplicación desacopla el contexto de representación del subproceso y, a continuación, elimina el contexto de representación.</span><span class="sxs-lookup"><span data-stu-id="a64f8-124">When it finishes calling OpenGL functions, the application uncouples the rendering context from the thread, and then deletes the rendering context.</span></span> <span data-ttu-id="a64f8-125">Una ventana puede tener varios contextos de representación que se dibujan en ella al mismo tiempo, pero un subproceso solo puede tener un contexto de representación activo actual.</span><span class="sxs-lookup"><span data-stu-id="a64f8-125">A window can have multiple rendering contexts drawing to it at one time, but a thread can have only one current, active rendering context.</span></span>

<span data-ttu-id="a64f8-126">Un contexto de representación actual tiene un contexto de dispositivo asociado.</span><span class="sxs-lookup"><span data-stu-id="a64f8-126">A current rendering context has an associated device context.</span></span> <span data-ttu-id="a64f8-127">Ese contexto de dispositivo no necesita ser el mismo contexto de dispositivo que el que se usó cuando se creó el contexto de representación, pero debe hacer referencia al mismo dispositivo y tener el mismo formato de píxeles.</span><span class="sxs-lookup"><span data-stu-id="a64f8-127">That device context need not be the same device context as that used when the rendering context was created, but it must reference the same device and have the same pixel format.</span></span>

<span data-ttu-id="a64f8-128">Un subproceso solo puede tener un contexto de representación actual.</span><span class="sxs-lookup"><span data-stu-id="a64f8-128">A thread can have only one current rendering context.</span></span> <span data-ttu-id="a64f8-129">Un contexto de representación puede ser actual a un solo subproceso.</span><span class="sxs-lookup"><span data-stu-id="a64f8-129">A rendering context can be current to only one thread.</span></span>

 

 




